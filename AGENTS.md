## 1. 在编码前先思考

**不要臆断。不要掩盖困惑。要坦诚地指出利弊权衡之处。**
在实施之前：
- 明确陈述你的假设。如有疑问，请询问。
- 若存在多种解释，予以一一列出——切勿默许选择某一种。
- 若存在更简单的解决方法，请提及。在必要时提出异议。
- 若有不清楚之处，请停止思考。指出令人困惑的地方，并提出疑问。

## 2. "简洁至上"

**解决该问题所需的最简代码。绝无推测成分。**
- 没有超出要求之外的功能。
- 不会为一次性使用的代码进行抽象处理。
- 不会提供未被要求的“灵活性”或“可配置性”。
- 不会为不可能出现的情况进行错误处理。
- 如果你写了 200 行代码，而实际上只需 50 行，那就重写它。

问问自己：“资深工程师会说这个设计过于复杂吗？”如果是这样，那就简化一下。

## 3. 手术改变

**只触及你必须触及的事物。只清理你自己的“污迹”。**
在编辑现有代码时：
- 不要修改相邻的代码、注释或格式。
- 不要对没有问题的部分进行重构。
- 保持现有风格，即便你的想法有所不同也应如此。
- 如果发现无关的无用代码，请提及——不要删除它。

当你的修改导致出现孤变量时：
- 清除因你的修改而变得无用的导入语句、变量和函数。
- 除非被要求，否则不要删除原有的无效代码。

测试要求：每一处改动的代码都应直接追溯到用户的操作请求。

## 4. 目标导向型执行

**明确成功标准。持续循环直至验证通过。**
将任务转化为可验证的目标：
- “添加验证” → “为无效输入编写测试，然后确保其能够通过”
- “修复错误” → “编写重现该错误的测试，然后确保其能够通过”
- “重构 X” → “在前后两次测试中确保其都能通过”

对于需要分步完成的任务，先概述一个简要的计划：

```
1. [步骤] → 验证：[检查]
2. [步骤] → 验证：[检查]
3. [步骤] → 验证：[检查]
```

明确的衡量标准能让你独立进行循环操作；模糊标准则需要先澄清。

---

# 项目速查：RAG 本地知识库问答系统

## 项目边界

- 后端：FastAPI、LangChain、ChromaDB、Ollama；前端：Vue 3、TypeScript、Vite。
- 业务主流程：文档入库、检索问答、博客同步、知识图谱提取与管理。
- 配置优先级：环境变量 > `config.ini` > 代码默认值。当前检索配置使用 Hybrid；Reranker、检索质量控制和 LLM 图谱抽取均由配置开关控制。
- 功能、接口和默认值以 `rag_knowledge/api/routes.py`、`rag_knowledge/services/`、`web/src/` 和配置文件为准；不要依据历史文档快照推断现状。

## 当前能力与入口

- 文档支持 PDF、Office、文本、Markdown、Excel、图片和视频；入库时维护文件索引与 Chroma 向量库。
- 检索支持语义、BM25、Hybrid、上下文化、多查询、可选重排与质量处理；默认只检索已审核 chunk。
- 前端提供知识库问答、博客管理、Chunk 审核、图谱候选审批和图谱管理页面。
- 管理 API 覆盖 Chunk 审核、知识库一致性检查、受控重建，以及图谱候选的审核、应用与质量检查。

## 关键约束

- 始终使用项目根目录的 `venv`；不要用系统 Python、`.venv` 或其他 Chroma 版本打开同一 `chroma_db`。依赖版本以 `requirements-base.txt` 为准。
- 操作正式 `chroma_db`、`data/` 或关系数据库前，先停止后端和评估进程。重建通过 `/rebuild` 或 `RebuildCoordinator` 执行，不要手动拼接清库流程。
- `pytest` 默认隔离运行时路径并排除 `integration` 标记；访问正式库的测试必须显式执行 `pytest -m integration`。
- 嵌入模型变更后必须重建知识库；重建会改变 chunk ID，相关评测集需要重新生成。
- 知识库回答必须基于检索上下文并附来源；联网搜索只能由请求显式开启，且与知识库来源分开标记。

## 常用命令

```powershell
# 后端依赖、启动与测试
.\venv\Scripts\python.exe -m pip install -r requirements-dev.txt
.\venv\Scripts\python.exe run.py
.\venv\Scripts\python.exe -m pytest

# 交付前只读检查
.\venv\Scripts\python.exe scripts/check_repo_hygiene.py
```

```bash
# 前端
cd web
npm install
npm run dev
npm run check
```

Docker 生产部署、卷挂载和验收要求见 [`deploy/README.md`](deploy/README.md)。

## SVN 交付

- 唯一提交目标是主干：`https://192.168.10.251:8443/svn/公司内部开发系统/博客_论坛/rag_python`。
- **SVN 客户端与提交步骤**：
  - SVN 命令行客户端通常使用：`C:\Program Files\SlikSvn\bin\svn.exe` (或系统中已加到 PATH 的 `svn.exe`)。
  - **避开中文路径编码问题**：若存在包含中文名称的文件/路径（例如 `docs/3_待办清单/...`），在 Windows 下直接在 Shell 传递路径参数可能会导致编码损坏报错。必须：
    1. 使用 Python 的 `subprocess.run` 和 `os.path.abspath(path)` 来调用 `svn add` 进行添加。
    2. 执行 `svn commit` 时，**先 cd 到项目根目录，以 `.` 作为路径参数执行**，例如：
       `& "C:\Program Files\SlikSvn\bin\svn.exe" commit -m "commit message" .`
  - **注销缺失文件**：提交或更新前先运行 `svn status .`，若存在移动/重命名导致的缺失文件（标记为 `!`），必须显式使用 `svn delete <path>` 进行清理，否则会阻碍提交。
- 提交或更新前先在项目目录执行 `svn info .`，确认 URL 不含 `branches/`。
- 交付前确认工作树范围，并运行仓库卫生检查；不要提交无关生成物或临时文件。
