# 2026-08-28 8·27 三份 PRD 统一收口——Deterministic Gold Matrix 验收状态

> **文档定位**：本文件为 Deterministic Gold Matrix 的薄状态索引，仅记录测试集元数据、用例分布与自动化验证结果，严禁在此逐条复制或维护第二份 Gold 内容。所有用例真源必须以 `tests/fixtures/*.json` 为准。

---

## 1. Gold 数据集与运行时验收总览

| 验收维度 | 对应文件 / 执行器 | 目标被测对象 | 用例数 | 确定性协议测试 | 真实模型 / E2E 验收 |
|---|---|---|---:|---|---|
| **Identity / Clarification Gold** | `tests/fixtures/identity_resolution_gold_v1.json`<br>`tests/test_identity_gold_matrix.py` | `EntityCandidateResolver`<br>`IdentityScopeResolver`<br>`Clarification Snapshot` | 37 | **PASS (37/37)** | **PASS** (`test_agent_real_micro_chains.py`) |
| **Text Evidence Admission Gold** | `tests/fixtures/text_evidence_admission_gold_v1.json`<br>`tests/test_text_evidence_gold_matrix.py` | `TextEvidenceAdmissionService.qualify()`<br>`resolve_entity_conflict` | 18 | **PASS (18/18)** | **PASS** (`test_agent_real_micro_chains.py`) |
| **Grounding Reviewer Gold** | `tests/fixtures/grounding_reviewer_gold_v1.json`<br>`tests/test_helper_grounding_reviewer.py` | `HelperGroundingReviewer`<br>`Claim Support Matrix` | 50 | **PASS (50/50)** | **PASS** (`test_agent_real_micro_chains.py`) |
| **真实模型 Micro-chain** | `tests/test_agent_real_micro_chains.py` | 8 组关键场景微链闭环 (Controller, Reviewer, Rewrite, Pipeline 等) | 15 | - | **PASS (15/15)** |
| **真实 Agent E2E & HTTP/SSE** | `tests/test_agent_real_online_e2e.py` | 流式管道、SSE 传输层与 QA Trace 对账、澄清锁定 | 3 | - | **PASS (3/3)** |
| **前端 Web UX 与构建** | `web` (vitest + vue-tsc) | ClarificationCard, Reasoning, Reviewer, 自由滚动 | 110 | - | **PASS (110/110)** |

---

## 2. 类别分布与覆盖要求

### 2.1 Identity / Clarification Gold (v1)

- **总 Case 数**：37
- **类别分布**：
  - `exact_canonical`: 10 条（覆盖 `StampServer`, `PipelineWebGL`, `PipelineWebRTC`, `PipelineBuilder`, `StampTools`, `StampWebGL`, `StampWebRTC`, `StampNodeServer`, `管线发布服务`, `管线更新服务`）
  - `alias`: 4 条（覆盖 `Pipeline WebGL`, `管线 WebGL`, `管线 WebRTC`, `StampGIS Server`）
  - `vague_english`: 4 条（覆盖 `pipeline`, `server`, `builder`, `stamp` 多候选召回与非实体库过滤）
  - `vague_chinese`: 1 条（覆盖 `管线`）
  - `bare_family_surface`: 2 条（覆盖 `WebGL`, `WebRTC` 在通用家族根约束下的排序与召回）
  - `typo`: 2 条（覆盖 `pipelien`, `StampSever` 拼写纠错）
  - `pronoun_followup`: 2 条（覆盖“它怎么部署？”、“它怎么配置？”等代词继承）
  - `subject_continuation`: 1 条（覆盖省略主语追问“默认端口是多少？”）
  - `subject_switch`: 1 条（覆盖显式换主体“那 StampServer 呢？”打断继承）
  - `not_required`: 3 条（覆盖普通话题问题，`entity_binding_required=False` 且防漂移不继承上一轮实体）
  - `hallucinated_candidate`: 2 条（覆盖随机伪造/未注册实体，解析为 unresolved 且 candidates 为空）
  - `different_from_isolation`: 1 条（覆盖 `different_from` 关系不得被反向拉入作为候选）
  - `callback_validation`: 4 条（覆盖“以上都不是”拒绝、有效 Snapshot 回调确认、篡改 ID 拦截、无 Snapshot 回调 fail-closed）

### 2.2 Text Evidence Admission Gold (v1)

- **总 Case 数**：18
- **四类协议覆盖**：
  - `TARGET_DIRECT` $\rightarrow$ `PASS` $\rightarrow$ `TARGET_SPECIFIC`
  - `RELATED_CONTEXT` $\rightarrow$ `PASS` $\rightarrow$ `CONTEXT_ONLY`
  - `CONFLICT` $\rightarrow$ `REJECT` $\rightarrow$ `NONE`
  - `IRRELEVANT` $\rightarrow$ `REJECT` $\rightarrow$ `NONE`
- **事故与边界覆盖**：
  - **Case A（WebGL / PipelineBuilder 隔离）**：Target 为 `PipelineWebGL`，`PipelineBuilder` 冲突片段判定为 `CONFLICT` / `REJECT` / `NONE`，污染率 = 0。
  - **Case B（PipelineWebRTC 跨文档）**：Target 为 `PipelineWebRTC`，即使文档实体不匹配或无图谱 link，文本命题直接归属目标时判定为 `TARGET_DIRECT` / `PASS` / `TARGET_SPECIFIC`，不被误杀。
  - **Case C（三维管线管理）**：Target 为 `三维管线管理`，内容描述系统通用能力但未证明直接属于该模块，判定为 `RELATED_CONTEXT` / `PASS` / `CONTEXT_ONLY`，严禁赋予 `TARGET_DIRECT`。
  - **边界难例**：
    - `target_direct_eligibility=False` 时即便语义给 `TARGET_DIRECT` 也会被代码封顶降级为 `RELATED_CONTEXT`
    - `different_from` 兄弟实体硬冲突拦截
    - 图谱路径存在但文本无支撑判定为 `IRRELEVANT` / `REJECT`
    - 仅有精确字面命中但语义无关判定为 `IRRELEVANT` / `REJECT`
    - `SemanticTaskContext` 缺失或 target 不匹配时的 fail-closed 拦截

---

## 3. 运行环境与验证基线

- **Python 环境**：项目根目录 `venv` (Python 3.11.9, pytest 9.1.1)
- **配置与生产代码状态**：
  - 零实体特判硬编码（通用规则驱动）
  - 零 legacy 双轨恢复（candidate_pipeline_v2 保持彻底删除）
- **自动化门禁**：
  - 确定性专项测试：全部通过
  - 全量非 integration 单元测试：`0 failed` (1472 passed)
  - 真实模型 Micro-chain：`0 failed` (15 passed)
  - 真实在线 E2E & HTTP/SSE：`0 failed` (3 passed)
  - 前端 Web 检查与构建：`0 failed` (110 passed, build 2.14s)
  - 仓库卫生门禁 (`scripts/check_repo_hygiene.py`)：`exit 0`

---

## 4. 最终验收与移档判定

- **Deterministic Gold DoD**：**PASS**
- **真实模型运行时 Micro-chain DoD**：**PASS**
- **HTTP/SSE E2E DoD**：**PASS**
- **Browser UX DoD**：**PASS**
- **三份 8·27 PRD 最终状态**：**已全部完成验收，达成 Definition of Done，可执行统一归档。**
