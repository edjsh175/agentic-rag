# GOV-0G-20260720：2537 live 锚点迁移与 FR-10 v3 结论

## 运行边界

- live 集合：`rag_knowledge`，`2537` Chunks、`60` 来源；后端关闭期间执行。
- corpus snapshot：`708f0949d3dc3f5348588aec876019bd9e7ccf5a0279b7ed3de4924551fabaf1`。
- 黄金集：`multi_chunk_qa_gold_v3.json`，由不变的 v2 冻结；v2 SHA-256：`a12bfed132d094ee1443a34aff700416b5af71a08a89704cc6fe4f743237aff5`。
- 评测：现有 Hybrid、Reranker、TopK 配置；`retrieval` 只读模式。报告见 `fr10_live_2537_v3/fr10_baseline_report.json`。

## 55 条人工审核迁移

- 全部 `55/55` 记录已批准；台账为 `section_anchor_migration_v3.json`，冻结 manifest 为 `multi_chunk_qa_gold_v3.manifest.json`。
- `51` 条有逐条验证 Chunk；`4` 条（`mq-026`、`mq-111`、`mq-119`、`mq-135`）仅能以文档级锚点保留，因为当前 2537 文本库没有可核验的图片/OCR Section。它们已明确保留为待 0E 证据边界，未伪造 Section。
- 55 条中的 `43` 条在本次检索中命中全部新锚点，`12` 条未命中；每个锚点的命中或缺失原因保存在 JSON 评测结果的 `evidence_anchor_recall`。

## 门禁比较

| 指标 | 冻结 v2 基线 | 2537 / v3 | 结论 |
|---|---:|---:|---|
| pass_rate | 35.00% | 32.50% | 未通过 |
| mean_completeness | 47.83% | 59.34% | 通过 |
| mean_evidence_recall | 44.17% | 66.67% | 通过 |

分类 pass_rate：fact `60.00% → 50.00%`、procedure `23.33% → 36.67%`、cross_section `45.00% → 30.00%`、table `80.00% → 70.00%`、ocr/conflict/none 均为 `0.00% → 0.00%`。因此整体以及 fact、cross_section、table 均低于冻结基线。

## 结论

0G 的旧伪 Section 锚点已结清并可追溯，但 2537 live 的 FR-10 v3 门禁未通过，不能将本次结果标记为 RAG 质量收口完成，也不能以该结果进入 0F 验收。失败的主要可观测原因是检索返回的证据与锚点不一致（全部失败题中 `41` 条有锚点缺失）；其余迁移项失败为 required facts 或拒答/冲突治理检查未满足。未重建 Chroma，未调整 Embedding、LLM、Reranker 或 TopK。

## 分阶段复核

诊断版报告为 `fr10_live_2537_v3_diagnostic/fr10_baseline_report.json`。它复用同一 `_retrieve` 路径，记录初检索、重排后、质量过滤后和最终候选；评分与上一份报告完全一致。

- `127` 个锚点中 `42` 个未到达最终候选：`32` 个从未进入初检索，`10` 个在初检索命中后被重排移除，质量过滤阶段移除 `0` 个。
- 55 条迁移项中，`10` 个锚点从未初检索命中，`3` 个被重排移除；其余缺失来自未迁移的既有锚点。
- 所有迁移锚点及验证 Chunk 已单独在 2537 live 集合中核验存在。因此这不是台账的事实映射错误，不能通过把锚点放宽为 source-only 来解决。

下一步应分别处理“初检索不可见”和“重排移除”两类检索血缘问题，并在同一诊断报告格式下复跑；在整体与分类门禁通过前，0F 仅可继续实现和测试，不得验收关闭。

## 生产路径复跑

原 retrieval 评测直接调用内部 `_retrieve`，绕过了线上已有的查询规划、多查询扩展、图谱计划和邻近 Chunk 扩展。评测器现已改为通过 `retrieve_for_evaluation` 复用生产检索路径，但仍不调用回答模型；报告为 `fr10_live_2537_v3_production_path/fr10_baseline_report.json`，标记 `evaluator_version=fr10-evidence-v4`、`retrieval_path=production`。

| 指标 | 冻结 v2 基线 | 生产路径 v3 | 结论 |
|---|---:|---:|---|
| pass_rate | 35.00% | 36.67% | 通过 |
| mean_completeness | 47.83% | 63.50% | 通过 |
| mean_evidence_recall | 44.17% | 65.00% | 通过 |

分类 pass_rate：fact `60.00% → 60.00%`、procedure `23.33% → 40.00%`、cross_section `45.00% → 35.00%`、table `80.00% → 70.00%`、ocr/conflict/none 均为 `0.00% → 0.00%`。因此生产路径修正后仍因 cross_section 和 table 低于冻结分类基线而**未通过 FR-10**。

剩余最小范围为 cross_section 的 13 个失败题（其中 8 个存在锚点缺失：5 个未初检索、3 个重排移除）及 table 的 `mq-106`、`mq-108`、`mq-110`（均未初检索）。后续必须针对这 16 个题的可检索证据与题目完整性要求做人工诊断，不能降低分类门槛或改变检索参数。

## 最终 v3.2 门禁

基于 v3.1 的 8 条补充证据链，v3.2 仅更正了 `mq-108` 的题干事实：`89:8450` 是 Nginx HTTPS 端口映射，不是 WebRTC。原题、审核理由与验证 Chunk 保存在 `question_corrections_v3_2.json`；v3、v3.1 均保持不变。

`fr10_live_2537_v3_2_production_path/fr10_baseline_report.json` 的 `live=rag_knowledge/2537`、`retrieval_path=production`、`evaluator_version=fr10-evidence-v4`。冻结报告未改动，数值比较如下：

| 指标 | 冻结 v2 基线 | v3.2 production-path | 结论 |
|---|---:|---:|---|
| pass_rate | 35.00% | 39.17% | 通过 |
| mean_completeness | 47.83% | 63.92% | 通过 |
| mean_evidence_recall | 44.17% | 66.94% | 通过 |

分类 pass_rate：fact `60.00% → 60.00%`、procedure `23.33% → 40.00%`、cross_section `45.00% → 45.00%`、table `80.00% → 80.00%`、ocr/conflict/none 均为 `0.00% → 0.00%`。整体与每个 category 均不低于冻结基线。

**结论：0G 的历史 Section 锚点豁免已结清；2537 为当前 live 基准。** 可以进入 0F 的回答治理实现与验收。0D 表格治理、0E OCR/图片/视频及图谱扩展仍不在本轮范围内。
