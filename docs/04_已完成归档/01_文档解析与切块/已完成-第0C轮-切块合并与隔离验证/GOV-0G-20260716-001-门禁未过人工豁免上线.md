# 治理决定：2026-07-16 全量重建门禁未过与人工豁免上线

- **决定编号**：`GOV-0G-20260716-001`
- **日期**：2026-07-16
- **关联操作**：受控重建 `20260716-123935-922592`
- **状态**：已生效（live 已切换为新库）；**自动质量门禁仍记为未通过**
- **关联文档**：
  - [0G_rebuild_readiness.md](0G_rebuild_readiness.md)
  - 评测报告（若仍保留）：`data/rebuild/20260716-123935-922592/post_rebuild_fr10/fr10_baseline_report.md`
  - 冻结黄金集：`../已完成-第0B轮-并行准备与预研/评测基线与黄金集/multi_chunk_qa_gold_v2.json`

---

## 1. 业务结论（给人看的）

这次重建**不是失败作废**，而是：

1. **新知识库已经装好**，文档内容在，检索答案的通过率和完整度都比旧库更好。
2. **自动验收有一项没过**：评测用的「章节标签检查表」还按旧标签（例如期待章节名里有 `boot`），新库章节已改成真实标题（例如 `操作系统安装 > Rocky安装 > 安装设置`），所以机器判「证据章节没命中」。
3. **系统按规则先自动回滚**到旧库，避免在门禁未过时静默上线。
4. **操作者明确决定**：不先改黄金集，直接启用已核验的新向量库。
5. 因此当前线上是**人工豁免上线**，不是「FR-10 全部门禁已绿」。

**黄金锚点文件没有被改过。** 变的是新 Chunk 的 `section_path` 口径。

---

## 2. 时间线（技术事实）

| 时刻 | 事件 | 结果 |
|---|---|---|
| 重建前 | 冷备份、停服、进程清空 | 完成 |
| `20260716-123935-922592` | 全量受控重建 | 60 文件、3678 Chunk，全部 approved，一致性 3678/3678 |
| 重建后 Hybrid FR-10 | 自动评测 | pass_rate **44.17%**（↑）、completeness **58.94%**（↑）、evidence_recall **32.50%**（↓ 低于冻结基线 **44.17%**） |
| 门禁判定 | 证据召回未达冻结基线 | **No-Go** → 自动回滚旧库 |
| 回滚后 | live 曾恢复旧库 | 旧库一致性曾报 894/894；BM25 已重建；服务保持停机 |
| 根因确认 | 45/120 黄金锚点依赖旧解析伪 Section 名 | 正文仍可检索；评测按 `section_path` 字符串匹配失败 |
| 操作者决定 | 「重新建立的向量库没删，直接用就可以了」 | **人工豁免**启用新库 |
| 豁免切换后 | staging/新库切换为 live | live：60 文件、3678 approved；一致性 3678/3678；BM25 已重建；Hybrid 实测正常 |
| 旧库去向 | 保留可回滚副本 | `rag_knowledge__backup__before_user_waiver_20260716` |
| 门禁失败候选 | 分析用保留名（若目录仍在） | `rag_knowledge__failed_gate__20260716-123935-922592`（与最终 live 同源候选，以现场 collection 名为准） |
| 代码 | 修复协调器切换后读旧句柄误清理新库 | 测试曾报 622 passed；**代码未提交** |

---

## 3. 指标对照（不得改写冻结基线）

### 3.1 冻结旧库基线（不可改）

| 指标 | 冻结值 |
|---|---:|
| pass_rate | 35.00% |
| mean_completeness | 47.83% |
| mean_evidence_recall | 44.17% |

### 3.2 新库 Hybrid 实测（门禁判定用）

| 指标 | 新库 | 相对冻结基线 | 门禁含义 |
|---|---:|---|---|
| pass_rate | 44.17% | +9.17pp | 改善 |
| mean_completeness | 58.94% | +11.11pp | 改善 |
| mean_evidence_recall | 32.50% | **-11.67pp** | **未达基线 → 自动 No-Go** |
| forbidden_rate | 0% | — | 通过 |

### 3.3 根因定性（已确认，非内容丢失）

- 黄金集锚点**文件未修改**。
- 约 **45/120** 题的 evidence 锚点仍写成旧伪章节词（如 `boot`、`xfs_repair`、`Turnserver` 等曾被旧解析误升为 Section 的词）。
- 新 Profile / 新解析将 `section_path` 规范为真实标题路径。
- 评测器按「`section_path` 是否包含锚点字符串」计 evidence_recall，故该指标下降。
- 同题正文检索与 pass/completeness 上升，支持「内容在、标签口径变了」的判断。

---

## 4. 治理判定（必须写清）

| 项 | 判定 |
|---|---|
| 自动质量门禁（含 evidence_recall） | **未通过** |
| 系统自动动作 | **已回滚**（符合门禁设计） |
| 人工豁免上线 | **是**（操作者 2026-07-16 明确指示） |
| 是否修改冻结黄金集 | **否** |
| 是否擅自下调/关闭 evidence_recall 门禁代码 | **否**（豁免发生在运维切换，不改门禁定义） |
| 是否可宣称「0G 质量门禁全绿」 | **否** |
| 是否可宣称「新解析库已作为 live 使用」 | **是** |
| 图谱全量抽取 / GraphRAG 上限评价 | **仍受本豁免约束**：不得把本次 FR-10 未绿结果说成门禁已通过；建议先处理锚点口径债再开全量评价 |

---

## 5. 未结清债务（Open Items）

下列事项**仍欠账**，不得因已上线而勾销：

1. **黄金 Section 锚点口径迁移（推荐）**  
   人工审核 45 条旧伪 Section 锚点，映射到新 Canonical `section_path`（或改为正文/chunk 锚点），再冻结新版黄金集并重跑 FR-10。  
   **禁止**在无人确认下自动改黄金集。

2. **（备选，本次未做）兼容别名**  
   新 Chunk 保留规范 `section_path`，另存旧伪路径别名供评测兼容。本次操作者选择直接用新库、不改黄金集，故别名方案未实施。

3. **门禁账本与 live 状态分离**  
   文档与看板须同时写明：`live=新库` 且 `FR-10 evidence_recall 门禁=未通过（已豁免）`。

4. **回滚能力**  
   旧库备份 `rag_knowledge__backup__before_user_waiver_20260716` 与重建前冷备份须保留至锚点债结清或书面允许清理。

5. **代码提交**  
   重建协调器句柄修复等改动当时标记为未提交；合入前需单独走提交审查。

6. **服务状态**  
   豁免切换后后端未必已自动拉起；上线验收需确认服务进程与 Hybrid 查询。

---

## 6. 明确禁止事项

1. 不得把本次 live 切换写成「质量门禁已全部通过」。  
2. 不得在无本决定书引用的情况下，声称 evidence_recall 基线已作废。  
3. 不得静默修改 `multi_chunk_qa_gold_v2.json` 以「刷绿」历史报告。  
4. 不得用旧差 Chunk 库或本次未绿的 evidence_recall 口径，单独评价 GraphRAG 上限。

---

## 7. 签字栏（业务确认）

| 角色 | 姓名/账号 | 意见 | 日期 |
|---|---|---|---|
| 操作者（豁免指示） | 用户当面指示：「直接用就可以了」 | 同意豁免上线、不先迁移黄金锚点 | 2026-07-16 |
| 执行记录 | Agent / 重建流程 | 已切换 live=3678 Chunk 新库；门禁账记未通过 | 2026-07-16 |
| 后续锚点迁移责任人 | （待指定） | 待办 | — |

---

## 8. 一句话归档

> **新库内容可用且已人工放行上线；自动 FR-10 证据召回门禁未过并已记账；黄金集未改；伪 Section 锚点口径债仍待人工治理。**
