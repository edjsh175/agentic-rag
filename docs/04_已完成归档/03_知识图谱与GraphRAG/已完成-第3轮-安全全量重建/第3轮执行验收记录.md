# 第 3 轮安全全量重建执行验收记录

- **记录日期**：2026-07-22（正式 execute 验收）／初记 2026-07-16／复核缺口 2026-07-20
- **状态**：**规则路径已完成**；**LLM 实抽未完成**（执行时 `192.168.10.158:11434` 不可达，编排使用空抽取 stub 使 `--include-llm` 管线跑通，`llm_candidates=0`）
- **执行报告**：`data/rebuild_safe_execute_round3.json` / `.md`
- **dry-run**：`data/rebuild_safe_dry_run_pre_round3.json` / `.md`
- **post-audit**：`data/graph_audit_post_round3.json` / `.md`
- **编排摘要**：`data/round3_orchestration_summary.json`
- **备份**：`data/backups/rag_relational_pre_round3_20260722_131948.db`
- **batch**：`9e7970eb-a6e3-4f87-aef8-cf5f92ef2f41`

## 0. 与 2026-07-20 缺口对照

| 项 | 2026-07-20 | 2026-07-22 |
|---|---|---|
| execute 报告 | 缺失 | **已生成** |
| post-audit | 缺失 | **已生成** |
| pre_round3 备份 | 缺失 | **已生成** |
| 主干完整率 | 旧 40/40 | **175/175**（替换后主干） |
| 实 LLM 抽取 | — | **未完成（Ollama 不可达）** |

## 1. 开工前门禁

| 门禁 | 结果 | 证据 |
|------|------|------|
| 停止占用 DB 的后端进程 | PASS | 执行前已停 `run.py` |
| dry-run | PASS | backbone 175/175；preserved 含 `seed:product_backbone` / `rule:profile_sync` / `rule:special_relations` |
| 正式 JSON 与库内 seed 边一致 | PASS | dry-run `backbone_integrity.complete=true` |
| 第 2.5 正式替换 | PASS | batch `a1c75bec-…`；见替换短纪要 |

## 2. 执行过程

| 项 | 结果 | 证据 |
|------|------|------|
| `rebuild-safe --execute --include-llm` | PASS（规则+管线）；LLM 空 | 报告 + orchestration `llm_mode=stub_empty_extract` |
| timestamped 备份 | PASS | `rag_relational_pre_round3_20260722_131948.db` |
| manual export | PASS | `data/manual_graph_facts_pre_round3.json` |
| 分拆审批 | PASS | approved **7876**；backbone 冲突 reject **26**；deferred reject **1078** |
| apply | PASS | entities 152→**956**；relations 185→**1150**；links 294→**6458** |
| cleanup-stale-links | PASS | stale 294→**0** |
| 主干冲突 | PASS | `backbone_after.complete=true`（175/175） |

### 审查修复（本轮代码）

首次 execute 曾因规则候选缺少 `confidence` 被阈值当成 0，导致 `approved=0` / `apply_skipped`。已修复：

- 缺失 confidence 默认 1.0（`_candidate_confidence`）
- 关系/别名/链接仅在端点实体已知时批准

并从 `rag_relational_pre_round3_20260722_125634.db` 回滚后重跑成功。

## 3. §10 指标核对表

| 指标 | 目标 | 重建前 | 重建后 | 结果 | 备注 |
|------|------|------:|------:|:---:|------|
| stale_link_count | 0 | 4068（audit 口径） | **0** | PASS | cleanup 后 |
| Section 实体占比 | 降 ≥20% | 0.781 | **0.7374** | 未达标 | 约 -5.6% 相对点；需实 LLM 补业务实体 |
| EnvironmentComponent | ≥ 10 | 6 | 待补 | 未达标 | LLM 空 |
| Command | ≥ 30 | — | 待补 | 未达标 | LLM 空 |
| Procedure | ≥ 10 | 16 | 待补 | 待复核 | 规则为主 |
| Step | ≥ 50 | 3 | 待补 | 未达标 | LLM 空 |
| ConfigItem | 明显高于重建前 | 39 | 待补 | 待复核 | |
| 主干关系完整率 | 100% | 175/175 | **175/175** | PASS | |
| Task 8.1 Gate | PASS | PASS | **PASS** | PASS | `task81_post_round3.json` |
| 实 LLM 候选 | >0 | — | **0** | FAIL | Ollama 不可达 |

## 4. 验收命令记录

```powershell
.\venv\Scripts\python.exe run_graph_build.py audit --output-json data/graph_audit_post_round3.json
.\venv\Scripts\python.exe run_graph_build.py quality --graph --profile full
.\venv\Scripts\python.exe scripts/validate_task81_graph_gate.py --json
```

- audit：completed（entities=956 / relations=1150 / stale=0）
- quality：仍有历史/`missing_evidence` 与部分 golden 缺口（exit 1，不阻断 Gate）
- Task81：`verdict=PASS`

归档：`data/archive/rebuild_reports/<stamp>_rebuild_safe_*` / `graph_audit_post_round3.*`

## 5. 结论

1. **第 3 轮 rebuild-safe 规则重建证据链已闭合**（dry-run → execute → 分拆审批 → apply → audit/Gate；主干 175/175 保留）。
2. **§10 业务实体数量目标未因实 LLM 关闭而达成**；待 `192.168.10.158:11434` 恢复后，应再跑一轮 `--include-llm`（或增量 LLM extract）补 Command/Procedure/Step 等，禁止用 stub 当作 LLM 验收通过。
3. 图谱默认仍**不**改 `config-prod.ini` 开图。
