# 实体身份与消解 — 阶段完成纪要

- **状态**：已完成（代码 + 单元验收）
- **完成日期**：2026-08-04
- **执行 PRD**：[执行PRD.md](执行PRD.md)
- **关联总览**：[../2026-07-13-知识图谱PRD剩余轮次总览.md](../../../01_当前有效/03_知识图谱与GraphRAG/2026-07-13-知识图谱PRD剩余轮次总览.md)

---

## 完成结论

建设写路径现有**单一实体身份权威**：抽取只产表面称谓；`EntityIdentityService` 裁决 `bind` / `alias_of` / `new` / `conflict` / `uncertain`；Staging 与 Apply 服从结论。已知别名（如 `StampGIS Tools → StampTools`）不可再建成独立实体。LLM 仲裁为可选后端，默认关闭，不替代确定性硬门。

---

## 实施摘要

```text
1. 新建 entity_identity.py
   - normalize_identity_key
   - 五层权威源查阅 → 五种标准结论
   - LLMIdentityArbiter（默认关；只仲裁 uncertain 候选对）
2. entity_resolution.py 全面委托 EntityIdentityService
3. pipeline Staging：非 new 实体候选自动拒绝
4. GraphCandidateApplier：别名不可 INSERT 新 entities
5. graph_governance：安全门覆盖 bind/alias_of/conflict/uncertain 等
6. graph_schema：aliases.alias 全局 UNIQUE
```

---

## 验收

| 项 | 结果 |
|----|------|
| PRD §6 八项不变量 | 已勾选 |
| `pytest`（identity + resolution + extraction + governance） | **45 passed**（2026-08-04 复跑确认） |

---

## 对后续的边界

- 本轮不负责历史脏图全量自动合并；可用同一 `resolve` 做只读审计另开任务
- 不改变生产默认开图决策；不扩大首抽 LLM 默认面
- 问答侧歧义反问规则仍与本身份子系统解耦
