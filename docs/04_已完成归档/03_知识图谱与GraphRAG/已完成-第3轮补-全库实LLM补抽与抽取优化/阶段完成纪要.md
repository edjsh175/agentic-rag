# 第 3 轮补 — 阶段完成纪要

- **状态**：已完成（建设目标 R1–R6c；经验产品化移交后续）
- **完成日期**：2026-07-27
- **执行 PRD**：[执行PRD.md](执行PRD.md)
- **关联总览**：[../2026-07-13-知识图谱PRD剩余轮次总览.md](../../../01_当前有效/03_知识图谱与GraphRAG/2026-07-13-知识图谱PRD剩余轮次总览.md)
- **关联第 3 轮规则路径**：[../已完成-第3轮-安全全量重建/第3轮执行验收记录.md](../已完成-第3轮-安全全量重建/第3轮执行验收记录.md)

---

## 完成结论

在**不改建设链路架构、不开全库图扩召回**的前提下，把第 3 轮未完成的「真实 LLM 补叶子」跑通：断点续抽、真 Ollama 抽取、噪声/方向治理、按类目扩批、保守分拆审批、实体→关系两段式回写均已落地并 apply。

**本阶段 GO 含义**：建设侧「可恢复的按类目实 LLM 补抽」可用。
**本阶段当时不 GO**（2026-07-27 收口时）：无差别全库盲抽；生产默认 `graph_retrieval.enabled`。
**后续**：第 4 轮 GraphRAG 已 PASS（见 [已完成-第4轮](../已完成-第4轮-GraphRAG实效验收/)）；生产默认开图仍未批准。

---

## 正式库变化（摘录）

```text
R5 前基线（约）：entities 956 / relations 1150
R6c 后：entities 1374 / relations 1214 / links 6583
关键叶子存量（约）：Command / Procedure / Step / ConfigItem 等有可解释增量
主干边：175/175（未破坏）
```

| 批次 | 说明 | 增量（约） |
|------|------|------------|
| `a6309a3d-…` | R5 StampTools+StampServer LLM | +114 entities / +3 relations |
| `8d323357-…` | R5b 齐套 `has_procedure` | +3 relations |
| `7c848f88-…` | R6 StampWebRTC+基础环境 | +154 entities / +2 relations |
| `6c91c1f4-…` | R6b Procedure/Step + `has_step` | +101 entities / +8 relations |
| `5bbb2199-…` | R6c possible_duplicate 放行 + `has_step` | +49 entities / +48 relations |

---

## 轮次完成对照

| 轮次 | 内容 | 结果 |
|:---:|------|:---:|
| R1 | 断点续抽 | ✅ |
| R2 | 实 LLM 冒烟 | ✅ |
| R3 | ConfigItem 噪声 / Command 召回 | ✅ |
| R4 | staging 硬拦 / 证据松绑 | ✅ |
| R4b | 方向 few-shot + 翻转 | ✅ |
| R5–R6 | 按类目扩批+审批 apply | ✅ |
| R6b–R6c | 两段式捞 `has_step`（含 diagnostic 分码） | ✅ |
| 收口对齐 | 与问答扩召回解耦；「全量」=按类目分批 | ✅ |

---

## 产物索引

| 路径 | 说明 |
|------|------|
| `data/llm_backfill_r5_*.json` / `*_apply.log` | R5 质量/审批/apply |
| `data/llm_backfill_r5b_relation_recovery.json` | R5b 关系回写 |
| `data/llm_backfill_r6_*.json` / `llm_backfill_r6_webrtc_baseenv.log` | R6 抽取与审批 |
| `data/llm_backfill_r6b_has_step_*.json` | R6b has_step 回收 |
| `data/llm_backfill_r6c_diagnostic_has_step*.json` | R6c diagnostic 放行 |
| `data/backups/rag_relational_pre_r5_llm_backfill_*.db` | R5 前备份 |
| `data/backups/rag_relational_pre_r6_apply_*.db` | R6 apply 前备份 |
| `data/backups/rag_relational_pre_r6b_has_step_*.db` | R6b 前备份 |
| `data/backups/rag_relational_pre_r6c_diagnostic_has_step_*.db` | R6c 前备份 |
| `scripts/r6_split_review_and_apply.py` | 分拆审批示范 |
| `scripts/r6b_recover_has_step.py` / `r6c_recover_diagnostic_has_step.py` | 两段式捞边示范（待收成 CLI） |

---

## 对后续的边界（移交）

1. **经验产品化**（不挡本 PRD 完成）：两段式捞边 CLI、审批策略文件化、非法边/泛名压制——见 [执行PRD §6.1](执行PRD.md)。
2. **扩面**：可按 `--doc-category` 继续博客/实景三维等；**禁止**无差别盲抽「其他」全库。
3. **问答**：第 4 轮已完成（[阶段总结](../已完成-第4轮-GraphRAG实效验收/2026-07-27-阶段总结.md)）；本地可开全图，生产模板仍关。
4. LLM batch **仍禁止** `--approve-all`；`type_conflict` **禁止**自动抬升。
