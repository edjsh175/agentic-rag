# R7 类目扩面 — 博客 / 实景三维 / 耕地保护

- **完成日期**：2026-07-27
- **状态**：已完成（抽取 + 收紧审批 + apply；叶子增量极少）
- **关联总览**：[../2026-07-13-知识图谱PRD剩余轮次总览.md](../../../01_当前有效/03_知识图谱与GraphRAG/2026-07-13-知识图谱PRD剩余轮次总览.md)
- **前置**：第 3 轮补 R1–R6c；经验产品化第 1–2 刀

---

## 结论

对剩余指定类目做了按批实 LLM 补抽闭环。语料以博客教程 / 实景三维开发说明为主，**运维叶子可放行量极低**；保守策略下仅入库 1 条 Command，未破坏主干。

**GO**：指定类目扩面流程可重复（extract → quality → 分拆审批 → apply）。  
**不 GO**：本批不代表「其他」可盲抽；不打开全库图召回。

---

## 执行摘要

| 项 | 值 |
|---|---|
| 范围 | `博客`(27) + `实景三维`(25) + `耕地保护`(5) = 57 chunks |
| batch | `07d2c577-c543-444c-8f02-035b751b932e` |
| extract | `llm_considered=57`；`category_scoped=56`；`llm_candidates=131`（pending LLM 72 + rejected diagnostic 59） |
| quality --llm | ok；`possible_duplicate=26`；非法边 0 |
| 审批 | approve **1** / reject **242**（规则候选全拒；ConfigItem/Module/Tool/Product/泛名 Procedure 全拒） |
| apply | entities **1374→1375**（+1 Command：`show create table`）；relations 1214；links 6583→6584 |
| 捞边 | 本批无齐套关系可捞，跳过 |

收紧原因：高置信 ConfigItem 多为 Spring 注解（`GetMapping` 等）与 SQL 类型（`TINYINT` 等）；Procedure 多为「线程池」「异步」「OrderService」等教程噪声。

---

## 产物

| 路径 | 说明 |
|------|------|
| `data/llm_backfill_r7_blog_3d_farmland.log` | extract 日志 |
| `data/llm_backfill_r7_quality_out.txt` | quality --llm |
| `data/llm_backfill_r7_review_plan.json` | 审批计划 |
| `data/llm_backfill_r7_apply.log` / `llm_backfill_r7_apply_summary.json` | apply |
| `data/backups/rag_relational_pre_r7_category_expand_*.db` | extract 前备份 |
| `data/backups/rag_relational_pre_r7_apply_*.db` | apply 前备份 |
| `scripts/r7_split_review_and_apply.py` | 本批收紧审批脚本 |

---

## 附带修复

- `run_graph_build.py quality`：去掉 `recover-relations` 分支内对 `GraphQualityService` 的局部 import，修复 UnboundLocalError。
