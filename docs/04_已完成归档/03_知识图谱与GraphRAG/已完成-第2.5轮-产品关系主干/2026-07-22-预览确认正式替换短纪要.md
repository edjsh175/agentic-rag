# 第 2.5 轮 — 预览确认正式替换短纪要

- **日期**：2026-07-22
- **决策**：1A 真正替换 + 2B 继续第 3 轮
- **正式 batch**：`a1c75bec-09ef-4420-be9a-e229add62c7b`
- **DB 备份**：`data/backups/rag_relational_pre_backbone_replace_20260722_125106.db`
- **正式 JSON 备份**：`data/archive/backups/product_relation_backbone.pre_replace_20260722_120840.json`
- **预览备份**：`data/archive/backups/product_relation_backbone_preview.pre_confirm_20260722_120840.json`
- **diff 报告**：`data/backbone_replace_diff_20260722_125106.json`

---

## Diff 摘要

| 项 | 数量 |
|---|---:|
| 旧 seed 关系 | 40 |
| 旧 seed 实体 | 36 |
| 正式 JSON 实体 / 关系 | 147 / 175 |
| 废弃边删除 | 33 |
| 保留边 | 7 |
| 待新增边 | 168 |
| 仅旧 seed 孤实体删除 | 22 |
| apply 后 seed 关系 | **175（missing=0 / obsolete=0）** |

Schema 对齐 remap（晋升时）：`keep=109`，`belongs_to→depends_on=36`，`belongs_to→requires=20`，`invert_belongs_to=10`。

类型冲突（apply 前）：`BIM模型` Procedure→Module、`UE材质集添加蓝图` Procedure→Tool（均为 `llm:schema_extractor`）；并删除非法边 `ModelBuilder -has_procedure-> BIM模型`。详见 `data/backbone_type_conflict_resolution.json`。

---

## 验收

| 检查 | 结果 |
|------|------|
| sync dry-run diagnostics | 0 |
| 主干边对账 | 175/175 |
| audit | completed（`data/graph_audit_post_backbone_replace.*`） |
| quality --graph --profile full | 仍有历史 `missing_evidence`（非本轮主干引入） |
| Task 8.1 Gate | **PASS** |

preview `status=confirmed`。正式真源：`data/product_relation_backbone.json`（`promoted_from=product_relation_backbone_preview.json`）。
