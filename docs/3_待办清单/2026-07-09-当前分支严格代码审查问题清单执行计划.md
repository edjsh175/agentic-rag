# 当前分支严格代码审查问题修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在保留当前知识图谱 MVP-2 未提交工作的前提下，依次收口破坏性操作、事实源边界、Graph-RAG 一致性、评测可信度和关键代码质量问题，使当前 `main` 工作区达到可审查、可回滚、可交付状态。

**Architecture:** 先把当前动态 WIP 修到可提交基线，再把向量知识库 `/rebuild` 改造成 POST-only、显式确认、临时 collection/临时 index 构建、校验后切换、失败自动回滚的两阶段流程。领域事实通过 `domain_catalog` 与 legacy Profile 迁移为待审核候选，运行时 Profile 只保留策略；评测严格区分 chunk-id 指标与内容诊断指标，并默认按生产 `approved` 口径阻断回归。

**Tech Stack:** Python 3.11、FastAPI、Pydantic、LangChain、ChromaDB 0.6.3、SQLite、pytest、Vue 3；所有 Python 命令统一使用项目根目录 `.\venv\Scripts\python.exe`。

## Global Constraints

- 仓库根目录：`E:\申浩霖实习文件夹\rag_cy\rag`。
- 基准提交：`76ca6f3`；工作区在审查期间仍持续出现新改动，执行时必须重新读取 `git status` 和 `git diff`。
- 当前工作树不是干净树；禁止 `git reset --hard`、禁止用 `git checkout --` 恢复项目文件、禁止 `git clean`，不得覆盖或丢弃现有 WIP。
- 严格遵守“少改、别臆断、手术式修改”；不要顺手重构相邻代码。
- 禁止裸 `python`、禁止 `.venv`、禁止使用其他 Chroma 版本打开 `chroma_db`。
- 默认 pytest 已排除 `integration`；单元测试必须使用 `isolated_storage`，不得接触正式 `chroma_db`、`data/rag_relational.db` 或正式评测集。
- LLM 抽取保持默认 disabled；LLM 输出必须有 `evidence_text`、`confidence`，只进入 candidate/review/apply 链路，不直写正式图谱。
- 不允许全库 LLM 抽取、不允许 `approve-all` 成为默认流程、不允许在本计划中执行正式库 apply 或正式库 rebuild。
- `Graph DB` 是 alias、different_from、belongs_to、has_table、has_field、defined_in 等领域事实的最终唯一事实源。
- `defined_in` 只表示 `Entity -> Document/Section`；chunk 证据只通过 `entity_chunk_links` 表达。
- 每项任务先写失败测试，再做最小实现；每个任务独立提交，提交前执行该任务列出的验证命令。

---

## 执行前必须理解的两个“重建”

| 名称 | 当前入口 | 数据对象 | 本计划处理方式 |
|---|---|---|---|
| 向量知识库重建 | FastAPI `/rebuild` | Chroma collection + `file_index.json` | P0：改成两阶段构建和可回滚切换 |
| 图谱安全重建 | `run_graph_build.py rebuild-safe --dry-run` | SQLite 图谱候选/事实 | 属于 MVP-2 WIP；本计划只要求稳定现有 WIP，不得用它替代 `/rebuild` 修复 |

二者不能合并理解。完成图谱 dry-run 不代表 `/rebuild` 已安全。

## 审查问题与任务映射

| 审查项 | 当前复核结论 | 对应任务 |
|---|---|---|
| P0-1 untracked/垃圾文件 | 原清单中的 `.agents/`、`AGENTS.md`、Profile PRD 已被后续提交正式跟踪，`NUL` 已消失；当前真正问题是动态 MVP-2 WIP 与被忽略的 `data/domain_catalog.json` | Task 1、Task 12 |
| P0-2 GET `/rebuild` | 仍存在 | Task 2 |
| P0-3 非原子 rebuild | 仍存在，只备份 index | Task 3、Task 4 |
| P0-4 默认覆盖评测集 | 仍存在 | Task 5 |
| P1-1 Profile 多事实源 | Task 8 + Task 8.1 已收口：policy-only 运行时 + Graph 评分；正式库 profile_sync 已 apply（2026-07-10） | Task 8、Task 8.1 |
| P1-2 schema 混领域词典 | 当前 WIP 正在迁移；`domain_catalog.json` 却被 `.gitignore` 忽略 | Task 1、Task 7 |
| P1-3 SpecialRuleRestorer | 已在 Task 7 删除；改由 catalog seed + review/apply 写入 | Task 7 |
| P2-1/P2-2 边界与迁移 | policy-only 运行时 + Graph 事实已 apply；legacy 评分已切断（Task 8.1，2026-07-10） | Task 8、Task 8.1 |
| P2-3 `defined_in` 语义 | Task 9 已收紧：`UNRESTRICTED_RELATIONS` 移除 `defined_in`，目标类型限定 Document/Section | Task 9 |
| P3-1 stale ids 软放过 | 仍为 PASS + warning | Task 6 |
| P3-2 content fallback 改写分母 | 仍存在 | Task 6 |
| P3-3 regression opt-in | 仍为 `--fail-on-regression` | Task 6 |
| P3-4 评测与生产审核口径不同 | `EvaluationRunner` 仍传 `review_status=None` | Task 6 |
| P4-1 私有 `_file_lookup()` 依赖 | Task 10 已抽出 `ChunkIndexLookupService`；`GraphRetriever` 不再依赖 `ChunkAdminService._file_lookup()` | Task 10 |
| P4-2 scanner 未初始化文案错误 | 仍存在 | Task 2 |
| P4-3 防线测试缺口 | 仍存在 | 各任务测试 + Task 12 |
| P4-4 Graph-RAG 重复逻辑 | Task 11 已统一 kwargs 构建与 `GraphRetriever.fuse()` 入口 | Task 11 |

---

### Task 1: 接管并稳定当前动态 MVP-2 WIP

**Files:**
- Modify: `.gitignore`
- Modify: `run_graph_build.py`
- Modify: `rag_knowledge/services/domain_catalog.py`
- Modify: `rag_knowledge/services/graph_extraction/__init__.py`
- Modify: `rag_knowledge/services/graph_extraction/pipeline.py`
- Modify: `rag_knowledge/services/profile_graph_sync.py`
- Track: `data/domain_catalog.json`
- Preserve and review: `rag_knowledge/services/entity_resolution.py`
- Test: `tests/test_domain_catalog.py`
- Test: `tests/test_candidate_normalizer.py`
- Test: `tests/test_entity_resolution.py`
- Test: `tests/test_graph_extraction.py`
- Test: `tests/test_graph_quality_cli.py`
- Test: `tests/test_profile_graph_sync.py`

**Interfaces:**
- Consumes: 当前工作树中的 MVP-2 WIP，不假设文件列表保持不变。
- Produces: 可跟踪的 catalog、稳定的 loader/normalizer/resolver、全绿的定向测试基线。

- [ ] **Step 1: 记录接管快照，禁止先改代码**

Run:

```powershell
git status --short --branch
git diff --name-status
git diff --check
git check-ignore -v data/domain_catalog.json
```

Expected at the 2026-07-10 review snapshot:

```text
HEAD = 76ca6f3
graph_schema.py / graph_extraction / profile_graph_sync.py / run_graph_build.py 有修改
domain_catalog.py / entity_resolution.py 及对应测试为 untracked
data/domain_catalog.json 被 .gitignore 的 /data/* 命中
```

如果实际列表更多，追加到接管记录；不得删除、还原或覆盖这些文件。

- [ ] **Step 2: 保留并锁死当前已复现的 Review CLI 失败测试**

现有失败：

```text
tests/test_graph_extraction.py::test_graph_build_cli_review_reports_invalid_candidate_ids
TypeError: string indices must be integers, not 'str'
```

在 `tests/test_graph_quality_cli.py` 增加断言，区分请求数、安全过滤数和实际更新数：

```python
def test_review_reports_requested_selected_and_safety_rejected(isolated_storage, capsys):
    db = make_db(
        isolated_storage,
        name="review-safety.db",
        data_dir_name="review-safety-data",
        chroma_name="review-safety-chroma",
    )
    batch_id = db.create_extraction_batch("incremental", {}, "snapshot")
    safe_id = db.add_extraction_candidate(
        batch_id,
        "entity",
        "safe",
        {
            "name": "DemoTool",
            "entity_type": "Tool",
            "evidence_text": "DemoTool",
            "source_chunk_id": "c1",
        },
        "c1",
        "DemoTool",
    )
    unsafe_id = db.add_extraction_candidate(
        batch_id,
        "alias",
        "unsafe",
        {"entity_name": "DemoTool", "alias": "演示工具"},
    )

    assert run_graph_build.main(
        ["review", "--batch", batch_id, "--approve", safe_id, unsafe_id, "missing-id"],
        db=db,
    ) == 0
    payload = json.loads(capsys.readouterr().out)
    assert payload == {
        "requested": 3,
        "selected": 2,
        "rejected_by_safety": 1,
        "updated": 1,
        "missing_or_not_pending": 1,
        "status": "approved",
        "remaining_pending": 1,
    }
```

- [ ] **Step 3: 运行单测并确认它按预期失败**

Run:

```powershell
.\venv\Scripts\python.exe -m pytest -q tests/test_graph_quality_cli.py::test_review_reports_requested_selected_and_safety_rejected
```

Expected: FAIL；当前 `ids` 是字符串列表，却被当成候选 dict 读取。

- [ ] **Step 4: 最小修复 Review CLI 的 ID 过滤与统计**

在 `run_graph_build.py` 的 review 分支使用字符串集合：

```python
requested_ids = list(ids)
unsafe = [
    item
    for item in pending
    if item["id"] in requested_ids and not _safe_review_candidate(item)
]
unsafe_ids = {item["id"] for item in unsafe}
ids = [candidate_id for candidate_id in requested_ids if candidate_id not in unsafe_ids]
updated = db.review_extraction_candidates(args.batch, ids, status, args.reason)
remaining = db.list_extraction_candidates(args.batch, "pending")
```

输出改为：

```python
_print({
    "requested": len(requested_ids),
    "selected": len(ids),
    "rejected_by_safety": len(unsafe_ids),
    "updated": updated,
    "missing_or_not_pending": len(ids) - updated,
    "status": status,
    "remaining_pending": len(remaining),
})
```

- [ ] **Step 5: 让 domain catalog 成为交付文件**

在 `.gitignore` 的 `/data/*` 例外中加入：

```gitignore
!/data/domain_catalog.json
```

Run:

```powershell
git check-ignore data/domain_catalog.json
git ls-files --error-unmatch data/domain_catalog.json
```

Expected: 第一条无输出；第二条在显式 `git add data/domain_catalog.json` 后成功。

- [ ] **Step 6: 稳定 catalog 的解析接口并避免每个 chunk 重读文件**

`DomainCatalogLoader` 必须保留这些公开方法签名：`resolve(self, name: str) -> tuple[str, str] | None`、`product_for_category(self, category: str) -> str | None`、`owner_for(self, name: str) -> str | None`、`entries(self, entity_type: str | None = None) -> list[tuple[str, str]]`。

加载时验证 `belongs_to` 为非空字符串或缺省，并保存 canonical owner：

```python
owner = item.get("belongs_to")
if owner is not None and (not isinstance(owner, str) or not owner.strip()):
    raise ValueError(f"invalid belongs_to at {self.path}: {name}")
if isinstance(owner, str):
    self._owners[name] = owner.strip()
```

在 `GraphBuilder._build()` 进入 chunk 循环前只创建一次：

```python
catalog = DomainCatalogLoader()
section_extractor = SectionPathExtractor(catalog=catalog)
candidate_normalizer = CandidateNormalizer(catalog=catalog)
entity_resolver = EntityResolutionService(self.db)
```

循环内改为 `section_extractor.extract(chunk)`，不得继续每个 chunk 调用 `SectionPathExtractor()`。

在 `profile_graph_sync.py` 给复合判断加括号：

```python
if (resolved and resolved[1] == "Tool") or normalized.endswith("Builder"):
    return "Tool"
if (resolved and resolved[1] == "Service") or normalized.endswith("服务"):
    return "Service"
```

- [ ] **Step 7: 跑当前 WIP 的定向回归**

Run:

```powershell
.\venv\Scripts\python.exe -m pytest -q tests/test_domain_catalog.py tests/test_candidate_normalizer.py tests/test_entity_resolution.py tests/test_graph_extraction.py tests/test_graph_quality_cli.py tests/test_profile_graph_sync.py
```

Expected: 全部 PASS；不得再出现 2026-07-10 已复现的 `1 failed, 34 passed`。

- [ ] **Step 8: 显式暂存并提交，不使用 `git add .`**

```powershell
git add .gitignore data/domain_catalog.json rag_knowledge/models/graph_schema.py rag_knowledge/services/domain_catalog.py rag_knowledge/services/entity_resolution.py rag_knowledge/services/graph_extraction/__init__.py rag_knowledge/services/graph_extraction/pipeline.py rag_knowledge/services/profile_graph_sync.py run_graph_build.py tests/test_candidate_normalizer.py tests/test_domain_catalog.py tests/test_entity_resolution.py tests/test_graph_extraction.py tests/test_graph_quality_cli.py tests/test_profile_graph_sync.py
git commit -m "fix: stabilize graph governance MVP-2 work"
```

---

### Task 2: 封住 `/rebuild` 破坏性入口

**Files:**
- Modify: `rag_knowledge/models/api.py`
- Modify: `rag_knowledge/api/routes.py`
- Test: `tests/test_routes_consistency.py`

**Interfaces:**
- Produces: `POST /rebuild`，body 为 `RebuildRequest(confirmation="REBUILD_KNOWLEDGE_BASE")`。
- Error contract: GET=405；错误确认=422；scanner 未初始化=503；并发 rebuild=409。

- [ ] **Step 1: 写 API 防线测试**

```python
from fastapi import FastAPI
from fastapi.testclient import TestClient

from rag_knowledge.api import routes


def make_client() -> TestClient:
    app = FastAPI()
    app.include_router(routes.router)
    return TestClient(app)


def test_rebuild_rejects_get():
    assert make_client().get("/rebuild").status_code == 405


def test_rebuild_requires_exact_confirmation():
    response = make_client().post("/rebuild", json={"confirmation": "yes"})
    assert response.status_code == 422


def test_rebuild_returns_503_without_scanner(monkeypatch):
    monkeypatch.setattr(routes, "_scanner", None)
    response = make_client().post(
        "/rebuild",
        json={"confirmation": "REBUILD_KNOWLEDGE_BASE"},
    )
    assert response.status_code == 503
    assert response.json()["detail"] == "扫描器未初始化，未执行重建"
```

- [ ] **Step 2: 运行测试并确认失败**

```powershell
.\venv\Scripts\python.exe -m pytest -q tests/test_routes_consistency.py
```

Expected: GET 当前仍可进入；函数没有 body 确认；scanner 缺失返回错误成功文案。

- [ ] **Step 3: 新增严格请求模型**

在 `rag_knowledge/models/api.py`：

```python
from typing import Literal


class RebuildRequest(BaseModel):
    confirmation: Literal["REBUILD_KNOWLEDGE_BASE"]
```

- [ ] **Step 4: 修改路由与错误映射**

在 `routes.py`：

```python
@router.post("/rebuild")
def rebuild_knowledge(request: RebuildRequest):
    if _scanner is None:
        raise HTTPException(status_code=503, detail="扫描器未初始化，未执行重建")
    try:
        return RebuildCoordinator(
            cfg=_cfg,
            store=VectorStore(),
            scanner=_scanner,
            consistency_service=KnowledgeBaseConsistencyService(),
            invalidate_retrieval_caches=_invalidate_retrieval_caches,
            rebuild_bm25=_rebuild_bm25,
        ).run()
    except RebuildAlreadyRunningError as exc:
        raise HTTPException(status_code=409, detail=str(exc)) from exc
    except Exception as exc:
        logger.exception("重建失败")
        raise HTTPException(status_code=500, detail=f"重建失败: {exc}") from exc
```

函数中不需要再次判断 confirmation；Pydantic `Literal` 已完成严格校验。

- [ ] **Step 5: 验证并提交**

```powershell
.\venv\Scripts\python.exe -m pytest -q tests/test_routes_consistency.py tests/test_rebuild_coordinator.py
git add rag_knowledge/models/api.py rag_knowledge/api/routes.py tests/test_routes_consistency.py
git commit -m "fix: guard destructive knowledge base rebuild route"
```

---

### Task 3: 为临时 collection 和临时 index 建立可测试接口

**Files:**
- Modify: `rag_knowledge/repository/vector_store.py`
- Modify: `rag_knowledge/services/scanner.py`
- Test: `tests/test_rebuild_coordinator.py`
- Test: `tests/test_scanner_cleanup.py`

**Interfaces:**
- `VectorStore.fork(collection_name: str) -> VectorStore`
- `VectorStore.rename_collection(new_name: str) -> None`
- `VectorStore.disconnect() -> None`
- `DirectoryScanner` 构造函数新增关键字参数 `store=None`、`index_path=None`、`refresh_retrieval=True`，并保留现有参数与默认值
- `DirectoryScanner.reload_index() -> None`

- [ ] **Step 1: 写隔离 handle 与 staging scanner 测试**

测试必须断言：fork 后 persist directory/embedding 相同、collection name 不同；staging scan 不调用 BM25/cache；staging index 写到临时路径而不是正式 index。

```python
def test_vector_store_fork_uses_independent_collection_handle(isolated_storage):
    isolated_storage()
    live = VectorStore()
    staged = live.fork("rag_knowledge__staging__op1")
    assert staged is not live
    assert staged._persist_dir == live._persist_dir
    assert staged._collection_name == "rag_knowledge__staging__op1"
    assert live._collection_name != staged._collection_name
```

- [ ] **Step 2: 运行测试并确认缺少接口**

```powershell
.\venv\Scripts\python.exe -m pytest -q tests/test_rebuild_coordinator.py tests/test_scanner_cleanup.py
```

Expected: FAIL，提示 `fork`、scanner 注入参数或 `reload_index` 不存在。

- [ ] **Step 3: 增加非单例 collection handle**

在 `VectorStore` 中增加：

```python
def fork(self, collection_name: str) -> "VectorStore":
    scoped = object.__new__(type(self))
    scoped._initialized = True
    scoped._embedding_cache = self._embedding_cache
    scoped._embeddings = self._embeddings
    scoped._persist_dir = self._persist_dir
    scoped._collection_name = collection_name
    scoped._store = None
    return scoped

def rename_collection(self, new_name: str) -> None:
    store = self._get_store()
    store._collection.modify(name=new_name)
    self._collection_name = new_name

def disconnect(self) -> None:
    self._store = None
```

`fork()` 仅用于同一进程内的临时 collection；不得赋值给 `VectorStore._instance`。

- [ ] **Step 4: 让 scanner 支持显式依赖注入**

```python
def __init__(
    self,
    *,
    cfg=None,
    store=None,
    loader=None,
    index_path: Path | None = None,
    refresh_retrieval: bool = True,
):
    self._cfg = cfg or Config()
    self._store = store or VectorStore()
    self._loader = loader or FileLoader()
    self._scheduler = None
    self._index_path = index_path or (self._cfg.data_dir / "file_index.json")
    self._refresh_retrieval = refresh_retrieval
    self._index = self._load_index()
```

`scan()` 中只有在 `self._refresh_retrieval` 为真时才 rebuild BM25/clear cache：

```python
if self._refresh_retrieval and (
    new or any(item.should_rebuild_bm25 for item in cleaned)
):
    BM25Store().rebuild()
    clear_query_cache()
```

增加：

```python
def reload_index(self) -> None:
    self._index = self._load_index()
```

- [ ] **Step 5: 验证并提交**

```powershell
.\venv\Scripts\python.exe -m pytest -q tests/test_rebuild_coordinator.py tests/test_scanner_cleanup.py tests/test_scanner_doc_category.py
git add rag_knowledge/repository/vector_store.py rag_knowledge/services/scanner.py tests/test_rebuild_coordinator.py tests/test_scanner_cleanup.py tests/test_scanner_doc_category.py
git commit -m "refactor: add isolated rebuild storage handles"
```

---

### Task 4: 把 `/rebuild` 改成两阶段构建、校验、切换和回滚

**Files:**
- Modify: `rag_knowledge/services/rebuild_coordinator.py`
- Modify: `rag_knowledge/api/routes.py`
- Test: `tests/test_rebuild_coordinator.py`
- Test: `tests/test_routes_consistency.py`

**Interfaces:**
- Coordinator 新增 `staging_scanner_factory` 注入。
- 成功响应新增 `operation_id`、`backup_collection`、`index_backup_path`、`graph_backup_path`。
- live collection/index 在 staging 校验通过前不得变化。

- [ ] **Step 1: 写四个失败场景测试**

覆盖：

```text
1. staging scan 返回 errors > 0：live collection 和 live index 完全不变。
2. staging 一致性失败：删除 staging collection，live 完全不变。
3. collection 已换名但 index os.replace 失败：collection 名和 live index 自动回滚。
4. 成功：staging 改名为 live，旧 live 保留为 backup，scanner reload，BM25/cache 只在切换后刷新。
```

每个 fake store 都记录 `rename_collection()`、`disconnect()` 调用；测试不得实例化正式 Chroma。

- [ ] **Step 2: 运行并确认当前 destructive-first 流程失败**

```powershell
.\venv\Scripts\python.exe -m pytest -q tests/test_rebuild_coordinator.py
```

Expected: 当前第一步就是 `store.clear()`，无法满足四个断言。

- [ ] **Step 3: 创建 staging 目录与工厂**

Coordinator 中使用：

```python
operation_id = datetime.now().strftime("%Y%m%d-%H%M%S-%f")
live_name = self._cfg.collection_name
staging_name = f"{live_name}__staging__{operation_id}"
backup_name = f"{live_name}__backup__{operation_id}"
work_dir = Path(self._cfg.data_dir) / "rebuild" / operation_id
staging_index = work_dir / "file_index.json"
index_backup = work_dir / "file_index.before.json"
graph_backup = work_dir / "rag_relational.before.db"
```

先复制现有 index 到 staging 路径，再由 staging scanner `reset_index()`，这样可继承 doc_category；正式 index 此时不动。

- [ ] **Step 4: 使用 SQLite backup API 备份图谱库**

```python
def _backup_sqlite(source: Path, destination: Path) -> str:
    if not source.exists():
        return ""
    destination.parent.mkdir(parents=True, exist_ok=True)
    with sqlite3.connect(source) as src, sqlite3.connect(destination) as dst:
        src.backup(dst)
    return str(destination)
```

不得在可能有活跃连接时用 `shutil.copy2()` 代替 SQLite backup API。

- [ ] **Step 5: staging 构建和预提交校验**

核心顺序必须保持：

```python
staged_store = self._store.fork(staging_name)
staged_scanner = self._staging_scanner_factory(staged_store, staging_index)
staged_scanner.reset_index()
result = staged_scanner.scan()
if result["errors"]:
    raise RuntimeError(f"staging rebuild has {result['errors']} file errors")

index_data = json.loads(staging_index.read_text(encoding="utf-8"))
chunk_snapshot = staged_store.get_chunk_stats_source()
KnowledgeBaseConsistencyService(
    index_data=index_data,
    chunk_snapshot=chunk_snapshot,
).assert_consistent()
```

这里不得调用 live `clear()`、live `reset_index()`、BM25 rebuild 或 cache invalidation。

- [ ] **Step 6: 实现最小可回滚切换**

```python
def _commit_swap(
    *,
    live_store,
    staged_store,
    live_name: str,
    staging_name: str,
    backup_name: str,
    live_index: Path,
    staging_index: Path,
    index_backup: Path,
) -> None:
    shutil.copy2(live_index, index_backup)
    live_store.rename_collection(backup_name)
    try:
        staged_store.rename_collection(live_name)
        os.replace(staging_index, live_index)
    except Exception:
        staged_store.rename_collection(staging_name)
        live_store.rename_collection(live_name)
        shutil.copy2(index_backup, live_index)
        raise
```

切换成功后：

```python
self._store.disconnect()
self._scanner.reload_index()
self._consistency_service.assert_consistent()
self._rebuild_bm25()
self._invalidate_retrieval_caches("rebuild_commit")
```

旧 collection 保留为 backup；本任务不增加自动删除策略。

- [ ] **Step 7: 状态文件必须反映阶段**

允许状态：

```text
staging
validating
committing
succeeded
failed_before_commit
rolled_back
rollback_failed
```

失败 state 至少包含 `operation_id`、`stage`、`error`、staging/backup collection 名、index/graph backup 路径。

- [ ] **Step 8: 验证并提交**

```powershell
.\venv\Scripts\python.exe -m pytest -q tests/test_rebuild_coordinator.py tests/test_routes_consistency.py tests/test_knowledge_base_consistency.py
git add rag_knowledge/services/rebuild_coordinator.py rag_knowledge/api/routes.py tests/test_rebuild_coordinator.py tests/test_routes_consistency.py
git commit -m "fix: make knowledge base rebuild staged and recoverable"
```

不要在正式数据上调用 `/rebuild` 作为本任务验收。

---

### Task 5: 保护正式评测集，禁止默认覆盖

**Files:**
- Modify: `run_eval_full.py`
- Create: `tests/test_run_eval_full.py`

**Interfaces:**
- `build_parser() -> argparse.ArgumentParser`
- `main(argv: list[str] | None = None) -> int`
- 默认 dataset：`data/eval_runs/eval_dataset_<timestamp>.json`
- 已存在输出只有显式 `--overwrite` 才允许写。

- [ ] **Step 1: 写路径与覆盖保护测试**

```python
def test_existing_dataset_requires_overwrite(tmp_path):
    output = tmp_path / "fixed.json"
    output.write_text("[]", encoding="utf-8")
    with pytest.raises(SystemExit, match="already exists"):
        run_eval_full.main(["--output", str(output)])


def test_default_output_is_timestamped(monkeypatch, tmp_path):
    monkeypatch.setattr(run_eval_full, "DEFAULT_OUTPUT_DIR", tmp_path)
    path = run_eval_full.resolve_output_path(None, now="20260710-120000")
    assert path == tmp_path / "eval_dataset_20260710-120000.json"
```

- [ ] **Step 2: 运行并确认当前 import 即执行且覆盖固定路径**

```powershell
.\venv\Scripts\python.exe -m pytest -q tests/test_run_eval_full.py
```

Expected: FAIL；当前没有 `main()`/parser，import 会直接执行。

- [ ] **Step 3: 重构为显式 CLI**

参数固定为：

```python
parser.add_argument("--output")
parser.add_argument("--overwrite", action="store_true")
parser.add_argument("--questions-per-chunk", type=int, default=2)
parser.add_argument("--max-chunks", type=int, default=100)
parser.add_argument("--methods", nargs="+", default=["mmr", "similarity", "bm25", "hybrid", "hybrid+rerank"])
```

路径保护：

```python
output = resolve_output_path(args.output)
if output.exists() and not args.overwrite:
    raise SystemExit(f"dataset already exists: {output}; pass --overwrite to replace it")
output.parent.mkdir(parents=True, exist_ok=True)
```

构建后写 sidecar `<dataset>.manifest.json`：

```python
manifest = {
    "created_at": datetime.now().astimezone().isoformat(),
    "dataset": output.as_posix(),
    "embedding_model": Config().embedding_model,
    "collection_name": Config().collection_name,
    "chunk_count": len(chunk_ids),
    "chunk_id_sha256": hashlib.sha256(
        "\n".join(sorted(chunk_ids)).encode("utf-8")
    ).hexdigest(),
}
```

- [ ] **Step 4: 验证并提交**

```powershell
.\venv\Scripts\python.exe -m pytest -q tests/test_run_eval_full.py tests/test_loader_and_dataset.py
git add run_eval_full.py tests/test_run_eval_full.py
git commit -m "fix: protect fixed retrieval evaluation datasets"
```

---

### Task 6: 修正评测健康、分母、审核口径和默认回归门禁

**Files:**
- Modify: `rag_knowledge/evaluation/dataset_health.py`
- Modify: `rag_knowledge/evaluation/runner.py`
- Modify: `run_retrieval_ab.py`
- Test: `tests/test_eval_dataset_health.py`
- Test: `tests/test_metrics.py`
- Test: `tests/test_run_retrieval_ab.py`

**Interfaces:**
- `check_eval_dataset_health` 新增关键字参数 `allow_stale_ids: bool = False`，保留现有参数与默认值。
- `EvaluationRunner` 构造函数新增 `review_status: str | None = "approved"` 和 `allow_stale_ids: bool = False`，保留现有参数与默认值。
- 标准 Recall/MRR 永远使用数据集原始 relevant IDs；内容匹配只输出独立诊断指标。
- A/B 默认检测到回归即非零退出；只有 `--allow-regression` 放行。

- [ ] **Step 1: 把 stale-id 测试改成严格默认**

```python
def test_health_blocks_stale_ids_even_when_expected_targets_match(tmp_path):
    report = check_eval_dataset_health(dataset_path, collection=collection)
    assert report.status == "BLOCK"
    assert report.needs_chunk_id_refresh is True
    assert report.target_health == 1.0


def test_health_can_report_stale_ids_in_explicit_diagnostic_mode(tmp_path):
    report = check_eval_dataset_health(
        dataset_path,
        collection=collection,
        allow_stale_ids=True,
    )
    assert report.status == "PASS"
    assert report.needs_chunk_id_refresh is True
```

- [ ] **Step 2: 写“不改变 Recall 分母”回归测试**

用 fake RagChain 返回一个内容匹配但 ID 不在 gold set 的 chunk；断言：

```python
assert metrics["recall@3"] == 0.0
assert metrics["mrr"] == 0.0
assert metrics["content_fallback_hit_rate"] == 1.0
assert metrics["review_scope"] == "approved"
```

- [ ] **Step 3: 运行并确认两个旧行为失败**

```powershell
.\venv\Scripts\python.exe -m pytest -q tests/test_eval_dataset_health.py tests/test_metrics.py
```

Expected: stale expected-target 当前是 PASS；content fallback 当前会把 `relevant` 替换成检索结果并抬高 Recall/MRR。

- [ ] **Step 4: 严格健康状态**

```python
needs_refresh = bool(missing_ids and target_stats["matched_expected_targets"])
status = "PASS" if invalid_questions == 0 else "BLOCK"
if needs_refresh and not allow_stale_ids:
    status = "BLOCK"
```

`EvaluationRunner._check_health()` 必须把实例的 `allow_stale_ids` 传入；默认 false。

- [ ] **Step 5: 删除分母改写，保留独立诊断**

删除：

```python
if matched_ids:
    relevant = matched_ids
```

改为只记录：

```python
content_fallback_hits.append(bool(matched_ids))
```

循环后增加：

```python
metrics["content_fallback_hit_rate"] = round(
    sum(content_fallback_hits) / len(content_fallback_hits), 4
) if content_fallback_hits else 0.0
metrics["review_scope"] = "all" if self._review_status is None else self._review_status
metrics["dataset_health"] = self._health_report.to_dict()
```

生产默认：

```python
review_status=self._review_status  # 默认 "approved"
```

只有显式诊断运行才传 `review_status=None`。

- [ ] **Step 6: 把 regression fail 改成默认行为**

CLI 改为：

```python
parser.add_argument("--allow-regression", action="store_true")
parser.add_argument("--allow-stale-ids", action="store_true")
parser.add_argument("--review-status", choices=("approved", "all"), default="approved")
```

门禁：

```python
if regressions and not args.allow_regression:
    raise SystemExit(
        build_regression_message(dataset_key, method, regressions, previous_result, result)
    )
```

报告必须保留 `review_scope`、`dataset_health`、`regression_threshold`；不能只写 Recall/MRR。

- [ ] **Step 7: 验证并提交**

```powershell
.\venv\Scripts\python.exe -m pytest -q tests/test_eval_dataset_health.py tests/test_metrics.py tests/test_content_match.py tests/test_run_retrieval_ab.py
git add rag_knowledge/evaluation/dataset_health.py rag_knowledge/evaluation/runner.py run_retrieval_ab.py tests/test_eval_dataset_health.py tests/test_metrics.py tests/test_run_retrieval_ab.py
git commit -m "fix: make retrieval evaluation strict and production-aligned"
```

---

### Task 7: 用可审核 domain seed 替代 GraphSpecialRuleRestorer

**Files:**
- Modify: `data/domain_catalog.json`
- Modify: `rag_knowledge/services/domain_catalog.py`
- Create: `rag_knowledge/services/domain_catalog_graph_sync.py`
- Modify: `rag_knowledge/services/graph_extraction/pipeline.py`
- Modify: `rag_knowledge/services/graph_extraction/__init__.py`
- Create: `tests/test_domain_catalog_graph_sync.py`
- Modify: `tests/test_graph_extraction.py`

**Interfaces:**
- `DomainCatalogGraphSyncService.preview() -> dict`
- `DomainCatalogGraphSyncService.build_batch(review_status="pending") -> BuildBatchResult`
- Catalog 事实只能进入 extraction candidates，默认 pending。

- [x] **Step 1: 写“apply 不得偷写特殊规则”测试**

```python
def test_applying_unrelated_batch_does_not_restore_special_rules(db):
    batch_id = make_approved_unrelated_batch(db)
    GraphCandidateApplier(db).apply(batch_id)
    assert db.get_entity_by_alias("管线发布工具") is None
    assert not relation_exists(db, "PipelineBuilder", "different_from", "管线发布服务")
```

当前应失败，因为 `GraphCandidateApplier.apply()` 无条件调用 `GraphSpecialRuleRestorer`。

- [x] **Step 2: 把 seed 事实显式放进 catalog**

`PipelineBuilder` 条目明确为：

```json
{
  "name": "PipelineBuilder",
  "aliases": ["管线发布工具"],
  "belongs_to": "StampTools",
  "different_from": ["管线发布服务", "PipelinePublishConfig"]
}
```

`DomainCatalogLoader` 验证 `different_from` 是字符串数组，并暴露结构化 seed，而不只返回 `(name, type)`。

- [x] **Step 3: 实现 catalog→candidate staging**

每条 candidate 统一 provenance：

```python
created_by = "seed:domain_catalog"
evidence_text = f"domain_catalog:{category}:{canonical_name}"
review_status = "pending"
```

生成：

```text
产品/工具/服务/组件 → entity candidate
aliases → alias candidate
belongs_to → relation candidate
different_from → relation candidate
```

不得调用 `create_entity()`、`create_alias()` 或 `create_relation()` 直接写正式图谱。

- [x] **Step 4: 删除隐式 special restorer**

删除 `GraphCandidateApplier.apply()` 中：

```python
GraphSpecialRuleRestorer(self.db).apply(conn)
```

删除 class 与 `__all__` 导出；任何历史恢复只能由已审核 seed/profile candidate 完成。

- [x] **Step 5: 验证并提交**

```powershell
.\venv\Scripts\python.exe -m pytest -q tests/test_domain_catalog.py tests/test_domain_catalog_graph_sync.py tests/test_graph_extraction.py tests/test_graph_manual_export.py
git add data/domain_catalog.json rag_knowledge/services/domain_catalog.py rag_knowledge/services/domain_catalog_graph_sync.py rag_knowledge/services/graph_extraction/pipeline.py rag_knowledge/services/graph_extraction/__init__.py tests/test_domain_catalog_graph_sync.py tests/test_graph_extraction.py
git commit -m "refactor: stage domain seeds through graph review"
```

---

### Task 8: 完成 Profile→Graph 迁移并让运行时 Profile 只保留策略

**Files:**
- Rename: `data/retrieval_intent_profiles.json` → `data/migrations/retrieval_intent_profiles_v1.json`
- Create: `data/retrieval_intent_policies.json`
- Modify: `.gitignore`
- Modify: `rag_knowledge/services/retrieval_intent.py`
- Modify: `rag_knowledge/services/profile_graph_sync.py`
- Modify: `rag_knowledge/services/rag.py`
- Modify: `sync_profiles_to_graph.py`
- Test: `tests/test_profile_graph_sync.py`
- Test: `tests/test_retrieval_intent.py`
- Test: `tests/test_graph_retrieval.py`

**Interfaces:**
- Legacy migration input 与 runtime policy 文件分离。
- Runtime policy 只含 `id`、`entity_ref`、`intent_terms`、`query_hints`、`preferred_doc_categories`、`fallback_doc_categories`、`candidate_min_k`。
- Profile migration 默认 dry-run/pending，不自动覆盖 legacy 文件或正式图谱。

- [x] **Step 1: 写事实源边界测试**

断言生产 loader 拒绝这些字段：

```text
entity_aliases
section_families
sibling_penalty_groups
recall_terms
preferred_sources
fallback_sources
```

断言 Graph alias 链接到 canonical entity 后，policy 通过 `entity_ref` 被选中；Graph 关闭时只退回通用检索，不从 legacy Profile 读取事实。

- [x] **Step 2: 分类 legacy 字段，禁止错误映射**

固定规则：

```text
entity_aliases → alias candidates
section_families → Section + defined_in / has_table / belongs_to candidates
sibling_penalty_groups → different_from candidates
recall_terms → 明确字段名才生成 Field/has_field；泛词只产生 diagnostic
preferred_sources/fallback_sources → 策略字段 preferred_doc_categories/fallback_doc_categories，不再生成 belongs_to
```

删除或停用当前 `_extract_strategy_relations()` 把 source 偏好转成弱 `belongs_to` 的逻辑。

- [x] **Step 3: 建立 policy-only 数据模型**

```python
@dataclass(frozen=True)
class RetrievalIntentPolicy:
    id: str
    entity_ref: str = ""
    intent_terms: tuple[str, ...] = ()
    query_hints: tuple[str, ...] = ()
    preferred_doc_categories: tuple[str, ...] = ()
    fallback_doc_categories: tuple[str, ...] = ()
    candidate_min_k: int | None = None
```

`entity_ref` 是对 Graph canonical name 的引用，不复制 alias/关系事实。

- [x] **Step 4: 调整检索时序**

`_prepare_graph_plan()` 在获得 `context.linked_entities` 后，用 canonical name 细化 policy：

```python
enriched = self._retrieval_intent_planner.refine_from_graph(
    enriched,
    canonical_names=tuple(
        item.canonical_name for item in context.linked_entities
    ),
)
```

Graph queries、aliases 和 different_from 只来自 Graph DB；policy 只改变候选池、doc_category 偏好和是否 rerank。

- [x] **Step 5: legacy 迁移命令只生成候选和建议 policy**

命令输出：

```text
候选 batch_id
各 kind 数量
跳过的泛词及原因
data/retrieval_intent_policies.generated.json
```

没有显式 `--write-policy-output` 时，不改任何 JSON；没有 review/apply 时，不改正式 Graph。

- [x] **Step 6: 验证事实不再双读**

```powershell
.\venv\Scripts\python.exe -m pytest -q tests/test_profile_graph_sync.py tests/test_retrieval_intent.py tests/test_graph_retrieval.py tests/test_routing_and_structured_boost.py
```

Expected: runtime loader 不再读取 legacy fact fields；Graph alias/relations 仍能驱动等价检索行为。

- [x] **Step 7: 提交**

```powershell
git add .gitignore data/migrations/retrieval_intent_profiles_v1.json data/retrieval_intent_policies.json rag_knowledge/services/retrieval_intent.py rag_knowledge/services/profile_graph_sync.py rag_knowledge/services/rag.py sync_profiles_to_graph.py tests/test_profile_graph_sync.py tests/test_retrieval_intent.py tests/test_graph_retrieval.py
git commit -m "refactor: make graph the runtime domain fact source"
```

#### Task 8.1：切断 legacy 评分双读，Graph 成为运行时唯一事实源

> **记录时间**：2026-07-10  
> **关联提交**：`9c53aad`（Task 8 分阶段安全方案）  
> **状态**：**已完成**（代码 + 正式库 apply，2026-07-10）  
> **验收报告**：`docs/3_待办清单/task81-production-validation/2026-07-10-正式库验收报告.md`

Task 8 已完成的边界：

- 运行时 loader 只读 `data/retrieval_intent_policies.json`，拒绝 `entity_aliases` / `section_families` 等事实字段。
- Legacy 事实文件迁至 `data/migrations/retrieval_intent_profiles_v1.json`，仅供迁移 CLI（`sync_profiles_to_graph.py`）与过渡期评分。
- `ProfileGraphSyncService` 不再把 `preferred_sources`/`fallback_sources` 转成弱 `belongs_to`；改为 `suggest_policies()` 输出 `preferred_doc_categories`。
- `rag.py` 的 `_prepare_graph_plan()` 已在 Graph 链接后调用 `refine_from_graph()`，通过 `entity_ref` 细化 policy 选择。

**仍保留的过渡期双读（已切断，以下为历史记录）：**

| 入口 | 文件 | 切断前行为 | 切断后（当前） |
|---|---|---|---|
| 检索扩展 / TopK | `retrieval_strategy.py` | legacy `recall_terms` fallback | 只读 policy `query_hints` |
| 检索后评分 | `retrieval_quality.py` | legacy profile 评分 | Graph 事实驱动 `score_signals()` |
| Resolver 默认构造 | `retrieval_intent.py` | policies + legacy 双读 | `default()` 仅 policies |
| Plan 评分路径 | `retrieval_intent.py` | `_score_legacy_doc()` | 已删除 |
| Section 匹配辅助 | `retrieval_intent.py` | legacy `section_families` | Graph provider |

**切断前置条件（2026-07-10 已全部满足）：**

1. pipeline 三表 alias、sibling、`recall_terms` 已通过 `profile_sync` batch apply 写入正式 Graph。
2. `domain_catalog` seed：pipeline 三表 `different_from` 已随 profile sync 写入（dom_builder 无 actionable 候选）。
3. 专项 Gate：`validate_task81_graph_gate.py` → **PASS**（全图 104 条 `missing_evidence` 单独记录）。
4. 集成验证：专项 pytest 17 passed；等价性测试 `test_intent_scoring_equivalence.py` 通过。

**切断步骤（已完成）：**

- [x] **Step 1**：`RetrievalIntentResolver.default()` 只加载 policies；`for_migration()` 供 CLI/测试。
- [x] **Step 2**：`apply_quality_scores()` Graph-fact-driven；删除 `_score_legacy_doc()`。
- [x] **Step 3**：`expand_query()` 只使用 `policy.query_hints`。
- [x] **Step 4**：`section_matches_expected()` 改读 Graph provider。
- [x] **Step 5**：更新相关测试；新增等价性与 production fixture 测试。
- [x] **Step 6**：正式库 apply + Gate PASS；`待办清单.md` §二.9/§二.10 已收口。

**验证命令：**

```powershell
.\venv\Scripts\python.exe -m pytest -q tests/test_retrieval_intent.py tests/test_graph_retrieval.py tests/test_routing_and_structured_boost.py tests/test_profile_graph_sync.py
```

---

### Task 9: 锁死 `defined_in` 语义

**Files:**
- Modify: `rag_knowledge/models/graph_schema.py`
- Modify: `rag_knowledge/services/graph_retrieval.py`
- Modify: `rag_knowledge/services/graph_extraction/prompts/llm_graph_extractor_v1.md`
- Test: `tests/test_knowledge_graph.py`
- Test: `tests/test_graph_retrieval.py`
- Test: `tests/test_llm_graph_extractor.py`

**Interfaces:**
- `validate_relation("Tool", "defined_in", "Section") == (True, "")`
- `validate_relation("Tool", "defined_in", "Step")` 返回 false。

- [x] **Step 1: 写目标类型测试**

```python
def test_defined_in_only_targets_document_or_section():
    assert validate_relation("Tool", "defined_in", "Document")[0]
    assert validate_relation("Tool", "defined_in", "Section")[0]
    assert not validate_relation("Tool", "defined_in", "Step")[0]
    assert not validate_relation("Tool", "defined_in", "ConfigItem")[0]
```

- [x] **Step 2: 确认当前 unrestricted 行为导致失败**

```powershell
.\venv\Scripts\python.exe -m pytest -q tests/test_knowledge_graph.py -k defined_in
```

- [x] **Step 3: 收紧 schema 并统一注释/Prompt**

从 `UNRESTRICTED_RELATIONS` 删除 `RelationType.defined_in`；保留 `VALID_RELATION_PAIRS` 中所有 source entity type 到 Document/Section 的合法组合。

把 GraphExpander 注释改成：

```python
# Entity defined_in Document/Section; chunk evidence uses entity_chunk_links.
```

Prompt 写明 `defined_in` 的 target 只能是 Document 或 Section，不能输出 Chunk。

- [x] **Step 4: 验证并提交**

```powershell
.\venv\Scripts\python.exe -m pytest -q tests/test_knowledge_graph.py tests/test_graph_retrieval.py tests/test_llm_graph_extractor.py
git add rag_knowledge/models/graph_schema.py rag_knowledge/services/graph_retrieval.py rag_knowledge/services/graph_extraction/prompts/llm_graph_extractor_v1.md tests/test_knowledge_graph.py tests/test_graph_retrieval.py tests/test_llm_graph_extractor.py
git commit -m "fix: enforce defined-in graph semantics"
```

---

### Task 10: 抽出公开 ChunkIndexLookupService

**Files:**
- Create: `rag_knowledge/services/chunk_index_lookup.py`
- Modify: `rag_knowledge/services/chunk_admin.py`
- Modify: `rag_knowledge/services/graph_retrieval.py`
- Create: `tests/test_chunk_index_lookup.py`
- Modify: `tests/test_chunk_admin.py`
- Modify: `tests/test_graph_retrieval.py`

**Interfaces:**
- `ChunkIndexLookupService.by_chunk_id(chunk_id: str) -> dict`
- `ChunkIndexLookupService.all() -> dict[str, dict]`
- `GraphRetriever` 构造函数新增关键字参数 `chunk_index_lookup=None`，保留现有参数与默认值。

- [x] **Step 1: 写公开 lookup 测试**

```python
def test_lookup_maps_chunk_ids_to_file_records(tmp_path):
    index = tmp_path / "file_index.json"
    index.write_text(
        json.dumps({"files": {"h1": {"chunk_ids": ["c1"], "kb_name": "文章附件"}}}),
        encoding="utf-8",
    )
    lookup = ChunkIndexLookupService(index)
    assert lookup.by_chunk_id("c1")["kb_name"] == "文章附件"
    assert lookup.by_chunk_id("missing") == {}
```

- [x] **Step 2: 实现只读服务**

解析失败返回空 map 并记录 warning；不得依赖 `ChunkAdminService`。

- [x] **Step 3: 两个消费者改用公开接口**

`ChunkAdminService.list_chunks()` 使用 `self._chunk_index_lookup.all()`。

`GraphRetriever.retrieve()` 优先 Chroma metadata，再 fallback index：

```python
actual_kb_name = meta.get("kb_name")
if not actual_kb_name:
    actual_kb_name = self.chunk_index_lookup.by_chunk_id(chunk_id).get("kb_name")
if kb_name and actual_kb_name != kb_name:
    continue
```

删除对 `ChunkAdminService()._file_lookup()` 的 import/call。

- [x] **Step 4: 验证并提交**

```powershell
.\venv\Scripts\python.exe -m pytest -q tests/test_chunk_index_lookup.py tests/test_chunk_admin.py tests/test_graph_retrieval.py
git add rag_knowledge/services/chunk_index_lookup.py rag_knowledge/services/chunk_admin.py rag_knowledge/services/graph_retrieval.py tests/test_chunk_index_lookup.py tests/test_chunk_admin.py tests/test_graph_retrieval.py
git commit -m "refactor: expose chunk index lookup boundary"
```

---

### Task 11: 统一 Graph-RAG kwargs 与融合逻辑

**Files:**
- Modify: `rag_knowledge/services/rag.py`
- Modify: `tests/test_rag_stage6.py`
- Modify: `tests/test_graph_retrieval.py`

**Interfaces:**
- `RagChain._build_graph_kwargs(plan, context, docs, *, include_cache_fields) -> dict`
- `RagChain._fuse_graph_docs(docs, graph_docs, *, top_k, graph_weight, excluded_chunk_ids, graph_guard) -> list[Document]`

- [x] **Step 1: 写 entrypoint 一致性测试**

用 fake graph context 和 monkeypatch，分别执行 sync、async、stream 的检索准备；断言三个路径都传递：

```text
graph_docs
graph_weight
graph_excluded_chunk_ids
graph_guard
```

异步/cache 路径额外传递：

```text
graph_entity_ids
graph_revision
```

- [x] **Step 2: 实现 kwargs helper**

```python
def _build_graph_kwargs(
    self,
    plan,
    context,
    docs,
    *,
    include_cache_fields: bool,
) -> dict:
    if context is None or context.fallback_reason is not None or not docs:
        return {}
    result = {
        "graph_docs": docs,
        "graph_weight": self._graph_cfg.graph_weight,
        "graph_excluded_chunk_ids": context.excluded_chunk_ids,
        "graph_guard": getattr(context, "guard", None),
    }
    if include_cache_fields:
        result["graph_entity_ids"] = tuple(
            item.entity_id for item in getattr(plan, "linked_entities", ())
        )
        result["graph_revision"] = plan.graph_revision
    return result
```

- [x] **Step 3: 实现唯一融合 helper**

```python
@staticmethod
def _fuse_graph_docs(
    docs,
    graph_docs,
    *,
    top_k,
    graph_weight,
    excluded_chunk_ids,
    graph_guard,
):
    if graph_docs is None:
        return docs
    return GraphRetriever.fuse(
        docs,
        graph_docs,
        top_k=top_k,
        graph_weight=graph_weight,
        excluded_chunk_ids=excluded_chunk_ids,
        graph_guard=graph_guard,
    )
```

替换 `_aretrieve_uncached()`、`_retrieve_multi()`、`_aretrieve_multi_uncached()` 的三处直接 `GraphRetriever.fuse()`，以及 `query/aquery/stream_query` 的三段 kwargs 构建。不要重构 LLM、history、budget 或 web search 逻辑。

- [x] **Step 4: 验证并提交**

```powershell
.\venv\Scripts\python.exe -m pytest -q tests/test_rag_stage6.py tests/test_graph_retrieval.py tests/test_query_cache.py tests/test_query_contextualizer.py
git add rag_knowledge/services/rag.py tests/test_rag_stage6.py tests/test_graph_retrieval.py
git commit -m "refactor: unify graph retrieval integration paths"
```

---

### Task 12: 建立交付门禁并更新审查状态

**Files:**
- Create: `scripts/check_repo_hygiene.py`
- Create: `tests/test_repo_hygiene.py`
- Modify: `docs/3_待办清单/2026-07-09-当前分支严格代码审查问题清单.md`
- Modify: `docs/3_待办清单/待办清单.md`
- Modify: `AGENTS.md`
- Modify: `CLAUDE.md`

**Interfaces:**
- `scripts/check_repo_hygiene.py` 非零退出条件：工作树脏、`NUL`/tmp/debug 垃圾、必需 catalog 未跟踪。
- `.agents/`、`AGENTS.md`、Profile PRD 是当前已确认跟踪内容，不得按旧审查快照删除。

- [ ] **Step 1: 写 hygiene 解析单测**

```python
def test_forbidden_paths_are_reported():
    lines = ["?? NUL", "?? output.tmp", "?? result_debug.json"]
    assert find_forbidden_paths(lines) == ["NUL", "output.tmp", "result_debug.json"]


def test_current_tracked_agent_files_are_not_forbidden():
    lines = [" M AGENTS.md", " M .agents/skills/code-simplifier/SKILL.md"]
    assert find_forbidden_paths(lines) == []
```

- [ ] **Step 2: 实现只读交付检查**

脚本执行：

```text
git diff --check
git status --porcelain --untracked-files=all
git ls-files --error-unmatch data/domain_catalog.json
```

脚本不执行 add/commit/delete；只报告并非零退出。

- [ ] **Step 3: 运行后端全量验证**

```powershell
.\venv\Scripts\python.exe -m pytest -q
.\venv\Scripts\python.exe -m pip check
```

Expected: pytest 全绿，默认仍显示 integration deselected；`pip check` 输出 `No broken requirements found.`。不要沿用历史的 `405 passed` 数字，记录本次实际输出。

- [ ] **Step 4: 运行前端与静态交付验证**

```powershell
Set-Location web
npm run check
Set-Location ..
git diff --check
.\venv\Scripts\python.exe scripts/check_repo_hygiene.py
```

在最终提交之前 hygiene 可因工作树脏而失败；提交后必须为 0。

- [ ] **Step 5: 做问题覆盖复核**

在严格审查清单每个 P0–P4 项下追加：

```text
状态：已修复 / 已由后续提交确认 / 明确延期
实现提交：粘贴 `git rev-parse HEAD` 的实际输出
验证命令：粘贴本任务实际执行的完整命令
验证结果：粘贴测试工具输出的实际摘要
```

不允许把 `.agents/`、`AGENTS.md` 再标成垃圾文件；说明它们已在后续提交中被有意纳入仓库。

- [ ] **Step 6: 最终提交与干净树验证**

```powershell
git add scripts/check_repo_hygiene.py tests/test_repo_hygiene.py docs/3_待办清单/2026-07-09-当前分支严格代码审查问题清单.md docs/3_待办清单/待办清单.md AGENTS.md CLAUDE.md
git commit -m "docs: close strict review remediation checklist"
.\venv\Scripts\python.exe scripts/check_repo_hygiene.py
git status --short --branch
```

Expected: hygiene exit 0；`git status --short` 没有未确认文件。

---

## 最终验收门槛

必须同时满足：

```text
1. 当前 MVP-2 WIP 已被保留并拆成明确提交，不存在覆盖他人改动。
2. data/domain_catalog.json 已被 Git 跟踪，干净环境可加载。
3. GET /rebuild = 405；错误确认 = 422；scanner 缺失 = 503。
4. rebuild staging 失败不会改变 live collection/index；切换中失败会自动回滚。
5. 正式评测集不会被 run_eval_full.py 默认覆盖。
6. stale chunk IDs 在严格 A/B 下 BLOCK；content fallback 不改变 Recall/MRR 分母。
7. A/B 默认 fail-on-regression；生产评测默认 review_status=approved。
8. graph_schema.py 不含具体产品词典；SpecialRuleRestorer 已删除。
9. runtime Profile 不再保存/消费 alias、section family、different_from 等领域事实。
10. defined_in 只连 Document/Section，chunk 证据只走 entity_chunk_links。
11. GraphRetriever 不依赖 ChunkAdminService 私有方法。
12. sync/async/stream 共用 Graph kwargs 与融合 helper。
13. 默认 pytest 全绿、pip check 通过、npm run check 通过、git diff --check 通过。
14. git status 无未确认 agent/debug/tmp/NUL 文件，审查文档记录实际提交和验证证据。
```

## 执行暂停点

以下动作不属于自动执行范围，必须停下并向用户报告后再获得明确授权：

```text
1. 对正式 `/rebuild` 发请求。
2. 在正式 rag_relational.db 上 approve/apply candidate。
3. 执行小范围或全库 LLM 抽取。
4. 删除旧 live backup collection 或 graph backup。
5. push、创建 PR、同步 SVN。
```

## Self-review 结果

- 审查清单 P0-1 至 P4-4 均有任务或“后续提交已确认”的明确处置。
- 已额外覆盖审查后新增的 `domain_catalog.json` 被忽略问题和 Review CLI 定向测试失败。
- 向量 KB rebuild 与 Graph rebuild-safe 已明确区分。
- 所有修改任务都有失败测试、最小实现、验证命令和提交边界。
- 函数名在前后任务中保持一致：`fork`、`rename_collection`、`reload_index`、`_build_graph_kwargs`、`_fuse_graph_docs`、`ChunkIndexLookupService`。
- 未包含正式库 destructive/apply 命令。