# 主干锚定问答收口纪要（A1 + A2）

- **日期**：2026-07-24
- **范围**：固定题集端到端验收 + 口语 aliases 补强
- **配置**：`[graph_retrieval] enabled=false`，`query_rewrite_enabled=true`；helper=`gemma3:4b`
- **验收脚本**：[`scripts/run_backbone_anchor_acceptance.py`](../../../../scripts/run_backbone_anchor_acceptance.py)
- **台账**：[`A1-固定题集验收台账.md`](./A1-固定题集验收台账.md) / `.json`

## 结论

**锚定主线 A1/A2 收口通过（锚定层 6/6）。**  
图 chunk 仍关闭；未改 helper 模型（A3 未触发）。  
全链路仍有 2 题 query 层不稳定/语料债，**不扩本阶段 scope**。

| 层 | 结果 |
|---|---|
| 锚定层 soft_hit + canonical | **6/6 pass**（含口语「管线工具」→ PipelineBuilder） |
| 全链路 `/admin/qa-debug` | 4/6 pass；2 题记语料债 |
| 单测 | `test_graph_query_rewrite` / `backbone_guard` / `query_entity_guard`：**37 passed** |

## A2 别名变更

[`data/product_relation_backbone.json`](../../../../data/product_relation_backbone.json) — `PipelineBuilder.aliases`：

- 已有：`PiplineBuilder`
- **新增**：`管线工具`

复测前口语题无 soft_hit，helper 误锚 `PipelineWebGL`（`alias_gap`）。补别名后 soft_hit 强制为 `PipelineBuilder`，两道口语题锚定与 query 均 pass。

回归：`tests/test_graph_query_rewrite.py::test_soft_match_live_backbone_oral_pipeline_tool`

## 固定题集结果（复测后）

| ID | 问法 | 锚定 | Query | 备注 |
|---|---|---|---|---|
| sm-intro | 介绍一下 StampManager | pass | fail `empty_context` | 同题曾召回过 StampManager 部署块；本次空答，**语料/生成不稳定债** |
| sm-alias | Stamp管理中心是做什么的 | pass | pass | 来源含 StampManager部署 |
| tools-vs-server | StampTools 和 StampServer 区别 | pass | pass | canonical 双锚 Tools+Server；回答用到关系摘要骨架 |
| pipeline-belongs | PipelineBuilder 属于什么 | pass | fail `retrieve_miss` | 锚定与关系摘要正确；引用治理丢源（「无可验证引用」），**语料/引用债** |
| oral-pipeline-what | 管线工具是什么 | pass | pass | A2 后不再锚 PipelineWebGL |
| oral-pipeline-how | 怎么用管线工具 | pass | pass | 来源含 StampTools / PipelineBuilder |

## PRD §5 勾选

- [x] 口语/别名题：canonical / anchored_queries 落在正确主干实体；soft_hit 不被改锚
- [x] Hybrid 抽检主体与锚点一致（StampManager / StampGIS Tools / PipelineBuilder 口语题）
- [x] 产品关系题回答侧含主干一跳摘要语义（对比题可见）
- [x] 图谱 chunk 召回保持关闭
- [x] 改写失败不阻断主问答（本轮未出现阻断）
- [x] sm-intro / pipeline-belongs 全链路稳定通过 — **2026-07-24 语料/引用债修复后复验 6/6**

## 明确未做

- 未打开 `graph_retrieval.enabled`
- （历史）收口当日未跑 Round3 实 LLM / Round4 A/B → **其后均已完成**（见第 3 轮补 / 第 4 轮目录）
- 未做反问、未开 0D/0E chunk 治理
- 未评估更换 helper 模型（A3）

## 建议下一动作

1. ~~语料债：为 StampManager「产品介绍」类问题补/标介绍性 chunk；排查 PipelineBuilder 归属题引用被丢原因。~~ **已于同日修复（见下节）。**
2. ~~择机：Round3 实 LLM 补抽~~ ✅（见「第 3 轮补」）
3. ~~锚定主线稳定后再评估图 chunk / Round4~~ ✅（第 4 轮 PASS；**生产默认 on 仍未批**）

> **2026-07-28**：现行可选下一刀见 [待办清单](../../待办清单.md)（`quality --graph` 金边/非法边等）。

---

## 附录：2026-07-24 语料/引用债修复

### 根因

- `sm-intro`：检索已命中 StampManager 部署等片段时，模型偶发直接输出固定未命中提示 → 来源被清空。
- `pipeline-belongs`：检索命中 PipelineBuilder 手册章节，但模型回答无 `[n]` 引用（常依赖主干关系骨架）→ `govern_answer` 整段替换为「无可验证引用」并丢源。

### 修复

- [`rag_knowledge/services/evidence_pack.py`](../../../../rag_knowledge/services/evidence_pack.py)：当 context 含问题主体相关 chunk 时，对「空答 / 未命中套话 / 无引用」回退为规则4部分作答（带有效 `[n]`）。
- [`rag_knowledge/services/rag.py`](../../../../rag_knowledge/services/rag.py)：强化系统规则 11（介绍类禁止主体已命中仍输出固定未命中；关系类无归属原文时按规则4引用相关片段）。
- 单测：`tests/test_evidence_pack.py` 覆盖上述回退。

### 复验

`scripts/run_backbone_anchor_acceptance.py`：**锚定 6/6 + 全链路 6/6（exit 0）**。