# 融合上限（图块最多 1 席 / 保护文本 top1）— 2026-07-27

## 改动

`GraphFusionScorer.fuse` / `GraphRetriever.fuse`：

1. **protect_text_top1**：文本路第 1 个 chunk 固定占融合结果第 1（若该 chunk 在 exclusion 名单则跳过保护）
2. **max_graph_only_slots=1**：仅出现在 graph 通道、未出现在 retrieval 的块，最多进 topK **1** 席
3. 双通道块（既在文本又在图）不占用 graph-only 配额

单测：`tests/test_graph_retrieval.py`（24 passed）

## 复测（同一 20 题）

| 组 | Recall@3 | MRR | Hit@3 | 相对 A 回退 |
|----|---:|---:|---:|------|
| A 关图 | 51.04% | 0.6875 | 17/20 | — |
| B 开图（改前） | 51.67% | 0.7042 | 16/20 | 005, 020 |
| **B 开图 + 融合上限** | **57.29%** | **0.7417** | **18/20** | **无** |
| D allowlist（改前） | 52.29% | 0.6833 | 16/20 | 005, 020 |
| **D allowlist + 融合上限** | **52.92%** | **0.7000** | **17/20** | **仅 020** |

关键题：

| 题 | A | B 改前 | B 改后 | D 改后 |
|----|:---:|:---:|:---:|:---:|
| 005 工具概述 | 命中 | 回退 | **命中** | **命中** |
| 020 UV展开错误 | 命中 | 回退 | **命中** | 仍回退 |
| 008 值域映射约束 | miss | 命中 | **仍命中** | **仍命中** |

## 结论

1. **全开图路径（B）融合上限有效**：005/020 回退消失；Hit@3 17→18；R@3/MRR 同步上升；008 增益保留。
2. **Allowlist 路径（D）**：005 修好，020 仍回退（文本路在改写后 top1 已偏，保护的是偏后的文本 top1；与「图块灌入」不同层）。
3. 推荐本地体验：**可开全图 + 现成融合上限**；allowlist  alone 不必再当成主控噪手段。
4. 020 on D 若还要修：再收紧主干改写/allowlist 触发条件，或排错意图禁用图块。
