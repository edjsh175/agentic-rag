# 第 3 轮安全全量重建执行验收记录

- **记录日期**：2026-07-16（初记）／**现场复核**：2026-07-20
- **状态**：**未完成**。不得将「execute 已启动」解读为「Round 3 已完成」。
- **期望执行报告**（尚未生成）：`data/rebuild_safe_execute_round3.json` / `data/rebuild_safe_execute_round3.md`
- **期望执行日志**（尚未生成）：`data/graph_round3_execute.log` / `data/graph_round3_execute.err.log`
- **期望 post-audit**（尚未生成）：`data/graph_audit_post_round3.json`

## 0. 现场与文档偏差（2026-07-20）

| 项 | 文档/初记口径 | 现场（2026-07-20） |
|---|---|---|
| execute 报告 `rebuild_safe_execute_round3.*` | 待生成 / 曾写「等待完成」 | **不存在** |
| 执行日志 `graph_round3_execute*.log` | 曾引用 | **不存在** |
| `graph_audit_post_round3.json` | 验收命令目标 | **不存在** |
| `manual_graph_facts_pre_round3.json` | 过程表记 PASS | **不存在** |
| `data/backups/rag_relational_pre_round3*.db`、`half_execute_aborted.db` | 保护备份 | **不存在** |
| dry-run | PASS | 已归档：`data/archive/rebuild_reports/rebuild_safe_dry_run_pre_round3.*` |
| 7 月 17 日重建目录 | （易与 Round 3 混淆） | `data/archive/rebuild/20260717-101707-191444/`（RebuildCoordinator before 快照，**非** execute 报告） |

**判定**：先收口第 3 轮证据链；不要据此开第 4 轮。细节见 [剩余轮次总览 §1.1](../2026-07-13-知识图谱PRD剩余轮次总览.md)。

---

## 1. 开工前门禁

| 门禁 | 结果 | 证据 |
|------|------|------|
| 停止占用 `chroma_db` / `rag_relational.db` 的后端与评估进程 | 待补（下次 execute 前重做） |  |
| chunk 重建后一致性断言 | PASS（历史） | `KnowledgeBaseConsistencyService.assert_consistent()` |
| Chroma chunk 数 | 3678（历史基线） | 执行前基线 |
| 正式库基线 | entities=1230 / relations=2737 | dry-run 留痕 |
| dry-run | PASS（历史） | `data/archive/rebuild_reports/rebuild_safe_dry_run_pre_round3.json` / `.md` |
| 保留集包含 `seed:product_backbone` | PASS | dry-run preserved source stats |
| 保留集包含 `rule:profile_sync` | PASS | dry-run preserved source stats |
| 保留集包含 `rule:special*` | PASS | dry-run preserved source stats |
| backbone_integrity | 40/40 complete | dry-run report |

> 若 live 库相对 7 月 16 日 dry-run 已变化，execute 前须重跑 dry-run，勿直接复用归档数字当「当前」门禁。

## 2. 执行过程

| 项 | 结果 | 证据 |
|------|------|------|
| `rebuild-safe --execute --include-llm` | **未验收完成** | 无最终报告/日志；初记曾写 2026-07-16 14:06:28 启动，但产物缺失 |
| timestamped 备份生成 | **现场缺失** | 文档路径 `rag_relational_pre_round3_20260716_140630.db` 当前不存在 |
| manual export 生成 | **现场缺失** | `data/manual_graph_facts_pre_round3.json` 不存在 |
| 低置信候选处理 | 待补 | 预计标记 `round3_deferred_pending` 后拒绝 |
| 主干冲突候选 | 待补 |  |
| apply 批次 | 待补 |  |
| cleanup-stale-links | 待补 |  |
| final audit | 待补 |  |

## 3. §10 指标核对表

| 指标 | 目标 | 重建前 | 重建后 | 结果 | 备注 |
|------|------|------:|------:|:---:|------|
| stale_link_count | 0 | 待补 | 待补 | 待补 |  |
| Section 实体占比 | 较第 1 轮基线下降 ≥ 20% | ~82.9% | 待补 | 待补 |  |
| EnvironmentComponent | ≥ 10 | 待补 | 待补 | 待补 |  |
| Command | ≥ 30 | 待补 | 待补 | 待补 |  |
| Procedure | ≥ 10 | 待补 | 待补 | 待补 |  |
| Step | ≥ 50 | 待补 | 待补 | 待补 |  |
| ConfigItem | 明显高于重建前 | 待补 | 待补 | 待补 |  |
| 业务实体 evidence_text | 新增自动事实 100% 有值 | 待补 | 待补 | 待补 |  |
| missing_evidence | 不新增；存量有治理方案 | 待补 | 待补 | 待补 |  |
| 主干关系完整率 | 40/40 | 40/40（dry-run） | 待补 | 待补 |  |

## 4. 验收命令记录（完成后执行）

```powershell
.\venv\Scripts\python.exe run_graph_build.py audit --output-json data/graph_audit_post_round3.json
.\venv\Scripts\python.exe run_graph_build.py quality --graph --profile full
.\venv\Scripts\python.exe scripts/validate_task81_graph_gate.py --json
```

报告验收通过后，将一次性产物迁入 `data/archive/rebuild_reports/` 与 `data/archive/graph_rounds/`（见 [data目录约定.md](../../../5_操作指南与规范/data目录约定.md)）。

## 5. 结论

**Round 3 未完成。** 下一步：核对代码入口与 live 基线是否仍匹配 dry-run → 重新执行并落盘完整证据链 → 再填本表 §2/§3 → 方可进入第 4 轮。
