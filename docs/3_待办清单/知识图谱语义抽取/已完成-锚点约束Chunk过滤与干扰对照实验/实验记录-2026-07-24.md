# 实验记录 — 锚点约束 Chunk 过滤与干扰对照

- **日期**：2026-07-24
- **模式**：`retrieve_only`（判定 cited≈final context top 源；未跑回答 LLM）
- **配置基线**：`graph_retrieval.enabled=false`，`query_rewrite_enabled=true`
- **产物**：同目录 `fixtures/condition_{A,B,C}.json`、`condition_AB_compare.json`
- **脚本**：`scripts/run_anchor_chunk_filter_ab.py`

## 1. 条件定义

| 条件 | 开关 |
|------|------|
| A | 过滤关、allowlist 图 chunk 关 |
| B | `anchor_chunk_filter_enabled=true` |
| C | B + `anchor_graph_chunk_enabled=true`（allowlist=`PipelineBuilder`） |

## 2. 汇总（T1–T3）

| 条件 | pass | 正例命中率 | 干扰主引用率 | 空答率 |
|------|-----:|----------:|-------------:|-------:|
| A | 1/3 | 0.67 | 0.33 | 0 |
| B | 2/3 | 0.67 | **0** | 0 |
| C | **3/3** | **1.0** | **0** | 0 |

## 3. 逐题要点

- **T1「怎么用管线工具」**
  - A：正例进了，但 Server 工程配置 + 管线更新服务同批进入 top。
  - B/C：仅保留 StampTools / `PipelineBuilder` 章节。
- **T2「PipelineBuilder 怎么用」**：A/B/C 均正例命中、无表内干扰。
- **T3「管线工具发布要配什么」**
  - A/B：Hybrid 未捞到 PipelineBuilder 正例（正例缺失）；B 去掉了 Server 块，但仍剩同 PDF 弱相关噪声。
  - C：allowlist `entity_chunk_links` 注入正例后，过滤收口为 StampTools / PipelineBuilder。
- **T4「管线更新服务是做什么的」**：对照题；命中更新服务相关源，不要求 PipelineBuilder。

## 4. Go / No-Go

**结论：B 不足（T3 正例未入选），C 明显更好。**

| 项 | 决定 |
|----|------|
| 锚点硬过滤 | **落地为可配置能力**，默认 **false**（`anchor_chunk_filter_enabled`） |
| allowlist 小开图 chunk | **保留实现**，默认 **false**（`anchor_graph_chunk_enabled` + `graph_chunk_entity_allowlist`）；口语工具题可择机开 |
| 全库 `graph_retrieval.enabled` | **仍关** |
| 第 4 轮 GraphRAG | 当时延后；**2026-07-27 已 PASS**（本实验只证明 allowlist 小开可行，不等于批准生产默认开图） |

## 5. 代码落点

- [`rag_knowledge/services/anchor_chunk_filter.py`](../../../../rag_knowledge/services/anchor_chunk_filter.py)
- [`rag_knowledge/services/rag.py`](../../../../rag_knowledge/services/rag.py) 后处理过滤 + `_load_allowlisted_anchor_graph_docs`
- 配置：`config.ini` / `config-prod.ini` / `GraphRetrievalConfig`
- 单测：`tests/test_anchor_chunk_filter.py`
