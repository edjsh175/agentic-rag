# 知识图谱语义抽取 — 剩余轮次总览

- **更新日期**：2026-07-22
- **母文档（总 PRD）**：[2026-07-09-知识图谱语义抽取升级整体计划与PRD.md](2026-07-09-知识图谱语义抽取升级整体计划与PRD.md)

> **怎么看这份目录**：根目录只留两份导航——**总 PRD**（目标/范围/§10/§13）+ **本剩余轮次总览**（当前进度与下一动作）。已完成阶段进 `已完成-*`；未开始轮次进 `待执行-*`。

---

## 1. 当前状态

| 阶段 | 状态 | 文档位置 |
|:---:|------|----------|
| MVP-1/2 + Task 8.1/8.2 + Phase A/B/C | 已完成 | [已完成-MVP与底座/](已完成-MVP与底座/) |
| 第 1 轮 规则图谱补全 | 已完成 | [已完成-第1轮-规则图谱补全/](已完成-第1轮-规则图谱补全/) |
| 第 2 轮 LLM 小范围试点 | 已完成（GO） | [已完成-第2轮-LLM小范围试点/](已完成-第2轮-LLM小范围试点/) |
| 第 2.5 轮 产品关系主干 | 已完成（含 2026-07-22 预览正式替换） | [已完成-第2.5轮-产品关系主干/](已完成-第2.5轮-产品关系主干/) |
| **第 3 轮 安全全量重建** | **规则路径已完成；实 LLM 待补** | [待执行-第3轮-安全全量重建/第3轮执行验收记录.md](待执行-第3轮-安全全量重建/第3轮执行验收记录.md) |
| 第 4 轮 GraphRAG 实效验收 | 条件阻塞（须实 LLM 补抽或明确豁免后再开） | [待执行-第4轮-GraphRAG实效验收/执行PRD.md](待执行-第4轮-GraphRAG实效验收/执行PRD.md) |

```text
第 1 轮 ✅ → 第 2 轮 ✅ GO → 第 2.5 轮 ✅（2026-07-22 正式替换）
    ↓
第 3 轮 ✅ 规则 rebuild-safe 证据链已闭合；⚠ 实 LLM 因 Ollama 不可达未产出候选
    ↓
第 4 轮 GraphRAG A/B（建议先补实 LLM 或书面豁免 §10 数量项）
```

**原则**：产品关系主干（`seed:product_backbone`）为官方边界；规则与 LLM 不得产出与之矛盾的边。

### 1.1 现场口径（2026-07-22 更新）

| 产物 | 现场 |
|---|---|
| `data/rebuild_safe_execute_round3.json` / `.md` | **存在**（batch `9e7970eb-…`） |
| `data/graph_audit_post_round3.json` | **存在** |
| `data/manual_graph_facts_pre_round3.json` | **存在** |
| `data/backups/rag_relational_pre_round3_20260722_131948.db` | **存在** |
| 主干完整率 | **175/175** |
| Task81 Gate | **PASS** |
| 实 LLM 候选 | **0**（`192.168.10.158:11434` 不可达；编排曾用空 stub 仅保证管线，**不算 LLM 验收通过**） |

细节见 [第3轮执行验收记录.md](待执行-第3轮-安全全量重建/第3轮执行验收记录.md)。2026-07-20 的「execute 产物缺失」勘误已被本次执行覆盖。

生成物归档约定见 [data目录约定.md](../../5_操作指南与规范/data目录约定.md)。

---

## 2. 正式库基线（2026-07-15 / dry-run 留痕）

```text
entities: 1230
relations: 2737
seed:product_backbone: 36 实体 + 40 关系（主干完整，dry-run 40/40）
section_ratio: ~82.9%（待第 3 轮改善）
```

可用回滚参考（非 Round 3 专用命名备份）：

- `data/archive/rebuild/20260717-101707-191444/rag_relational.before.db`
- live：`data/rag_relational.db`

文档中旧路径 `data/backups/rag_relational_pre_round3*.db` 在当前现场**已缺失**；若需 Round 3 专用备份，须在下次 execute 前重新生成到 `data/backups/`（目录可新建）。

---

## 3. 下一动作（先收口第 3 轮，再谈第 4 轮）

**当前判断**：优先收口第 3 轮现场证据链，不要直接开第 4 轮。

**已完成准备**：chunk 治理与一致性门禁（历史记录）；`rebuild-safe --dry-run` 有归档报告；`SafeRebuildService` + `--execute` 代码与单测已落地。

**尚未完成**：带最终报告的 `rebuild-safe --execute --include-llm`、分拆审批/apply 留痕、post-round3 audit、§10 指标表实测。

```powershell
# 1. 如基线已变：重跑 dry-run（新报告写 data/ 根；旧报告在 archive）
.\venv\Scripts\python.exe run_graph_build.py rebuild-safe --dry-run `
  --output-json data/rebuild_safe_dry_run_pre_round3.json `
  --output-md data/rebuild_safe_dry_run_pre_round3.md

# 2. 正式重建（须先停占用 DB 的进程；完成后归档到 data/archive/rebuild_reports/）
$db = (.\venv\Scripts\python.exe -c "from rag_knowledge.services.graph_governance import resolve_db_path; print(resolve_db_path())").Trim()
.\venv\Scripts\python.exe run_graph_build.py rebuild-safe --execute --include-llm `
  --confirm-db-path $db `
  --backup-dir data/backups `
  --output-json data/rebuild_safe_execute_round3.json `
  --output-md data/rebuild_safe_execute_round3.md
```

细节见 [待执行-第3轮-安全全量重建/执行PRD.md](待执行-第3轮-安全全量重建/执行PRD.md) 与 [第3轮执行验收记录.md](待执行-第3轮-安全全量重建/第3轮执行验收记录.md)。

---

## 4. 轮次一览

| 轮次 | 核心目标 | 状态 |
|:---:|------|:---:|
| MVP 与底座 | audit / cleanup / export-manual / LLM 候选 / 归一化 / GraphRAG 接入 | ✅ |
| 第 1 轮 | 章节层级 + 数据规范表结构边 | ✅ |
| 第 2 轮 | StampTools LLM 试点闭环（precision 82.9%，GO） | ✅ |
| 第 2.5 轮 | 官方产品关系 seed 入库（40/40 边） | ✅ |
| 第 3 轮 | `rebuild-safe` + 主干边界内 rules/LLM 全量重建 | ✅ 规则完成；⚠ 实 LLM 待补 |
| 第 4 轮 | 专项问答 A/B、§10/§13 交付门禁 | 阻塞 |

### 产品关系数据三层（约定）

| 层 | 路径 |
|---|---|
| 原始材料 | `已完成-第2.5轮-产品关系主干/产品关系材料/原始材料/` |
| 规范化主干 | `data/product_relation_backbone.json` |
| 可编辑预览 | `data/product_relation_backbone_preview.json`（`?source=product_backbone_preview`） |
| 运行时图谱 | `data/rag_relational.db`（`created_by=seed:product_backbone`） |

---

## 5. 已完成阶段目录

| 阶段 | 目录 |
|------|------|
| MVP 与底座 | [已完成-MVP与底座/](已完成-MVP与底座/) |
| 第 1 轮 | [已完成-第1轮-规则图谱补全/](已完成-第1轮-规则图谱补全/) |
| 第 2 轮 | [已完成-第2轮-LLM小范围试点/](已完成-第2轮-LLM小范围试点/) |
| 第 2.5 轮 | [已完成-第2.5轮-产品关系主干/](已完成-第2.5轮-产品关系主干/) |

历史轮次审计/导出 JSON 现位于 `data/archive/graph_rounds/`（见 data 目录约定）。

---

## 6. 每轮共同约束

```text
1. 停止后端与其他占用 chroma_db / rag_relational.db 的进程后再做正式库写入
2. LLM / profile_sync / product_backbone_seed 候选禁止 --approve-all
3. apply 须 --confirm-db-path / --confirm-batch / --confirm-backup
4. 每轮开始前 audit，结束后 quality --graph
5. 规则与 LLM 不得产出与 seed:product_backbone 矛盾的边
6. 轮次报告验收通过后，将一次性产物迁入 data/archive/，勿与 live 真源混放
```

---

## 7. 非目标（延续母 PRD）

```text
前端审核台、Neo4j、在缺少主干边界时强行全库 LLM、把 RebuildCoordinator 向量库重建误当成图谱 Round 3
```
