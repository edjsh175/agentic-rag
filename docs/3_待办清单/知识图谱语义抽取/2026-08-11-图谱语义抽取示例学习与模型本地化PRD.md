# 图谱语义抽取 · 示例学习与模型本地化 · 产品需求说明书（PRD）

| 项目 | 内容 |
|------|------|
| 文档版本 | V1.2 |
| 基线日期 | 2026-08-11 |
| 修订 | V1.1：全面本地化、抛弃外部长期双轨。V1.2：**全体品类共用一套 universal 示例包**；不再规划/新增品类专包（含 Server 专包）；既有 `stamptools_v1` 叠加视为过渡残留，应收敛进 universal 或停用 |
| 状态 | **进行中**：策略 B（LLM 主路径 + universal exemplar + 规则兜底）已提交；StampServer 定向试抽已做；规则主干收紧与本地 JSON 达标待办 |
| 范围 | 图谱抽取 **单一** few-shot 示例包（universal）、跨品类 pattern 指导、规则叶子兜底、**以本地 Ollama 为唯一目标运行时**的抽取能力达标与退出外部依赖 |
| 实施原则 | **最小关系主干**：图只承载导航骨架；细节仍以文本召回为主。示例教「怎么挂」，不教「全挂」。**一套 pattern 指导所有类**。**交付与验收只认本地** |
| 关联文档 | [知识图谱语义抽取升级整体计划与 PRD](2026-07-09-知识图谱语义抽取升级整体计划与PRD.md)、[第 3 轮补 · 全库实 LLM 补抽](已完成-第3轮补-全库实LLM补抽与抽取优化/执行PRD.md)、[经验产品化 · 两段式捞边](已完成-经验产品化-两段式捞边与抽取硬化/执行PRD.md)、[第 4 轮 GraphRAG 实效验收](已完成-第4轮-GraphRAG实效验收/执行PRD.md)、[data 目录约定](../../5_操作指南与规范/data目录约定.md) |

> **阅读说明**：本文描述本轮「用**一套**黄金样板教会本地 LLM 指导所有品类 + 收紧规则主干 + 退出外部抽取依赖」的目标、契约与分阶段验收，不是全库图谱总 PRD。  
> **战略声明**：**目标不是长期 local/mix 可切换**，而是逐步抛弃 Google/OpenAI 等外部抽取模型，图谱语义抽取**全部跑在本地**（Ollama 等）。迁移期内允许用外部模型做一次性摸底或正例起草，但**不得**把「mix 好看」写成验收通过。  
> **边界声明**：不改变 GraphRAG 生产默认关图政策；不把关系正文当回答事实；正式库写入仍须 review/apply 门禁。  
> **进度（2026-08-11）**：  
> - 已落地：`pattern_universal_v1` 全品类注入、ChapterLeaf/ServerLeaf 兜底去重、LLM 默认开、`--no-llm` / `--include-llm`（Git `ba688bc` / SVN r174）；代码里仍残留可选 `stamptools_v1` 叠加（**非目标形态**，待收敛）  
> - 已试抽：StampServer「服务部署」定向批；**本地 JSON 几乎全灭**（阻塞本地化）；规则暴露 `has_procedure` 扇入平铺；mix 仅作摸底参考  
> - 未做：Server 规则主干收紧、本地结构化输出达标、停用外部抽取配置、**取消品类专包叠加**、全量 apply  
> - **明确不做**：`stampserver_v1` 等新品类专包


## 一、项目概述

### 1.1 背景与问题

图谱语义抽取已具备规则管线、LLM 抽取、候选 staging、审批 apply，以及 StampTools 等品类上的试点经验。当前矛盾集中在三点：

| # | 问题 | 表现 |
|---|------|------|
| P1 | **LLM 缺稳定「正例」** | 无 few-shot 时，跨品类输出风格漂移：叶子 Procedure/Command 漏抽，或把章节标题、模块文件名乱挂成主干边 |
| P2 | **规则与 LLM 职责不清** | 纯规则易漏语义叶；纯 LLM 不稳定。需要「LLM 主抽叶子 + 规则兜底」而不是双轨各抽一套再硬合并 |
| P3 | **抽取仍依赖外部模型，本地接不住** | 开发习惯用 `config-mix`（Gemini 等）；目标运行时是本地 Ollama（`config-local` / `qwen3.5:9b` 等）。同一套 exemplar + prompt 下，本地 JSON 失败率极高 → **策略 B 在目标环境名存实亡**，无法宣布本地化完成 |

另外，试抽 StampServer 时观察到：即便 LLM 失败，规则兜底仍会产生 **多 Service → 同一「Apache服务配置」** 的 `has_procedure` 扇入——违反「最小主干」，审图时像「全部平铺挂接」。本地未达标时，规则就是唯一路径，此病必须先治。

根因不是「再加一个品类开关」或「做个切换器」，而是缺少：

1. **可版本化的单一示例学习契约**（仅 universal pattern，占位符覆盖 Tool/Service 等）——专门把**本地**模型教会、指导**所有** `doc_category`  
2. **明确的主路径 / 兜底边界**（策略 B），且兜底也满足最小主干  
3. **本地抽取达标定义与退出外部依赖的阶段门**（不是长期双轨对照；也不是按品类叠专包）


### 1.2 第一性原理（设计公理）

| # | 公理 | 含义 |
|---|------|------|
| A1 | **最小关系主干** | 图只保留产品/服务导航必需的实体与边；配置片段、长命令参数、叙述性说明仍以 chunk 文本召回为主 |
| A2 | **一套 pattern 指导所有类** | 全体 `doc_category` 共用同一套 universal exemplar；用 `{Tool}`/`{Service}` 等占位，不按品类再叠专包；教模式不教穷举 |
| A3 | **层级优于平铺** | `服务部署 → 具体 Service`、文档 `Section` 树清晰；禁止 N 个叶子共用同一父章节名当 Procedure 目标形成扇入 |
| A4 | **证据可核对** | 实体/关系须能对照 chunk 原文或 `section_path`；无证据不进 pending 主干边 |
| A5 | **本地为唯一交付运行时** | 图谱抽取的默认与验收环境是本地 LLM；外部模型仅迁移期临时使用，达标后停止依赖 |
| A6 | **失败可降级且不污染主干** | 本地 LLM 单 chunk 失败不阻断整批；规则兜底必须仍满足 A1–A3，禁止「失败就胡挂」 |
| A7 | **事实通道不变** | 回答事实仍来自检索 context；图用于导航/消歧/扩召回（受既有 GraphRAG 开关约束） |

违反 A1/A3/A5 的方案（例如用外部模型结果冒充本地验收通过，或为提高召回把所有正文标题挂成 `has_procedure`）不在本 PRD 接受范围内。


### 1.3 目标

1. **示例学习功能**：维护**唯一** Exemplar Pack（`pattern_universal_v1` 或其后继版本）；全品类只注入这一套；预算可控——**以本地模型能跟样为准**。  
2. **策略 B 管线**：默认 LLM 主路径抽导航叶；ChapterLeaf / ServerLeaf 仅对缺失叶子做去重兜底；CLI 可强制 `--no-llm` / `--include-llm`。  
3. **全面本地化**：抽取 LLM 最终只跑本地（Ollama + 项目约定模型）；通过配置与流程**逐步停用** mix/外部抽取路径。  
4. **本地达标验收**：固定 chunk 集在 `config-local` 下达到 JSON/可用候选/主干审阅门槛；外部摸底结果不计入通过。  
5. **Server 主干收口**：消除规则侧章节名扇入；在**同一套** universal 指导下做本地扩面（不新增 Server 专包）。


### 1.4 非目标

| 非目标 | 说明 |
|--------|------|
| 长期 local / mix 双轨并行验收 | **明确不做**；mix 不是产品形态 |
| 全库自动 apply / 免审写入 | 仍走候选 review + apply 确认三件套 |
| 生产默认打开 `graph_retrieval` | 第 4 轮结论仍有效；本 PRD 不批准生产开图 |
| 把关系 / evidence 当回答正文事实 | 与既有回答契约一致 |
| 按品类维护多套 exemplar 专包（Tools/Server/…） | **明确不做**；差异用 universal 占位符与规则兜底表达，不叠专包 |
| 动态相似 chunk 检索 exemplar（RAG-for-exemplar） | 可列为后续；本轮用静态单一 JSON 包 |
| 管理后台可视化编辑 exemplar | 本轮以文件维护；UI 另案 |
| 为每个数字单独加配置爆炸 | 预算常量可代码内固定；环境差异走 ini |
| 用更强外部模型掩盖规则或本地缺陷 | 规则在 `--no-llm` 下也须满足最小主干；本地未达标不得结项 |


### 1.5 成功标准（总览）

| 编号 | 标准 | 度量方式 |
|------|------|----------|
| S1 | 任意 `doc_category` 的 LLM prompt 均注入 universal pattern（非 `(none)`） | 单测 + 本地实抽抽查 |
| S2 | 多品类（至少 Tools + Server）在**仅 universal** 注入下，叶子命名/挂接风格可人工勾选为「符合最小主干」（**本地开 LLM**） | 跨品类定向 chunk 审阅表 |
| S3 | LLM 已产出叶子时，规则兜底不重复造同键实体/边 | 单测 `leaf_fallback` |
| S4 | `--no-llm` 与默认开 LLM 行为可预期；配置缺省 `enabled=true` | CLI + config 单测 |
| S5 | 同一批 StampServer「服务部署」chunk：`--no-llm` 下 **无**「多 Service → 同一 Apache服务配置」扇入 | 批次关系计数 + 原文对照 |
| S6 | **本地开 LLM** 定向批：JSON 成功率与可用候选达到阶段门槛（见 §3.3）；失败不阻断批次；降级结果仍满足 S5 | `config-local` 定向批 + diagnostic |
| S7 | **本地化退出门**：默认抽取配置指向本地；文档/运维指南不再要求 mix 才能抽图；外部抽取仅保留「禁用/遗留」说明 | 配置审查 + 操作说明 |
| S8 | 不默认 apply；任何正式库写入有人工审与 confirm | 流程检查 |

**S6 阶段门槛（本地，操作定义）**

| 档位 | 条件 | 是否可进入下一阶段 |
|------|------|-------------------|
| 未达标（当前） | 可用 LLM 候选 ≈ 0，或 JSON 成功率 &lt; 30% | 否；须继续 Phase 2 |
| 可用 | 同题集 JSON 成功率 ≥ 70%，且主干人工抽检无系统性扇入/越级 | 可做跨品类本地扩面试抽 |
| 达标可结项 | 连续两轮固定题集达「可用」，且 S5/S7 勾选 | 可宣布抽取路径本地化完成 |

2026-08-11 实测：**未达标**（JSON 几乎全灭）。外部 mix 摸底「有候选」**不**改变此判定。


## 二、现状基线（2026-08-11）

### 2.1 已落地能力

| 能力 | 位置 | 说明 |
|------|------|------|
| Universal exemplar | `data/extraction_exemplars/pattern_universal_v1.json` | **目标唯一包**；全品类 pattern；`{Tool}`/`{Service}` 占位 |
| 品类叠加（残留） | `stamptools_v1.json` + `exemplar_pack._CATEGORY_PACKS` | 历史过渡；**目标形态应取消叠加**，内容有价值则并入 universal |
| 注入逻辑 | `rag_knowledge/services/graph_extraction/exemplar_pack.py` | 现状仍可能叠专包；V1.2 要求收敛为只注 universal |
| 策略 B 管线 | `pipeline.py` + `leaf_fallback.py` | 结构规则 → LLM → leaf 兜底去重 → staging |
| Server/Chapter 叶子规则 | `server_leaf_extractor.py` / `chapter_leaf_extractor.py` | Procedure/Command/Format |
| LLM 默认开 | `[graph_extraction.llm] enabled=true`；`run_graph_build.py --no-llm` | 配置与 CLI 互斥覆盖 |
| 运行时配置 | `Config.graph_extraction_llm` + `RAG_CONFIG` | **目标**：`config-local` → ollama / 约定本地模型；`config-mix` 仅迁移期遗留 |

### 2.2 已验证事实（试抽）

| 环境 | 批次（示例） | 结论摘要 |
|------|----------------|----------|
| local（qwen3.5:9b） | `6fa625fc-…`（12 chunk） | **11/12 JSON 解析失败**；可用 LLM 实体/关系 ≈ 0；结果≈纯规则；**本地化阻塞** |
| mix（Gemini，摸底） | `ca38c625-…`（9 chunk） | 说明「题集与 prompt 在强模型上可出候选」；**不计入验收**；仍有类型/挂接噪声，不可 apply |
| 规则共病 | 两环境 | `Service → has_procedure → Apache服务配置` 扇入；父章节标题被当成 Procedure；FunctionArea `Service::父段` 反向挂接仍拧 |

### 2.3 现行数据流（抽取）

```text
chunk（含 doc_category / section_path）
  → 结构规则（Section / FunctionArea / …）
  → LLM（prompt 含 {extraction_exemplars}；失败 → diagnostic）
  → leaf_fallback（ChapterLeaf / ServerLeaf，对已有叶子去重）
  → extraction_candidates（pending）
  →（人工）review → apply（须 confirm；本 PRD 默认不自动走）
```

目标稳态：上述 LLM 步**仅**调用本地 endpoint。迁移期内若临时指向外部，纪必须标注「非验收、不结项」。


## 三、功能需求

### 3.1 示例学习（Exemplar Pack）

#### FR-E1 包格式与存放

- 路径：`data/extraction_exemplars/*.json`（纳入版本管理；非 runtime 乱写）。  
- 根对象字段建议：`id`、`version`、`description`、`exemplars[]`。  
- 单条 exemplar：`section_path`（可选）、`doc_category`（可选）、`input_excerpt` 或场景说明、`expected`（实体/关系示意）、`notes`（为何这样挂 / 勿挂什么）。  
- Universal 包使用跨品类占位符，避免写死单一产品名导致 Server/Tools 串味。

#### FR-E2 注入策略（单一包）

| 优先级 | 内容 | 说明 |
|--------|------|------|
| 1 | 始终且仅注入 `pattern_universal_v1`（或其版本后继） | 全品类同一套 |
| — | **禁止**按 `doc_category` 再追加专包 | 新品类不新建 `*_v1.json` 映射 |

预算：以单一包总长可控为准（当前实现量级约 3200 chars 内；专包预算槽位取消后全部留给 universal）。

代码收敛要求：清空或停用 `exemplar_pack._CATEGORY_PACKS`；`format_exemplars_for_prompt` 不再读取品类文件。有价值的 StampTools 正例应**改写为占位符后并入** universal，而不是继续叠文件。

#### FR-E3 示例内容原则

- **要教**：叶子命名、`belongs_to` 层级、`has_procedure` / `runs_command` 的合法端点、与文本召回的分工（细节不进图）；用 `{Tool}`/`{Service}` 等覆盖 Tools 与 Server。  
- **不要教**：把整段配置、全部 `.so`、全部正文小标题抽成实体；不要示范「多服务共用一个父章节 Procedure」。  
- **不要**为 StampServer / StampTools 各维护专包；品类差异靠占位符 + 规则兜底，不靠第二份 few-shot。  
- 正例起草若曾借助外部模型，须经人工改写为「最小主干」风格后写入 **universal**；**不得**把外部原始 JSON 整包当 exemplar。

#### FR-E4 与 prompt 版本关系

- Exemplar 注入与 `prompt_version`（v3/v4）正交：换 prompt 不得悄悄关掉 universal。  
- 变更 exemplar 或 prompt 任一侧，须用同一固定 chunk 集在 **`config-local`** 做前后对照。


### 3.2 规则兜底与最小主干

#### FR-R1 叶子兜底

- LLM 已产出同名 Procedure/Format/Command（及对应关系）时，规则跳过同键。  
- LLM 全失败时，规则可全量兜底，但 **仍受 FR-R2 约束**。

#### FR-R2 Server 叶子收紧（待实现，本 PRD 下一刀）

针对 `Apache服务配置 > 服务部署 > {某服务}`：

1. Procedure **优先**取路径叶子（且符合标题规则），而非正文中的祖先章节名。  
2. 正文标题若等于 `section_path` 祖先段（如「Apache服务配置」「服务部署」），**禁止**作为本 chunk 的 Procedure。  
3. 泛名「服务配置」等不得成为多服务共享的 Procedure 目标。  
4. `--no-llm` 复测必须通过 S5。

#### FR-R3 不做的规则扩面

- 不在本轮用规则批量制造 ConfigItem 洪水。  
- 不把 Service 直接 `belongs_to` Product 以绕过「服务部署」等中间层（已有 backbone 冲突诊断应保留）。


### 3.3 模型全面本地化（取代「可切换双轨」）

#### FR-M1 目标运行时契约

| 项 | 要求 |
|----|------|
| 默认抽取环境 | `RAG_CONFIG=config-local.ini`（或等价：provider=ollama + 约定本地 model） |
| 配置入口 | `[graph_extraction.llm]` 的 provider / model / base_url；禁止业务代码写死外部模型名 |
| CLI | `--no-llm` 强制关；`--include-llm` 强制开；默认跟随 ini `enabled` |
| 外部配置（mix 等） | **迁移期遗留**：可用于一次性摸底或起草，**禁止**写入验收通过结论；达标后从操作指南主路径移除 |

#### FR-M2 本地可用性（硬要求）

- Ollama 不可达或模型未拉取：抽取批次仍完成；LLM 段记 diagnostic；规则兜底继续且须满足 S5。  
- 不得因单 chunk JSON 失败中断整批。  
- 本地失败率高时，纪要必须写明「结果≈规则路径 / 本地未达标」，**禁止**改用 mix 结果宣称「改造已验证」。

#### FR-M3 本地验收协议（唯一主路径）

固定 chunk 列表（建议 ≤15，含「服务部署」多样叶子）：

1. `config-local` + `--no-llm` → 验收 S5（规则主干）。  
2. `config-local` 默认开 LLM → 记 JSON 成功率、可用 LLM 候选、主干边样本 → 对照 S6 档位。  
3. （可选，迁移期）`config-mix` 同题摸底 → 仅写入「摸底附录」，**不**更新 S6/S7 勾选。  
4. **禁止**将试抽批自动 apply。

#### FR-M4 退出外部依赖（结项条件）

同时满足：

1. S5、S6「可用」以上、S8 勾选；  
2. 日常抽图操作说明只描述本地启动与模型拉取；  
3. 团队约定：新试抽 / 扩面默认禁止依赖外部 API Key。  

未满足前，状态保持「本地化进行中」，不得写「已支持本地化完成」。


## 四、分阶段计划

### Phase 0 — 策略 B + Exemplar 骨架（已完成）

- [x] universal 全品类注入（策略意图）；品类叠加代码为过渡残留  
- [x] LLM 默认主路径 + leaf 兜底去重  
- [x] CLI `--no-llm` / `--include-llm`  
- [x] 单测与 Git/SVN 提交  

### Phase 1 — Server 规则最小主干收紧（下一步，优先）

**为何仍优先**：本地 LLM 未达标时规则是唯一路径；本地达标后规则扇入仍会污染 staging。先满足 A1/A3/S5。

交付：

1. 修改 `server_leaf_extractor.py`（祖先标题禁止、叶子优先）。  
2. 单测覆盖授权服务路径 / HTTPD 叶子。  
3. `config-local` + `--no-llm` 同批复测 + 原文核对纪要。  

**出口**：S5 勾选；不 apply。

### Phase 2 — 本地 LLM 结构化输出达标（本地化主攻）

在 Phase 1 通过后：

1. 增强 JSON 清洗/截断修复、输出约束或必要的 prompt/参数调整（最小改动；目标是本地模型稳定出合法结构）。  
2. 必要时评估换用/加大本地模型（仍限 Ollama 本地），以 S6 为准，不以「接近 Gemini」为结项条件。  
3. 同批 `config-local` 开 LLM 复测，直至 S6 达「可用」。  
4. **禁止**用「改回 mix 验收」代替本阶段。

**出口**：S6「可用」；纪要明确本地模型名与成功率。

### Phase 3 — 取消专包叠加 + 本地跨品类扩面

- 代码与数据：停用品类专包注入；必要时把 `stamptools_v1` 中仍符合 A1/A2 的条目并入 `pattern_universal_v1` 后归档/删除专包文件。  
- **不新增** `stampserver_v1` 或任何 `_CATEGORY_PACKS` 映射。  
- Phase 1 + Phase 2「可用」后：用**同一套** universal 对 Server/Tools 等子集做本地扩抽 → 人工审 → 分拆 approve；全库 apply 另立执行卡。  
- 若某品类仍抽歪：优先改 universal 占位示例或规则兜底，**禁止**开新品类包绕过。

### Phase 4 — 退出外部抽取依赖与观测（结项）

- 操作指南 / 默认配置口径改为仅本地；mix 抽取标为遗留或移除主路径。  
- 抽取 stats 明确区分：`llm_json_ok` / `llm_usable_candidates` / `leaf_fallback_*`。  
- exemplar 变更 checklist：变更后必须本地同题复测。  
- 勾选 S7；宣布本 PRD 范围内「抽取路径本地化完成」（仍不等于生产开图）。


## 五、验收与审阅清单（人工）

对每一试抽批次，审阅人按下列勾选（抽检 ≥ 5 个 chunk 原文）：

| 项 | 通过条件 |
|----|----------|
| 最小主干 | 无明显「能挂尽挂」；Config/长命令未灌进图 |
| 层级清晰 | 能说出 Service 应挂在哪一层；无大量越级 `belongs_to` |
| 非平铺 | 无多叶子共用同一父章节 Procedure 扇入 |
| 原文一致 | 抽中的实体名/边在原文或 section_path 有依据 |
| 示例有效 | （本地开 LLM 且 JSON 成功时）叶子命名接近 exemplar 模式 |
| 环境诚实 | 纪要写明 **config-local**、模型名、`--no-llm` 与否、JSON 失败率；若用了 mix 须标注「摸底非验收」 |


## 六、风险与依赖

| 风险 | 影响 | 缓解 |
|------|------|------|
| 本地小模型 JSON 长期不稳 | 无法退出外部依赖 | Phase 2 必达；可换更大本地模型；绝不结项于 mix |
| 用 mix 结果自我安慰 | 假本地化 | S6/S7 只认 local；PRD 明确禁止 |
| Exemplar 写太偏某一产品 | 其他品类学歪 | 强制占位符；跨品类审阅 S2；禁止开专包「补丁」 |
| 规则收太死漏叶子 | 导航缺失 | 允许叶子标题 Procedure + link；细节靠文本 |
| 误 apply 噪声批 | 污染正式库 | 默认不 apply；confirm 三件套 |
| prompt_version 与 exemplar 不同步 | 本地回归失败 | 变更任一侧强制 local 同题复测 |


## 七、关键命令（操作备忘）

```powershell
# 【主路径】本地抽取（默认开 LLM）——验收只认这条
$env:RAG_CONFIG="config-local.ini"
$env:PYTHONPATH=(Get-Location).Path
.\venv\Scripts\python.exe run_graph_build.py extract --chunk-id <id> ...

# 只验规则主干
.\venv\Scripts\python.exe run_graph_build.py extract --no-llm --chunk-id <id> ...

# 【迁移期摸底，非验收】外部模型——结果不得写入通过结论
# $env:RAG_CONFIG="config-mix.ini"
# .\venv\Scripts\python.exe run_graph_build.py extract --chunk-id <id> ...
```

正式写入仍须：`review` → `apply` + `--confirm-db-path` / `--confirm-batch` / `--confirm-backup`。本 PRD 范围内的试抽批默认停留在 pending。


## 八、文档维护

| 变更类型 | 要求 |
|----------|------|
| 完成 Phase 1/2/3/4 | 更新本文「状态 / 进度」表与成功标准勾选 |
| 重要试抽 | 在本目录或 `docs/2_历史记录与总结/` 增短纪要，链回本文 S5–S7；默认只报 local |
| 与总览关系 | 总览/剩余轮次文档可链至本文；本文不替代第 1–4 轮已完成执行 PRD |

---

**一句话**：全体品类共用**一套** universal 示例包，教会**本地** LLM「最小主干怎么挂」；规则兜底先消扇入；外部模型只作迁移期摸底并最终抛弃——不搞品类专包，下一步先收紧 Server 规则，再攻本地 JSON 与取消专包叠加。
