# RAG 项目交付前查漏补缺待办清单

- **记录日期**：2026-07-03
- **最近更新**：2026-07-03，Git 远端 main 推送后同步更新
- **阶段名称**：交付前查漏补缺阶段
- **检查范围**：`E:\申浩霖实习文件夹\rag_cy\rag`
- **整理依据**：代码审查、Git 工作区检查、测试运行、文档同步检查
- **远端状态**：已推送到 `origin/main`
  - 最新已推送提交：`21b6847 chore: finalize rag delivery updates`
  - 本地 `main` 与 `origin/main` 已同步
- **历史验证结果（2026-07-03）**：
  - 后端全量单测：`199 passed, 21 warnings, 8 subtests passed`
  - `pip check`：`No broken requirements found`
  - 前端构建：`npm run build` 通过
  - 前端 citation 手动测试：`node --test src/utils/*.test.mjs`，`4 passed`
- **2026-07-09 复核结果**：
  - 知识库一致性审计：通过，`file_index.json` 与 Chroma 均为 767 chunks，缺失 0，孤儿 0。
  - 默认 pytest：未通过，当前 `51 failed, 303 passed, 6 deselected`。
  - 失败主要原因：测试未全部迁移到 `isolated_storage`，被正式库路径防污染熔断器拦截。
  - 当前状态：正式库防污染已生效，但测试隔离迁移未完成。

- **2026-07-09 知识图谱语义抽取升级整体计划与 PRD**：
  - 文档：`docs/3_待办清单/2026-07-09-知识图谱语义抽取升级整体计划与PRD.md`
  - 核心调整：从“文档级规则硬编码抽取”升级为“Schema 约束 LLM 抽取 + 规则抽取 + 实体融合 + 安全重建”。
  - 重点覆盖：
    - Graph Audit 与 stale link 清理
    - domain_catalog 种子体系
    - LLMGraphExtractor
    - Candidate Normalizer / Entity Resolution
    - 审批分层与 rebuild-safe
    - GraphRAG 质量门禁
  - 当前判断：后续知识图谱建设以该 PRD 为主，不再针对单个文档增加大量硬编码规则。

- **2026-07-09 知识图谱语义抽取 MVP 任务清单**：
  - 文档：`docs/3_待办清单/2026-07-09-知识图谱语义抽取MVP任务清单.md`
  - 状态：**已于 2026-07-09 17:30 完成**
  - 覆盖范围：
    - Graph Audit 与 stale link 清理（stale links 已完全清除并有单元测试）
    - Graph Cleanup-stale-links Command（支持事务性批量安全删除）
    - export-manual Command（导出 manual, seed, special 事实以 canonical name 呈现）
    - LLMGraphExtractor 基础版（严格的实体/关系/别名 schema 约束和置信度与证据校验，防污染诊断）
    - pipeline 融合（rule 与 LLM 候选 staging/apply，保留顶层 confidence/prompt_version/extractor_version 属性）
  - 执行原则：先建立可审计、可回滚、可保护人工事实的底座，再扩大 LLM 自动构建范围。

- **2026-07-09 知识图谱语义抽取 MVP-2 任务清单**：
  - 文档：`docs/3_待办清单/2026-07-09-知识图谱语义抽取MVP-2任务清单.md`
  - 阶段目标：从“能生成 LLM 候选”升级到“候选可归一化、可消歧、可审核、可安全应用”。
  - 重点任务：
    - domain_catalog 外置
    - CandidateNormalizer
    - Entity Resolution
    - LLM 候选质量评估
    - Review CLI 升级
    - Safe Rebuild Dry-run
    - 小范围 Apply 验证
    - Quality Gate v2

- **2026-07-09 严格代码审查新增问题清单**：
  - 文档：`docs/3_待办清单/2026-07-09-当前分支严格代码审查问题清单.md`
  - 重点覆盖：
    - P0：知识库 / Chroma / SQLite / 评测集数据污染风险
    - P1：领域事实源与检索策略职责边界
    - P2：Graph-RAG schema、Profile、Retriever 一致性
    - P3：评测集 stale chunk、fallback、回归门禁
    - P4：代码质量、测试缺口和重复逻辑
  - 当前判断：不建议仅以“测试通过”作为交付依据，需要先完成 P0 和事实源收口。

- **2026-07-09 通用深度架构审查遗漏问题清单**：
  - 文档：`docs/3_待办清单/2026-07-09-通用深度架构审查遗漏问题清单.md`
  - 重点覆盖：
    - P0：管理接口无鉴权、ChatStorage 路径安全、危险写入边界
    - P1：doc_category/review_status 等事实源分裂、规则补丁治理、评测口径分叉、博客发布事务
    - P2：模块边界、VectorStore 封装、routes 拆分、chunk identity 演化
    - P3：OpenAPI 类型生成、配置模块化、测试补充
  - 当前判断：可以继续开发，但应优先处理 P0/P1 治理问题，否则后续 Graph Agent、多知识库和自动知识构建会放大架构债。

---

## 一、当前总览

当前主线功能已经进入可交付前收口状态。后端全量测试、依赖检查、前端构建和前端 citation 测试均已通过，并且代码已推送到远端 `main`。

但是仍不建议把“Git 已推送”直接等同于“最终交付完全无风险”。目前剩余风险主要集中在 **SVN 交付清理、Excel 复杂真实文件测试、同步/异步检索路径完全统一、前端测试脚本接入、交付报告口径同步** 这几项。

当前优先级：

1. **P0：测试隔离收口** —— 将仍直接初始化 `Config()`、`VectorStore()`、`RagChain()`、`QueryPlanner()` 等对象的单元测试迁移到 `isolated_storage` 或纯 mock 配置，恢复默认 `pytest -q` 全绿。
2. **P0：UEModelBuilder 检索回归** —— 增加 `ModelBuilder -> ueModelBuilder` 追问场景测试，确保工程设置、数据管理等关键 chunk 稳定召回。
3. **P1：修复 section_path 污染** —— 处理 DOCX 标题边界，避免 `2）不支持自定义曲线 > UEModelBuilder > ...` 这类路径影响 Graph 和检索。
4. **P1：回答变短专项观测** —— 增加 final context、reranker 前后排序、query planner 结果日志，再决定是否调整 top_k/reranker/context budget。
5. **P2：继续处理原交付收尾事项（SVN、Excel fixture、前端脚本、Docker ignore、telemetry）。**

---

## 二、已完成事项

### 1. Git 工作区整理与远端 main 推送

**状态**：已完成。

已完成操作：

```text
git add -A
git commit -m "chore: finalize rag delivery updates"
git push origin main
```

推送结果：

```text
b2a5994..21b6847  main -> main
```

最终确认：

```text
HEAD -> main
origin/main -> 21b68476e12ff61e0be8a46136a98e5b8c832055
local main  -> 21b68476e12ff61e0be8a46136a98e5b8c832055
```

说明：推送中包含两个本地 ahead 提交：

```text
21b6847 chore: finalize rag delivery updates
beadb3b docs: design nonblocking history summary and stream status
```

---

### 2. `CLAUDE.md` 跟随项目进度更新

**状态**：已完成。

已补充内容：

```text
/stats/chunks
chunk_stats.py
chunk_hit_telemetry.py
query_contextualizer.py
/review/status 审核状态更新后 BM25 rebuild
对话式查询上下文化
多查询召回
上一轮来源锚点 query
chunk 命中统计
当前阶段：检索能力优化 + 交付前查漏补缺
```

已更新位置包括：

```text
当前阶段
核心功能
目录结构
问答流程
API 路由总览
关键设计决策
```

---

### 3. 补齐 `/stats/chunks` 的任务要求统计

**状态**：已完成。

当前已新增：

```text
GET /stats/chunks
rag_knowledge/services/chunk_stats.py
rag_knowledge/services/chunk_hit_telemetry.py
```

覆盖能力：

```text
Chunk 总量
平均 Chunk 长度
平均 Chunk Token 估算
最大/最小 Chunk 长度
按文件分布
按文件类型分布
按 review_status 分布
按 kb_name 分布
线上 chunk 命中次数
离线评估 hit rate 摘要入口
```

对应测试：

```text
tests/test_chunk_stats.py
```

验证结果：已随全量测试通过。

---

### 4. 审核状态更新后 BM25 同步

**状态**：核心问题已完成，仍建议后续做并发加固。

当前 `/review/status` 已在更新 Chroma metadata 后执行：

```text
BM25Store().rebuild()
clear_query_cache()
```

对应测试：

```text
tests/test_review_status_bm25_sync.py
```

已额外修复：

```text
BM25 在极小语料或特殊 IDF 情况下，score <= 0 会被全部跳过的问题。
现在增加了词面重合兜底，避免有关键词重合但被 BM25 非正分过滤掉。
```

验证结果：已随全量测试通过。

剩余建议：

```text
BM25Store.rebuild() 后续仍可进一步加锁，并将 bm25/docs/metadatas 用局部变量构建完成后一次性替换，降低极端并发读写风险。
```

---

### 5. 对话式查询上下文化与多查询召回

**状态**：已完成基础实现。

当前已新增：

```text
rag_knowledge/services/query_contextualizer.py
```

能力包括：

```text
识别依赖历史的追问
将“它/这个/继续说”等问题改写成独立查询
基于历史消息生成补强查询
基于上一轮来源生成来源锚点 query
LLM 不可用时使用启发式降级
```

对应测试：

```text
tests/test_query_contextualizer.py
```

验证结果：已随全量测试通过。

---

### 6. 测试隔离问题修复

**状态**：已完成。

发现问题：

```text
tests/test_chunk_stats.py 会向 sys.modules 注入假的 jieba / rank_bm25，导致同一 pytest 进程后续 BM25 测试拿到 stub，实现被污染。
```

修复方式：

```text
只有真实依赖不存在时，才注入 stub。
```

验证结果：

```text
tests/test_chunk_stats.py
tests/test_query_contextualizer.py
tests/test_review_status_bm25_sync.py
tests/test_upload_response.py
```

组合运行结果：

```text
45 passed, 19 warnings
```

并已随全量测试通过。

---

### 7. 统一同步和异步检索路径

**状态**：已完成。

已完成操作：
- 移除了同步 `_retrieve()` 中直接调用 `chroma.as_retriever` 的老 MMR 分支和 `_build_filter` 本地辅助函数。
- 同步 `_retrieve()` 在多知识库 fallback 时，改为对两个知识库分别调用 `self._strategy.retrieve(...)`。
- 检索返回的文档复用了统一的合并逻辑 `_merge_multi_kb_docs(...)`。
- 新增单元测试 `test_unknown_kb_sync_path_merges_two_targets_deterministically`，从参数透传、多路知识库检索逻辑以及最终合并去重结果等维度完整覆盖。

验证结果：
- `tests/test_rag_stage6.py` 和 `tests/test_retrieval_strategy.py` 全量通过。
- pytest 后端全量测试全部通过（200 passed）。

---

### 8. Profile 关系沉淀到知识图谱及图谱候选审批工作台（第一阶段）

**状态**：已完成第一阶段（意图关系候选抽取、CLI同步脚本与前端手动审批工作台开发）。

已完成操作：
- 后端新增 `ProfileGraphSyncService` (位于 `rag_knowledge/services/profile_graph_sync.py`)，负责从 `retrieval_intent_profiles.json` 中抽取稳定领域关系（同义别名、字段属性、兄弟关系等）为图谱候选实体/别名/关系边。
- 提供了命令行同步脚本 `sync_profiles_to_graph.py`（支持 `--dry-run` 预览及 `--apply` 实际批量写入）。
- 增加了完整的后端单测 `tests/test_profile_graph_sync.py`。
- 前端新增了图谱候选审批页面 `web/src/views/GraphCandidatesView.vue`，配合路由 `/admin/graph-candidates`，实现对 Profile 沉淀候选关系的批量审核（Approve/Reject）。
- 在 API 路由和导航栏中做了完整入口对接，实现了“意图治理 -> 稳定知识沉淀 -> 知识图谱”的闭环链路。

---

## 三、仍需处理事项

### 1. Profile 自动瘦身与清理机制开发

**状态**：未完成（属 `Profile关系沉淀到知识图谱PRD` 第二阶段）。

说明：目前完成了关系候选提取与入库，但在候选关系被审批通过正式沉淀为图谱事实后，需要开发从 `retrieval_intent_profiles.json` 中自动/手动清理、删减这部分已被图谱固化的知识属性（如别名、字段）的逻辑，避免双重维护，实现 Profile 瘦身。

---

### 2. RAG 检索治理与评测闭环升级 P1/P2 阶段开发

**状态**：未完成（属 `RAG 检索治理闭环升级 PRD` 后续阶段）。

说明：目前完成了 Dataset Health Check 数据集健康检查回归门禁，但在 PRD 中规定的后续阶段工作尚未展开。需要开发：
* 完善 `content_fingerprint`、`section_path` 校验与数据源校验机制。
* 增强 `hardcases` 评测集的自动生成与人工审核工作流。
* 实现 A/B 测试指标的历史版本与基准线（Baseline）管理。

---

### 3. 清理 SVN 工作副本污染

**状态**：未完成，仍是交付前最高风险。

Git 当前已经提交并推送，但 SVN 是另一套状态。此前检查到 SVN 工作副本中有大量运行时文件、依赖包、构建产物、向量库和日志变更：

```text
1188 !
 889 ?
3890 M
```

典型污染对象：

```text
venv/
.venv/
web/node_modules/
web/dist/
chroma_db/
chroma_db_backup_*/
logs/
watch_directory/
data/chats/
data/*.db
data/eval_dataset*.json
data/retrieval_ab_results.json
backups/
models/
.pytest_cache/
.git/
```

原因：`.gitignore` 不会自动作用于 SVN。若最终交付仍走 SVN，必须配置 `svn:ignore` 并清理已被 SVN 追踪或污染的运行文件。

建议处理：

```text
1. 先确认 SVN 最终需要提交哪些代码和文档。
2. 配置根目录、web/、data/ 等目录的 svn:ignore。
3. 对不该进入 SVN 的已跟踪运行产物执行 svn revert 或按公司规范移除。
4. 再次运行 svn status .，确认只剩应提交内容。
```

验收标准：

```text
[ ] svn status 不包含 venv/.venv
[ ] svn status 不包含 web/node_modules 或 web/dist
[ ] svn status 不包含 chroma_db/chroma_db_backup
[ ] svn status 不包含 logs
[ ] svn status 不包含 watch_directory 运行样本
[ ] svn status 不包含 data/chats 或本地数据库
```

---

### 2. Excel 支持补真实文件集成测试

**状态**：代码已支持基础解析，但真实复杂 fixture 测试仍不足。

当前代码支持：

```text
.xlsx -> openpyxl
.xls  -> xlrd
Excel 转 Markdown 表格格式入库
Markdown/Excel 表格切块时按完整行切分并重复表头
```

仍建议补真实 `.xlsx / .xls` fixture 测试，覆盖：

```text
多 sheet
空表
合并单元格
公式列
日期/数字格式
表头为空
超长表格切块
```

实现注意点：

```text
1. .xlsx 使用 data_only=True 只能读缓存公式结果，不会现场计算公式。
2. 合并单元格通常只有左上角有值，当前没有补齐合并区域值。
3. _rows_to_markdown() 当前以第一行列数为列数，第一行为空或后续列更多时可能截断。
4. .xls 日期可能是序列号，当前没有看到 xlrd.xldate_as_datetime 格式恢复逻辑。
```

验收标准：

```text
[ ] 新增真实 .xlsx fixture 测试
[ ] 新增真实 .xls fixture 测试
[ ] 覆盖多 sheet、空 sheet、公式缓存、日期/数字、合并单元格
[ ] 超长表格能按完整行切块并重复表头
```

---

### 3. 前端 citation 测试接入 npm scripts

**状态**：未完成。

当前情况：

```text
手动运行 node --test src/utils/*.test.mjs 已通过 4 tests。
但 web/package.json 目前只有 dev/build/preview，没有 test/check。
```

建议修改：

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "test": "node --test src/utils/*.test.mjs",
    "check": "npm run test && npm run build",
    "preview": "vite preview"
  }
}
```

验收标准：

```text
[ ] cd web && npm run test 通过
[ ] cd web && npm run check 通过
```

---

### 4. 关闭 Chroma telemetry，而不是只压日志

**状态**：部分处理。

当前测试和运行中仍能看到类似日志：

```text
Failed to send telemetry event ClientStartEvent: capture() takes 1 positional argument but 3 were given
```

当前代码主要是压制 logger，但没有彻底从 Chroma settings 或环境变量层关闭匿名 telemetry。

建议处理：

```text
1. 优先在 Chroma client settings 中显式设置 anonymized_telemetry=False。
2. 或在启动入口设置 ANONYMIZED_TELEMETRY=False。
3. 以 chromadb==0.6.3 和 langchain-chroma==0.2.3 的实际支持方式为准。
```

验收标准：

```text
[ ] 启动、查询、collection get/count 时不再出现 telemetry 发送失败日志
```

---

### 5. 更新过时的交付总结报告

**状态**：未完成。

以下文档仍可能存在过时口径：

```text
docs/2_历史记录与总结/2026-07-02/RAG工作完成总结报告-2026-07-02.md
```

需要重点核对并更新：

```text
Excel 状态：不能再写“尚未落地/未完成”，应改为“基础解析已落地，复杂真实文件兼容性待补充测试”。
测试数量：不能再写旧的 73 或 191，应更新为当前验证结果，或改成“以最新 pytest 输出为准”。
Rerank 方案：当前是本地 BGE/FlagEmbedding + CrossEncoder 备选，不支持 Jina/Cohere 外部 API。
```

验收标准：

```text
[ ] 完成总结报告与代码实际能力一致
[ ] Excel、Rerank、测试数量不再与当前实现冲突
```

---

### 6. 补充 `.dockerignore`

**状态**：未完成。

建议补充：

```text
.venv/
web/node_modules/
web/dist/
chroma_db/
chroma_db_backup_*/
watch_directory/
backups/
models/
.pytest_cache/
.svn/
.git/
config.ini
config-prod.ini
```

验收标准：

```text
[ ] Docker build context 不包含虚拟环境
[ ] Docker build context 不包含向量库和运行数据
[ ] Docker build context 不包含 SVN/Git 元数据
[ ] Docker build context 不包含本地私有配置
```

---

### 7. Rerank 方案与任务文档口径统一

**状态**：需要文档确认。

任务文档中提到：

```text
bge-reranker
jina-reranker
Cohere-rerank
```

当前代码实际支持：

```text
bge -> FlagEmbedding，本地运行
cross_encoder -> sentence-transformers CrossEncoder，本地运行
```

当前不支持：

```text
Jina reranker API
Cohere rerank API
```

建议文档口径：

```text
当前实现采用本地 BGE/FlagEmbedding 方案，兼容 sentence-transformers CrossEncoder 作为本地备选；出于企业内网部署和数据安全考虑，暂不接入 Jina/Cohere 外部 API。
```

验收标准：

```text
[ ] 交付文档不再暗示 Jina/Cohere 已实现
[ ] config.ini.example 的 reranker 说明与实际代码一致
```

---

## 四、下一步建议执行顺序

### 第一步：处理 SVN 交付风险

```text
1. 配置 svn:ignore。
2. 清理 venv、web/dist、chroma_db、logs、watch_directory、data/chats 等污染状态。
3. 再次确认 svn status 只剩应提交代码和文档。
```

### 第二步：补检索路径一致性 [已完成]

```text
1. 移除 rag.py 中 chroma.as_retriever(...) 老分支。（已完成）
2. 同步路径改走 RetrievalStrategy.retrieve(...)。（已完成）
3. 跑 tests/test_rag_stage6.py、tests/test_retrieval_strategy.py 和全量 pytest。（已完成）
```

### 第三步：补测试和前端脚本

```text
1. 补 Excel 真实 fixture 测试。
2. 给 web/package.json 加 test/check。
3. 执行 pytest、pip check、npm run check。
```

### 第四步：更新交付文档

```text
1. 更新 RAG 工作完成总结报告。
2. 更新 Rerank 实现口径。
3. 更新 Excel 实现状态。
4. 更新测试结果口径。
```

### 第五步：测试隔离与正式库防污染收口

```text
1. P0：迁移 tests/test_review_status_bm25_sync.py 到 isolated_storage。
2. P0：pytest.ini 默认增加 addopts = -m "not integration"。
3. P1：为 integration 测试隔离 PATH_DATA_DIR，避免写正式 chunk_hit_stats.json。
4. P1：扩展 isolated_storage，隔离 watch/log/blog/crawl 等运行时目录。
5. P1：增加 pytest live-path 熔断器，禁止测试误打开正式 chroma_db / data/rag_relational.db。
6. P2：把 tests/test_graph_extraction.py 中分散的手写 setenv/reset 模式统一到 isolated_storage / reset_singletons。
```

---

## 五、交付前最终检查清单

```text
[x] Git main 已推送到 origin/main
[x] CLAUDE.md 已跟随当前项目进度更新
[x] pytest 全量通过：200 passed, 21 warnings, 8 subtests passed
[x] pip check 通过
[x] npm run build 通过
[x] 前端 citation 手动测试通过：4 passed
[x] /review/status 后 BM25 rebuild 测试通过
[x] /stats/chunks 满足任务文档基础统计要求
[x] 对话式查询上下文化测试通过
[x] Chunk stats / telemetry 测试通过
[ ] SVN 工作副本清理完成
[ ] SVN ignore 配置完成
[ ] Excel 真实 fixture 测试已补
[x] 同步/异步检索路径完全一致
[x] web/package.json 已接入 test/check
[ ] Chroma telemetry 彻底关闭
[x] 完成总结报告与当前实现完全一致
[ ] Docker ignore 补齐
[x] Rerank 文档口径与实现一致
[ ] test_review_status_bm25_sync.py 已隔离正式 Chroma
[ ] pytest 默认排除 integration 测试
[ ] integration 测试不会写正式 chunk_hit_stats.json
[ ] isolated_storage 已覆盖 watch/log/blog/crawl 运行目录
[ ] 已增加 pytest live-path 熔断器
```
