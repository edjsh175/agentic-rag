# RAG 项目待办清单（现行）

- **记录起点**：2026-07-03（交付前查漏补缺）
- **最近更新**：2026-07-28（与代码 / 配置 / 第 4 轮阶段总结对齐）
- **当前阶段**：**质量治理收尾 + 本地已开图试用（生产模板仍关）**
- **导航**：总览见 [docs/README.md](../README.md)；图谱轮次见 [知识图谱PRD剩余轮次总览](知识图谱语义抽取/2026-07-13-知识图谱PRD剩余轮次总览.md)；阶段总结见 [2026-07-27-阶段总结](知识图谱语义抽取/已完成-第4轮-GraphRAG实效验收/2026-07-27-阶段总结.md)

> 本文是**活文档**。更早的交付收口细节（Git 推送、Task 8.1 apply、审查清单逐条）见文末「历史归档摘要」与 `2_历史记录与总结/`，不再当作现行未完成项。

---

## 一、当前总览

| 主线 | 状态 | 关键口径 |
|------|:---:|----------|
| 文本 RAG（Hybrid ± Rerank、FR-10 v4） | ✅ 基线可复现 | 2537 live：39/45 = **86.67%** |
| 切块基石 0A/0B/0C/0F/0G | ✅ | 0D/0E 仍待执行；见 [切块 README](切块基石治理/README.md) |
| 图谱 MVP + Task 8.1/8.2 | ✅ | Gate **PASS** |
| 图谱第 1–3 轮 + 第 3 轮补 + R7 | ✅ | 正式库约 entities **1375** / relations **1214** |
| 主干锚定 + 锚点过滤对照 | ✅ | A1/A2 收口；过滤/allowlist 代码已落地 |
| 第 4 轮 GraphRAG A/B + 融合上限 | ✅ 检索 PASS | 生产默认 on **未批准** |
| `missing_evidence` 历史债 | ✅ 已清零 | 清债前口头 104；见 [清债纪要](知识图谱语义抽取/已完成-第4轮-GraphRAG实效验收/2026-07-27-missing_evidence清债纪要.md) |
| 本地开全图体验 | ✅ 可开 | `config.ini`：`graph_retrieval.enabled=true` 等 |
| 生产模板开图 | ❌ | `config-prod.ini` 仍 `enabled=false` |

测试与工程底座（已对齐代码）：

- 测试隔离：`isolated_storage` + live-path 熔断 + `pytest.ini` 默认 `-m "not integration"`
- 前端：`web/package.json` 已有 `test` / `check`
- Docker：已有 `.dockerignore`；生产骨架见 `deploy/README.md`
- 同步/异步检索路径已统一走 `RetrievalStrategy`

---

## 二、现行优先序（未完成 / 可选）

### P0 / 可选收口（不挡本地开图试用）

1. **`quality --graph` 全绿**
   仍缺约 2 条 golden 边 + 1 条 `illegal_relation`。补齐后再议生产默认 `graph_retrieval.enabled=true`。
   → 验证：`run_graph_build.py quality --graph` → `ok=true`

2. **生产默认开图决策**
   实效已证（专项 + 扩测），默认 on 仍需 quality 全绿 + 运维观察窗口。
   → 验证：部署建议清单勾选完成，且明确变更 `config-prod.ini`

### P1（治理债，可并行）

3. **legacy Profile 自动瘦身**
   Graph 已承载 alias / sibling 等事实；`data/migrations/retrieval_intent_profiles_v1.json` 仍待瘦身，避免双重维护。

4. **管线面表 Phase B Section / `defined_in`**
   面表 DataTable / Field / sibling 已在；`PipelineBuilder > 数据规范 > 管线面表` 类 Section 与 `defined_in` 仍可能缺失。

5. **切块 0D / 0E**
   - 0D：表格治理与策略收口（待评估）
   - 0E：OCR / 媒体 / PDF 接线（已延期，见延期决定文档）

6. **全量 `--mode qa` 生成式回答批测**
   FR-10 是检索路径冻结比较，不能混同生成式回答质量批测。

### P2（交付 / 运维）

7. **SVN 工作副本清理与 `svn:ignore`**
   Git 与 SVN 是两套状态；交付走 SVN 时仍须清运行产物污染。

8. **Excel 真实复杂 fixture 集成测试**
   基础解析已落地；多 sheet / 合并单元格 / 公式缓存等仍建议补测。

9. **Chroma anonymized telemetry 彻底关闭**
   若仍见 `Failed to send telemetry event`，在 client settings / 环境变量层显式关闭。

10. **部署风险跟踪**
    镜像标签版本化等见 [部署风险.md](部署风险.md)（工程债，非图谱主线阻断）。

---

## 三、明确不再当作「未完成」的项

下列事项在旧版待办中曾标未完成，**2026-07-28 复核已完成或已过时**：

| 旧表述 | 现行 |
|--------|------|
| 测试隔离未完成 / 默认 pytest 红 | ✅ 隔离体系已落地；以当前 `pytest` 输出为准 |
| `web/package.json` 无 test/check | ✅ 已接入 |
| 缺少 `.dockerignore` | ✅ 已有 |
| 同步/异步检索路径不一致 | ✅ 已统一 |
| 第 4 轮 / GraphRAG「仍在等待 Round 3」 | ✅ 第 3/4 轮均已收口 |
| 全图 104 条 `missing_evidence` 未清 | ✅ 已清零（另有金边/非法边缺口） |
| 前端 citation 测试只能手跑 | ✅ 已进 `npm run test` / `check` |
| 「图谱接入前」且不得影响 `/query` | ⚠️ **部分过时**：本地可开图；**生产模板仍关**；见第 4 轮总结 |

---

## 四、配置对照（现场）

| 项 | 本地 `config.ini` | 生产 `config-prod.ini` |
|----|:---:|:---:|
| `graph_retrieval.enabled` | `true` | `false` |
| `query_rewrite_enabled` | `true` | `false` |
| `anchor_chunk_filter_enabled` | `true` | `false` |
| `anchor_graph_chunk_enabled` | `true` | `false` |
| `reranker.enabled` | 本地可开 | 默认 `false` |

Intent 评分（alias / `different_from` 等）**不依赖** `graph_retrieval.enabled`，始终可读正式图做排序加减分。

---

## 五、文档与目录约定

- 图谱阶段目录一律 `已完成-*`（第 1–4 轮、主干锚定、经验产品化、锚点过滤等）；切块仅 **0D/0E** 仍为 `待执行-*`。
- 历史报告、带日期验收纪要：保留原文，文首可有「历史快照」说明；进度以本文件与轮次总览为准。
- 生成物归档：`data/` live vs `data/archive/` 见 [data目录约定.md](../5_操作指南与规范/data目录约定.md)。

---

## 六、历史归档摘要（2026-07-03 ~ 07-10）

以下为早期交付收口记录的压缩索引，细节见对应报告，**勿再当作现行任务板**：

- Git `main` 推送与交付前检查清单（2026-07-03）
- `/stats/chunks`、query 上下文化、BM25 审核同步、检索路径统一
- 知识图谱语义抽取 MVP / MVP-2（2026-07-09）
- 严格代码审查 Task 1–12 收口（2026-07-10）
- Task 8.1 / 8.2 正式库 apply + Gate PASS（2026-07-10）
  → [正式库验收报告](task81-production-validation/2026-07-10-正式库验收报告.md)

后续图谱建设（第 1–4 轮）进度以 [剩余轮次总览](知识图谱语义抽取/2026-07-13-知识图谱PRD剩余轮次总览.md) 为准，不再回填到本节逐条展开。
