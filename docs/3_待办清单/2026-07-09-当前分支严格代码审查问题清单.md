# 当前分支严格代码审查问题清单（2026-07-09）

- **审查日期**：2026-07-09
- **审查范围**：当前 `main` 工作区，重点覆盖数据污染、架构职责、Graph-RAG 一致性、评测可信度和普通代码质量。
- **审查结论（2026-07-09 初稿）**：当时不建议直接交付。P0–P4 项已于 2026-07-10 按执行计划收口（见各条「复核状态」）。
- **收口结论（2026-07-10）**：严格审查清单对应修复已落地；交付前须 `scripts/check_repo_hygiene.py` exit 0。
- **历史 Git 观察（2026-07-09）**：当时存在 untracked `.agents/`、`AGENTS.md`、`NUL` 等；现 `.agents/`、`AGENTS.md`、Profile PRD、`data/domain_catalog.json` 均已正式跟踪，`NUL` 已清理。

---

## P0：数据污染风险

### P0-1：当前工作区存在不应直接提交的 untracked 文件

**影响**

这些文件如果被 `git add .` 误提交，会把 agent 工具、临时垃圾文件、重复项目说明文档带进仓库。尤其 `.agents/` 是工具侧文件，不是业务代码；`NUL` 是 Windows 常见误生成垃圾文件；`AGENTS.md` 与已有 `CLAUDE.md` 内容高度重叠，容易造成维护口径分裂。

**证据**

```text
## main...origin/main
?? .agents/
?? AGENTS.md
?? NUL
?? docs/3_待办清单/Profile关系沉淀到知识图谱PRD.md
```

`.gitignore` 当前只忽略了部分运行数据、配置和个别零字节文件：

```text
.gitignore:45:/data/*
.gitignore:46:!/data/retrieval_intent_profiles.json
.gitignore:47:!/data/structured_retrieval_regression.json
.gitignore:56:config.ini
.gitignore:57:config-prod.ini
.gitignore:60:=1.2.0
```

**建议修复**

- 交付前清理或确认每个 untracked 文件的归属。
- 删除或移动 `.agents/`、`NUL`。
- `AGENTS.md` 要么正式替代 `CLAUDE.md` 并纳入维护，要么不要提交。
- `Profile关系沉淀到知识图谱PRD.md` 如果是正式需求文档，可以单独提交；如果只是草稿，保持待办草稿状态。

**复核状态（2026-07-10）**

- **状态**：已由后续提交确认
- **实现提交**：`af670e7`、`21b6847`、Task 12 收口提交
- **验证命令**：`.\venv\Scripts\python.exe -m pytest -q tests/test_repo_hygiene.py`
- **验证结果**：agent 文件与 data/domain_catalog.json 已跟踪；NUL 已消失；hygiene 单测 2 passed。`.agents/`、`AGENTS.md` 为有意纳入仓库。


---

### P0-2：`/rebuild` 同时支持 GET 和 POST，且会触发清库重建

**影响**

这是当前最危险的生产风险之一。`GET /rebuild` 被浏览器访问、预加载、爬虫、误点链接或监控探测触发时，可能直接清空 Chroma 集合和文件索引，然后重新扫描。当前锁只能防并发，不能防误触发。

**证据**

```text
rag_knowledge/api/routes.py:539:@router.api_route("/rebuild", methods=["GET", "POST"])
rag_knowledge/api/routes.py:542:    重建知识库 —— 清空向量数据库 + 文件索引，然后全量重新扫描
rag_knowledge/api/routes.py:549:    注意：此操作不可逆，执行后所有已有问答数据将被清空
rag_knowledge/api/routes.py:552-560: RebuildCoordinator(...).run()
```

实际执行路径会清 Chroma、重置 index：

```text
rag_knowledge/services/rebuild_coordinator.py:57:self._store.clear()
rag_knowledge/services/rebuild_coordinator.py:59:self._scanner.reset_index()
rag_knowledge/repository/vector_store.py:347:store.delete_collection()
```

**建议修复**

- `/rebuild` 改成 **POST-only**。
- 增加显式确认参数，例如 `confirm=true`、`operation_id` 或管理员 token。
- 更稳妥方案：拆成 `/rebuild/plan` 与 `/rebuild/execute` 两步，先输出影响范围，再执行。

**复核状态（2026-07-10）**

- **状态**：已修复
- **实现提交**：`e5376f6`
- **验证命令**：`.\venv\Scripts\python.exe -m pytest -q tests/test_routes_consistency.py`
- **验证结果**：GET /rebuild → 405；POST 须 confirmation=REBUILD_KNOWLEDGE_BASE。


---

### P0-3：重建流程非原子，失败后可能留下“空库 + 半残 index”

**影响**

当前流程只备份 `file_index.json`，然后先清 Chroma，再重置 index，再扫描。如果扫描、embedding、Chroma 写入或一致性校验中途失败，正式知识库已经被清掉，恢复成本高。SQLite 图谱也没有随重建做一致快照。

**证据**

```text
rag_knowledge/services/rebuild_coordinator.py:46-48: 仅备份 file_index.json
rag_knowledge/services/rebuild_coordinator.py:57: self._store.clear()
rag_knowledge/services/rebuild_coordinator.py:59: self._scanner.reset_index()
rag_knowledge/services/rebuild_coordinator.py:60: result = self._scanner.scan()
rag_knowledge/services/rebuild_coordinator.py:61: self._consistency_service.assert_consistent()
```

`reset_index()` 会直接写入空索引：

```text
rag_knowledge/services/scanner.py:156:self._index = {"version": 1, "files": {}}
rag_knowledge/services/scanner.py:157:self._save_index()
```

**建议修复**

- 重建改成两阶段提交：新建临时 Chroma collection/临时 index，扫描成功并通过一致性校验后再切换正式 collection。
- 最低限度也要在执行前同时备份 `file_index.json`、Chroma 目录、SQLite 图谱库。

**复核状态（2026-07-10）**

- **状态**：已修复
- **实现提交**：`a98ee28`、`2b50876`
- **验证命令**：`.\venv\Scripts\python.exe -m pytest -q tests/test_rebuild_coordinator.py tests/test_rebuild_staging.py`
- **验证结果**：staging 切换与失败回滚路径有单测覆盖。


---

### P0-4：`run_eval_full.py` 默认覆盖正式评测集路径

**影响**

评测集是检索指标可信度的基础。该脚本一运行就重建 `./data/eval_dataset.json`，没有确认、没有时间戳、没有 `--output` 参数保护。它不会污染 Chroma，但会污染评测基准，导致历史 A/B 结果失去可比性。

**证据**

```text
run_eval_full.py:24:builder = TestDatasetBuilder(questions_per_chunk=2, max_chunks=100)
run_eval_full.py:25:dataset = builder.build("./data/eval_dataset.json")
run_eval_full.py:35:runner = EvaluationRunner("./data/eval_dataset.json")
```

**建议修复**

- 改成 CLI。
- 默认输出 `data/eval_dataset_YYYYMMDD-HHMMSS.json`。
- 覆盖已有文件必须传 `--overwrite`。

**复核状态（2026-07-10）**

- **状态**：已修复
- **实现提交**：`86725e7`
- **验证命令**：`.\venv\Scripts\python.exe -m pytest -q tests/test_run_eval_full.py`
- **验证结果**：默认不覆盖 data/eval_dataset.json；覆盖须 --overwrite。


---

## P1：架构职责错误

### P1-1：领域事实仍写在 Retrieval Profile 里，形成多个事实源

**影响**

`retrieval_intent_profiles.json` 里存了 alias、章节族、召回词、偏好来源、同级惩罚组。这些已经不只是“检索策略”，而是领域事实。这样会导致事实同时存在于 Profile、Graph、代码规则中，后期维护会分裂：图谱里改了 alias，但 Profile 仍然可能把检索拉回旧事实。

**证据**

```text
data/retrieval_intent_profiles.json:3:"id": "pipeline_point_table"
data/retrieval_intent_profiles.json:4:"entity_aliases": ["管线点表", "点数据结构"]
data/retrieval_intent_profiles.json:6:"recall_terms": ["点数据结构", "管点编号", "地面高程", "字段名", "说明"]
data/retrieval_intent_profiles.json:7-8:"section_families": [["PipelineBuilder > 数据规范 > 管线点表", ...]]
```

Profile 会参与 query 扩展和质量分调整：

```text
rag_knowledge/services/retrieval_intent.py:31-43: expand_query() 使用 recall_terms 扩展 query
rag_knowledge/services/retrieval_intent.py:51-66: apply_quality_scores() 修改 quality_score
```

**建议修复**

- Profile 只保留策略参数，例如 `candidate_min_k`、是否扩大候选池、是否 rerank。
- `entity_aliases`、`section_families`、`sibling_penalty_groups`、`preferred_sources` 应迁入图谱或图谱种子数据。

**复核状态（2026-07-10）**

- **状态**：已修复
- **实现提交**：`9c53aad`、`4bb8530`、`69775ec`
- **验证命令**：`.\venv\Scripts\python.exe -m pytest -q tests/test_retrieval_intent.py tests/test_profile_graph_sync.py`
- **验证结果**：policy-only 运行时；正式库 profile_sync 已 apply；Task 8.1 Gate PASS。


---

### P1-2：图谱 schema 文件里混入产品/工具/服务词典

**影响**

Schema 应该定义“有哪些实体类型、关系类型、合法关系”，不应该内置具体产品事实。现在 `graph_schema.py` 里写了 `PipelineBuilder`、`DOMBuilder`、`管线发布服务` 等具体实体，导致每新增一个产品/服务都要改 schema 代码。

**证据**

```text
rag_knowledge/models/graph_schema.py:179-190: KNOWN_TOOL_NAMES
rag_knowledge/models/graph_schema.py:192-196: KNOWN_SERVICE_NAMES
```

这些词典又被抽取器用来生成实体和关系：

```text
rag_knowledge/services/graph_extraction/__init__.py:126-135: 根据 KNOWN_TOOL_NAMES / KNOWN_SERVICE_NAMES 创建 Tool/Service 和 belongs_to
```

**建议修复**

- 把这些词典移出 schema。
- 建议放到 `data/graph_seed_entities.json`、`data/domain_catalog.json` 或 SQLite 图谱种子表。

**复核状态（2026-07-10）**

- **状态**：已修复
- **实现提交**：`af670e7`、`6dfe00c`
- **验证命令**：`.\venv\Scripts\python.exe -m pytest -q tests/test_domain_catalog.py`
- **验证结果**：KNOWN_* 词典迁出 schema；data/domain_catalog.json 已跟踪。


---

### P1-3：`GraphSpecialRuleRestorer` 是长期不可维护的规则补丁

**影响**

这里硬编码了 alias 和 different_from，而且插入时 `source_chunk_id` 为空、自动 approved。它绕过了正常的证据链和审核机制。短期能救一个误召回问题，长期会变成“谁也不知道为什么存在”的黑盒规则。

**证据**

```text
rag_knowledge/services/graph_extraction/pipeline.py:314-324:
DIFFERENT_FROM_RULES = (
    ("PipelineBuilder", "管线发布服务"),
    ("PipelineBuilder", "PipelinePublishConfig"),
)
ALIAS_RULES = (
    ("PipelineBuilder", "管线发布工具"),
)
```

插入 alias 时没有真实 chunk 证据：

```text
rag_knowledge/services/graph_extraction/pipeline.py:344-352:
source_chunk_id = ''
evidence_text = special_rule:...
review_status = 'approved'
```

**建议修复**

- 删除 hardcoded restorer。
- 改成可审计的 graph seed migration。
- 每条 alias / different_from 至少要有来源：来自 Profile 迁移、人工录入、或某个 chunk 证据。

**复核状态（2026-07-10）**

- **状态**：已修复
- **实现提交**：`6dfe00c`
- **验证命令**：`.\venv\Scripts\python.exe -m pytest -q tests/test_graph_extraction.py`
- **验证结果**：GraphSpecialRuleRestorer 已删除；事实经 catalog seed + review/apply 写入。


---

## P2：Graph-RAG 一致性

### P2-1：职责大体有分层，但边界还不干净

**影响**

Graph Retriever、Entity Guard、Query Planner、Profile 各有职责，但 Profile 仍承担了图谱事实功能；Schema 也承担了领域词典功能。结果是 Graph-RAG 已经接起来了，但事实源没有完全统一。

**证据**

Query Planner 主要做意图分类和通用阶段词扩展，这部分职责合理。Graph Retriever 负责实体链接、图扩展、证据 chunk 加载、融合，这也合理。但 Profile 仍存 alias、section family、source preference；Schema 仍存具体产品词典。

**建议修复**

建立明确边界：

- Query Planner：只判断意图和检索参数。
- Profile：只放策略偏好，不放领域事实。
- Graph Schema：只放实体/关系类型和约束。
- Graph DB：唯一事实源，存 alias、different_from、belongs_to、has_table、has_field、defined_in。

**复核状态（2026-07-10）**

- **状态**：已修复
- **实现提交**：`9c53aad`、`4bb8530`
- **验证命令**：`.\venv\Scripts\python.exe -m pytest -q tests/test_retrieval_intent.py tests/test_graph_retrieval.py`
- **验证结果**：Profile 仅存策略；图谱为运行时领域事实源。


---

### P2-2：Graph Retriever 能利用图关系，但只能利用“已经进入图谱”的事实

**影响**

Graph Retriever 已经能利用 alias、different_from、belongs_to、has_table、has_field、defined_in 等关系；但如果这些事实还停留在 Profile，它就无法统一使用。也就是说 Phase C 的能力存在，但数据迁移没完成。

**证据**

关系扩展白名单：

```text
rag_knowledge/services/graph_retrieval.py:418-425:
procedure/deployment/config/definition/comparison/troubleshooting 对应不同 relation allowlist
```

方向规则支持：

```text
rag_knowledge/services/graph_retrieval.py:448-490:
belongs_to / uses_config / has_table / has_field / defined_in 的方向判断
```

图证据最后通过 chunk_id 取回：

```text
rag_knowledge/services/graph_retrieval.py:646-647:
collection.get(ids=list(context.chunk_ids), include=["documents", "metadatas"])
```

**建议修复**

做 `Profile → Graph migration/sync`：

- `entity_aliases` → `aliases`
- `section_families` → `Section` 实体 + `defined_in`
- `sibling_penalty_groups` → `different_from`
- `preferred_sources` / `fallback_sources` → 如果是事实，转成 `belongs_to` / `defined_in`；如果只是策略，保留在 Profile，但命名成 policy。

**复核状态（2026-07-10）**

- **状态**：已修复
- **实现提交**：`9c53aad`、`4bb8530`
- **验证命令**：`.\venv\Scripts\python.exe scripts/validate_task81_graph_gate.py --json`
- **验证结果**：profile_sync 事实已写入正式图谱；GraphRetriever 消费 DB 关系。


---

### P2-3：`defined_in` 的语义存在注释/模型不一致

**影响**

Schema 里 `defined_in` 是 `Entity -> Document/Section`，但 GraphExpander 注释写成 `Entity defined_in Chunk`。虽然代码实际通过 `entity_chunk_links` 找 chunk，但这个注释会误导后续维护者把 `defined_in` 直接连到 chunk。

**证据**

Schema 口径：`defined_in = Entity -> Document/Section`。

GraphExpander 注释：

```text
rag_knowledge/services/graph_retrieval.py:488-490:
# Entity defined_in Chunk
return direction == "forward"
```

**建议修复**

- 统一语义：`defined_in` 只连 Document/Section。
- chunk 证据统一走 `entity_chunk_links`。

**复核状态（2026-07-10）**

- **状态**：已修复
- **实现提交**：`db89023`
- **验证命令**：`.\venv\Scripts\python.exe -m pytest -q tests/test_knowledge_graph.py`
- **验证结果**：defined_in 目标限定 Document/Section；chunk 证据走 entity_chunk_links。


---

## P3：评测可信度

### P3-1：已有 dataset health check，但 stale chunk_id 仍可能被“软放过”

**影响**

现在健康检查已经能识别 chunk_id 是否存在、expected_targets 是否还能匹配，这是好设计。但如果 chunk_id 失效而 expected_targets 还能匹配，当前状态只是 warning，不一定阻断 A/B。严格 A/B 下，这会让历史 ID 失效问题被 content fallback 掩盖。

**证据**

健康检查存在：

```text
rag_knowledge/evaluation/runner.py:36-54: load() 默认调用 _check_health()
```

状态逻辑：

```text
rag_knowledge/evaluation/dataset_health.py:
status = "PASS" if invalid_questions == 0 else "BLOCK"
warnings.append("chunk ids are stale, but expected_targets still match current chunks")
```

**建议修复**

- 严格模式下，只要 `needs_chunk_id_refresh=True` 就阻断 A/B，除非显式传 `--allow-stale-ids`。

**复核状态（2026-07-10）**

- **状态**：已修复
- **实现提交**：`7519fbd`
- **验证命令**：`.\venv\Scripts\python.exe -m pytest -q tests/test_dataset_health.py`
- **验证结果**：needs_chunk_id_refresh 且未传 --allow-stale-ids 时 status=BLOCK。


---

### P3-2：content fallback 会污染 recall 分母

**影响**

这是核心可信度问题。当前逻辑在 chunk_id 没命中时，如果 content fallback 命中，就把 `relevant` 改成 `matched_ids`。这等于把“检索出来的文档”反过来写成“标准答案集合”，Recall 的分母会变小，指标可能虚高。

**证据**

Fallback 改写 relevant：

```text
rag_knowledge/evaluation/runner.py:106-117:
if matched_ids:
    relevant = matched_ids
```

Recall 分母来自 `len(relevant_ids)`：

```text
rag_knowledge/evaluation/metrics.py:10-15:
return len(top_k & relevant_ids) / len(relevant_ids)
```

**建议修复**

- 不要在评测循环里把 `relevant` 替换为 `matched_ids`。
- 评测前先用 expected_targets 生成“当前库 chunk_id 映射”，刷新数据集。

**复核状态（2026-07-10）**

- **状态**：已修复
- **实现提交**：`7519fbd`
- **验证命令**：`.\venv\Scripts\python.exe -m pytest -q tests/test_metrics.py -k content_fallback`
- **验证结果**：Recall/MRR 分母保持原始 relevant_ids；content_fallback_hit_rate 独立输出。


---

### P3-3：fail-on-regression 是 opt-in，不是默认防线

**影响**

`run_retrieval_ab.py` 支持回归检测，这是好的；但必须传 `--fail-on-regression` 才会失败。实际交付/CI 中如果忘了加这个参数，指标下降只会写入结果，不会阻止交付。

**证据**

```text
run_retrieval_ab.py: parser.add_argument("--fail-on-regression", action="store_true")
run_retrieval_ab.py: if regressions and args.fail_on_regression: raise SystemExit(...)
```

**建议修复**

- CI 脚本默认启用 fail-on-regression。
- 本地脚本可以保留 `--allow-regression` 作为反向参数。

**复核状态（2026-07-10）**

- **状态**：已修复
- **实现提交**：`7519fbd`
- **验证命令**：`.\venv\Scripts\python.exe -m pytest -q tests/test_run_retrieval_ab.py`
- **验证结果**：A/B 默认检测回归即非零退出；仅 --allow-regression 放行。


---

### P3-4：评测默认 `review_status=None`，与生产检索口径不一致

**影响**

生产检索默认只返回 approved；评测时不限制审核状态，会让 pending/rejected chunk 也可能参与检索。这样指标可能比真实线上表现更乐观，尤其在新资料刚入库未审核时。

**证据**

```text
rag_knowledge/evaluation/runner.py:95:
review_status=None  # 评估时不限制审核状态
```

**建议修复**

至少输出两套指标：

- `prod_eval`：`review_status="approved"`
- `audit_eval`：`review_status=None`


**复核状态（2026-07-10）**

- **状态**：已修复
- **实现提交**：`7519fbd`
- **验证命令**：`.\venv\Scripts\python.exe -m pytest -q tests/test_metrics.py -k review_scope`
- **验证结果**：EvaluationRunner 默认 review_status=approved；metrics 输出 review_scope。


---

## P4：普通代码质量

### P4-1：`GraphRetriever` 依赖 `ChunkAdminService()._file_lookup()` 私有方法

**影响**

私有方法 `_file_lookup()` 被 Graph Retriever 直接调用，说明服务边界不清。以后 ChunkAdminService 内部实现变化，会破坏 Graph Retriever。Graph Retriever 不应该依赖 admin service 的私有细节。

**证据**

```text
rag_knowledge/services/graph_retrieval.py:666 起:
if kb_name:
    from rag_knowledge.services.chunk_admin import ChunkAdminService
    files = ChunkAdminService()._file_lookup()
```

**建议修复**


**复核状态（2026-07-10）**

- **状态**：已修复
- **实现提交**：`1b127ca`
- **验证命令**：`.\venv\Scripts\python.exe -m pytest -q tests/test_chunk_index_lookup.py tests/test_graph_retrieval.py`
- **验证结果**：GraphRetriever 不再调用 ChunkAdminService._file_lookup()。


---

### P4-2：`/rebuild` 在 scanner 未初始化时返回信息不准确

**影响**

如果 `_scanner` 不存在，接口返回“知识库已清空，但扫描器未初始化”，但代码并没有执行 `VectorStore().clear()`。这会误导运维判断。

**证据**

```text
rag_knowledge/api/routes.py:561:
return {"message": "知识库已清空，但扫描器未初始化"}
```

**建议修复**


**复核状态（2026-07-10）**

- **状态**：已修复
- **实现提交**：`e5376f6`
- **验证命令**：`.\venv\Scripts\python.exe -m pytest -q tests/test_routes_consistency.py`
- **验证结果**：scanner 未初始化时 HTTP 503，文案含「未执行重建」。


---

### P4-3：测试覆盖面不错，但缺少几个关键防线测试

**影响**

当前测试目录覆盖 graph、retrieval、dataset health、storage isolation、rebuild coordinator 等模块，基础不错。但最危险的路径没有被测试锁死，容易回归。

**证据**

已有隔离机制：

```text
pytest.ini:addopts = -m "not integration"
tests/conftest.py: isolated_storage 将 Chroma、SQLite、data、logs、watch_dir 等重定向到 tmp_path
rag_knowledge/config.py:_assert_test_paths_are_isolated() 在 pytest 下拒绝正式路径
```

缺口：

- 没有测试 `/rebuild` 禁止 GET。
- 没有测试 `/rebuild` 必须带确认参数。
- 没有测试 content fallback 不得修改 recall 分母。
- 没有测试 Profile facts 全部迁入 Graph 后 Profile 不再作为事实源。
- 没有交付前检查 untracked agent/debug 文件。

**建议修复**


**复核状态（2026-07-10）**

- **状态**：已由后续提交确认
- **实现提交**：`e5376f6`、`7519fbd`、`9c53aad`、Task 12 收口提交
- **验证命令**：`.\venv\Scripts\python.exe -m pytest -q tests/test_repo_hygiene.py tests/test_routes_consistency.py tests/test_metrics.py`
- **验证结果**：rebuild/eval/profile/hygiene 防线均有单测；scripts/check_repo_hygiene.py 已落地。


---

### P4-4：Graph-RAG 相关融合逻辑在 `rag.py` 中有重复迹象

**影响**

`GraphRetriever.fuse` 和 `graph_kwargs` 构建在同步、流式、异步路径中多处出现。重复逻辑容易造成“普通问答正常，流式问答不一致”的问题。

**证据**

```text
rag_knowledge/services/rag.py:481-490
rag_knowledge/services/rag.py:928-936
rag_knowledge/services/rag.py:1012-1020
rag_knowledge/services/rag.py:1338-1347
rag_knowledge/services/rag.py:1439-1447
rag_knowledge/services/rag.py:1551-1559
```

**建议修复**


**复核状态（2026-07-10）**

- **状态**：已修复
- **实现提交**：`971796c`
- **验证命令**：`.\venv\Scripts\python.exe -m pytest -q tests/test_rag_stage6.py tests/test_graph_retrieval.py`
- **验证结果**：query/aquery/stream 与检索路径共用 _build_graph_kwargs、_fuse_graph_docs。


---

## 建议修复顺序

1. **先处理 P0**：清理 untracked 文件；禁止 GET rebuild；重建流程加确认和原子性；评测脚本禁止默认覆盖。
2. **再处理 P1/P2**：把 Profile 中的领域事实迁到 Graph，移除 hardcoded special rules。
3. **然后处理 P3**：修正 content fallback 指标污染，把 fail-on-regression 变成 CI 默认。
4. **最后处理 P4**：清理私有方法依赖和 Graph-RAG 重复逻辑。

---

## 交付门槛建议

交付前至少满足以下条件（2026-07-10 复核：均已落地，以 hygiene 脚本为准）：

- `.env\Scripts\python.exe scripts/check_repo_hygiene.py` exit 0（工作树干净、无 NUL/tmp/debug 垃圾、`data/domain_catalog.json` 已跟踪）。
- `/rebuild` 仅 POST，且须 `confirmation=REBUILD_KNOWLEDGE_BASE`。
- 破坏性重建走 staging 切换，失败自动回滚。
- 固定评测集默认不覆盖。
- A/B 默认 fail-on-regression（`--allow-regression` 反向放行）。
- content fallback 不污染 Recall/MRR 分母。
- Profile 仅存策略；领域事实由 Graph DB 承载（正式库 profile_sync 已 apply）。

**全量验证（2026-07-10）**

```text
.env\Scripts\python.exe -m pytest -q
.env\Scripts\python.exe -m pip check
cd web && npm run check
git diff --check
.env\Scripts\python.exe scripts/check_repo_hygiene.py
```

**验证结果**：`480 passed, 6 deselected`；`pip check` OK；`npm run check` OK（node 4/4、vitest 20/20、build 成功）。
