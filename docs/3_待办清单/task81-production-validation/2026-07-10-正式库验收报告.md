# Task 8.1 / 8.2 正式库验收报告

- **日期**：2026-07-10
- **范围**：`pipeline_point_table` / `pipeline_line_table` / `pipeline_face_table` / `dom_builder_publish` 四个 Profile 的 Graph 事实迁移
- **结论**：**通过** — 正式库已 apply，专项 Gate `PASS`

---

## 1. 背景与目标

Task 8.1 切断 legacy 评分双读后，运行时意图评分依赖正式 Graph 中的 approved 事实（alias、`different_from`、scoped Field 等）。Task 8.2 补齐 Schema 兼容与 Profile Migration 管线。

正式库验收目标：

1. Gate 只读复验为 `NEEDS_APPLY`（缺口仅为预期事实）
2. dry-run 无裸 Field/Section、无阻断 diagnostic
3. 分拆审批后 apply 至正式 Graph 主表
4. apply 后 Gate 为 `PASS`，dry-run 无 actionable 候选

---

## 2. 阶段 1：Gate 只读复验（apply 前）

**命令**：`scripts/validate_task81_graph_gate.py --json`

| 项 | 结果 |
|---|---|
| verdict | `NEEDS_APPLY` |
| 运行时缺口 | 点表/线表 alias 与 `different_from`；面表整表未加载 |
| 阻断项 | 无（无 `pending_only_*` / `rejected_*` / `ambiguous_section_entity` / 裸实体） |
| preview 可覆盖 | entity 4 / alias 3 / relation 14（跨 profile 去重前） |
| global_graph_quality | `missing_evidence_count = 104`（历史债，不改变专项判定） |

---

## 3. 阶段 2：逐 Profile dry-run

四份 JSON 见本目录 `dry-run-*.json`。

| Profile | entities | aliases | relations | diagnostics |
|---|:---:|:---:|:---:|:---:|
| pipeline_point_table | 1 | 1 | 4 | 2 × `generic_recall_term` |
| pipeline_line_table | 1 | 1 | 4 | 2 × `generic_recall_term` |
| pipeline_face_table | 2 | 1 | 6 | `missing_section_entity` + 2 × `generic_recall_term` |
| dom_builder_publish | 0 | 0 | 0 | 0 |

核对通过：Field 均为限定名；无裸 Section；alias 仅进 aliases 候选；`dom_builder_publish` 无 actionable 项。

---

## 4. 阶段 3–6：备份、staging、分拆审批

### 备份

- 路径：`backups/rag_relational-pre-task81-apply-20260710-151353.db`
- SHA256 与 `data/rag_relational.db` 一致

### Batch

| 项 | 值 |
|---|---|
| batch_id | `e8267357-e5d2-41e8-9848-b37383be7b1f` |
| mode | `profile_sync` |
| review_status | `pending`（staging）→ 分拆审批 → `approved` |

staging 写入 20 候选（含 7 diagnostic）；actionable 去重后 **13 项**全部 approved：

- 实体 2：`管线面表`（DataTable）、`管线面表.管面编号`（Field）
- alias 3：点数据结构 / 线表数据结构 / 面表数据结构
- 关系 8：`belongs_to`×3、`has_table`×1、`has_field`×1、`different_from`×3

未批准：`defined_in`（无候选）、全部 diagnostic。

`quality --batch`：**ok = true**

---

## 5. 阶段 7：正式 apply

**命令**（含生产写确认参数）：

```powershell
.\venv\Scripts\python.exe run_graph_build.py apply `
  --batch e8267357-e5d2-41e8-9848-b37383be7b1f `
  --confirm-db-path "<项目>/data/rag_relational.db" `
  --confirm-batch e8267357-e5d2-41e8-9848-b37383be7b1f `
  --confirm-backup "<项目>/backups/rag_relational-pre-task81-apply-20260710-151353.db"
```

| 指标 | before | after | Δ |
|---|---:|---:|---:|
| entities | 702 | 704 | +2 |
| relations | 1615 | 1623 | +8 |
| aliases | 1 | 4 | +3 |

---

## 6. 阶段 8：apply 后专项复验

| 检查 | 结果 |
|---|---|
| Gate | **`PASS`**，`issues = []` |
| dry-run | 四 profile 无 actionable entity/alias/relation |
| 专项 pytest | **17 passed** |
| 全量 pytest | **471 passed**, 2 failed, 6 deselected |

全量 2 项失败与本次 apply 无关（CLI review `missing_or_not_pending` 计数断言）：

- `tests/test_graph_extraction.py::test_graph_build_cli_review_reports_invalid_candidate_ids`
- `tests/test_graph_quality_cli.py::test_review_reports_requested_selected_and_safety_rejected`

---

## 7. 阶段 9：全图质量（单独记录）

| 项 | 值 |
|---|---|
| ok | false（历史债） |
| missing_evidence_count | **104**（本次未新增） |
| invalid_schema_count | 0 |
| type_conflict_unresolved_count | 0 |
| stale_link_count | 0 |

104 条 Phase B 历史 `missing_evidence` 另开数据治理任务，不纳入 Task 8.1 专项判定。

---

## 8. 已知残留

| 项 | 影响 | 后续 |
|---|---|---|
| 管线面表无 Phase B Section | `section_paths` 为空；dry-run 仍有 `missing_section_entity` | Phase B 抽取 + `defined_in` 治理 |
| legacy migration 文件未瘦身 | alias/recall_terms 仍保留在 `retrieval_intent_profiles_v1.json` | Profile 自动瘦身（待办 §三.1） |
| 2 项 CLI review 测试失败 | 不影响 Task 8.1 专项验收 | 交付前修复断言 |

---

## 9. 交付状态表述（口径）

- **Task 8.1**：代码完成，正式 Graph 事实已 apply 并通过专项 Gate
- **Task 8.2**：正式 Graph Schema 兼容与 Profile Migration 修复完成
- **全图质量**：仍有 104 条 Phase B 历史 `missing_evidence`，另行治理
